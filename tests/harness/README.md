# Test harnesses (Chromium headless + node/python unit tests)

כל קובץ כאן הוא הוכחה שהתכונה עובדת: מרנדר את `index.html` האמיתי ב-Chromium headless
(375px, רשת חסומה), או מריץ את קוד השרת/הכלים האמיתי עם `fetch`/`requests` מדומים.
נכתבו בסשן 2026-09-18/19 (PRs #535–#577) ורצו ירוק על `main` בסיום.

```
npm i -g playwright            # פעם אחת (הדפדפנים: npx playwright install chromium)
bash tests/harness/run.sh      # הכל
bash tests/harness/run.sh trustee   # רק הנאמנים
```

חבילה שלא מדפיסה שורת סיכום = **כשלון** (קרסה או עברה את הזמן). שתי היוצאות היחידות — `sc-test.js` ו-`notif-scan-test.js` — מודעות ב-`NO_ASSERTIONS` בתוך `run.sh`; **אין בהן אף בדיקה**, הן מדפיסות דוח בלבד.


| קובץ | מה מוכיח |
|---|---|
| trustee-t1-test.js | חיווט `trustee_reports`, קטלוג 8 המשימות, ניקוד לפי המסמך |
| trustee-t2-test.js | מסך הנאמן: הסבר, חודש, מי מוביל, טופס סיור (משימות × פריטים), סגירה «צלם אחרי», pull |
| trustee-t3-test.js | מסך המנהל: פילים, לוח ניקוד, ממצאים, ⋯ (נסגר/פתח/משימה/הערה), CSV, היום |
| trustee-roster-test.js | רשימת הנאמנים: עורך המנהל, בוררים, לוח כולל 0 |
| notif-trustee-test.js | הגדרות התראות: שורת «ליקוי מנאמן», יעדים, «שלח בדיקה», toast ב-realtime |
| trustee-notify-test.mjs | הפונקציה `functions/api/trustee-notify.js` עם fetch מדומה (24 בדיקות) |
| sw-cache-test.mjs | ה-service worker האמיתי מ-`functions/sw.js.js`, רץ ב-global מדומה: install/activate, מעטפת מהמטמון בלי להמתין לרשת, אופליין, מה שלא נוגעים בו, ודיפלוי שמגיע למשתמש |
| console-quiet-test.js | הקונסול שקט: אין `console.log`/`console.info` חשופים, `dlog()` כבוי כברירת מחדל, `tfgnDebug(true)` / `?debug=1` מדליקים, והדשבורד נטען בלי חריגות בשני המצבים |
| home-button-test.js | כפתור 🏠: קיים ומחזיר הביתה מ-6 עמודים, הלוגו עושה אותו דבר ומסמן זאת, השורה לא גולשת ב-360–768px, המונה עובר בין 📝 ל-☰, ובמצב נאמן השורה נשארת נקייה |
| search-test.js | חיפוש אחד במקום שניים: הכפילות מחקה עד השורה האחרונה, סיכונים נוספו כדי שלא יאבדו, NCR רגיש מוסתר ממנהל, ↑↓/Enter, ו-Ctrl+K / `/` מגיעים לאותו חיפוש |
| ncr-dates-test.js | נכונות NCR: מסנני שבוע/חודש/רבעון מוצאים רשומות קיימות, תיאור שנראה כמו תאריך לא מתגנב, מיון לפי תאריך גילוי, מספר NCR לעולם לא מונפק פעמיים, ויעד ריק נשמר כ-null |
| expiry-test.js | דף התפוגות: תאריך חסר לא מוצג ירוק, «≤30» כולל את מה שכבר פג, לשונית «ללא תאריך», המונים מסכימים עם הרשימות, וכפתורי צפה/צור-משימה על כל שורה |
| storage-full-test.js | אחסון מלא: שמירה שלא נכתבה מדווחת ככישלון, הפעולה נשמרת בזיכרון ונשלחת כשיש מקום, המונה לא מראה 0, והעותק המקומי לא נמחק |
| force-sync-test.js | «סנכרון מלא» הוא טעינה בלבד (אפס POST), ההעלאה היא פעולה נפרדת שמזהירה על דריסת השרת, ושתיהן מופיעות בתפריט בשמות נכונים |
| delete-guards-test.js | מחיקה: האישור מציין מה נמחק, טבלה מוגנת מקבלת אזהרה, יומן הביקורת רושם כותרת, הסיסמה ניתנת לשינוי, ומרשם השמיעה דורש גיבוי+סיסמה |
| prefill-fonts-test.js | תאריך יעד ואחראי נמלאים מדיווח הנאמן, והדף מצויר גם כשהבקשה ל-Google Fonts נבלעת בשתיקה |
| trustee-draft-test.js | טיוטת סיור: סיור שלא נשלח שורד סגירה בטעות עם הממצאים והתמונות, סירוב מוחק לצמיתות, דיווח סגירה לעולם לא מקבל טיוטה, וטופס ריק אינו טיוטה |
| edit-everywhere-test.js | עריכה ב-14 המודולים שהיו צפייה+מחיקה בלבד: שמירה חדשה = insert, עריכה = update על אותה שורה, הטופס נמלא מהרשומה, תיבות הסימון של היתר עבודה חוזרות, ופתיחה רגילה מתחילה רשומה חדשה |
| vitre-review-test.mjs | ערוץ ה-SMS דרך Vitre: `notify` (כל משתמש צוות, נעול לטופס 11998, מגיש = עובד המערכת 9001 או `VITRE_NOTIFY_CREATED_BY`, נמען = מגיש נדחה, `to/title/details/link`), פותר הנמען בצד הלקוח (טלפון ואז שם, רק עובדים עם `ext_id`, אחרת null ולא נשלח כלום); ובדיקת ההתראות דרך ה-API (`vitre.js` האמיתי, fetch מדומה): `review_submit_test` הוא POST בלבד, אדמין בלבד, נעול בקוד לטופס 11997 (reviewId בגוף הבקשה נזרק), מאמת גוף, מחזיר את תשובת Vitre כמו שהיא; `review_schema` קריאה לאדמין; מוצא המפתחות בצד הלקוח קורא שאלה/תשובה משתי צורות סכמה ומחזיר null כשאין התאמה |
| vitre-auto-sync-test.mjs | סנכרון יומי Vitre → `toolbox` (הקוד האמיתי מ-`index.html`, fetch/DB/localStorage מדומים): רץ רק לאדמין אחרי המשיכה מהשרת, פעם ביום, מייבא רק `ext_id` שאין אצלנו באותה צורת שורה כמו הידני, שקט כשאין חדש, לא מסמן יום כש-Vitre נפלה, לא רץ לצד התצוגה המקדימה |
| ncr-import-test.js | ייבוא NCR: גיליון לטבלה עם 375 רשומות אכן מייבא (היה מייבא אפס), מיספור מהפנוי הבא, ייבוא חוזר לא משכפל, והייבוא האוטומטי לא מנפיק מספר של רשומה חיה |
| interactions-test.js | המפגש בין תשעת המיזוגים של 19/09 — מצב עריכה מול טיוטת סיור, תגי תפוגה, מיספור מול מחיקה, ותור הזיכרון מול הסנכרון |
| daily-loop-test.js | חמשת הדברים שמיכאל נוגע בהם כל בוקר: סבב הבוקר נפתח מאוכלס, «משימות להיום» כולל פיגור, תג 📝 סופר לפי שם ולא מול אימייל (והתאמה מדויקת, לא תת-מחרוזת), הסריקה היומית רצה פעם ביום גם אחרי פתיחה קרה ו«סבב חסר» יורה בנפרד, וכרטיס 7 הימים כולל בדיקות ציוד. שלושת התיקונים נבדקו בהחזרת הבאג |
| registers-dataloss-test.js | שתי דרכים שבהן מרשם איבד את הראיה שהוא קיים בשבילה: עריכת הדרכה שומרת את התעודה (והסרה מכוונת עדיין עובדת, ורשומה בלי קובץ לא יורשת מהקודמת), וגיבוי השמיעה מכיל שם, ת"ז, תאריך, בודק ומחלקה במקום חמש עמודות ריקות. נבדק בהסרת התיקון |
| ncr-core-test.js | ארבעת היסודות של NCR: שדה העדיפות קיים ושומר (והתראת «NCR קריטי» סוף-סוף יורה), עריכה לא מאפסת עדיפות וטופס ריק לא יורש מהקודם, הצפייה וה-PDF מציגים סיבת שורש ופעולה מתקנת תחת התוויות שלהן, פתיחה מחדש מבטלת את האימות ומחזירה לתור, ואף מסנן לא קורא את התיאור כתאריך |
| backup-coverage-test.mjs | כיסוי הגיבוי: ה-cron הלילי מכסה את כל 35 הטבלאות שמחזיקות עובדה (כולל כל תוכנית הנאמנים ו-`ncr_ai`/`audit_log` שלא היו באף גיבוי), אף שם ב-`_BACKUP_TABLES` אינו טבלה שלא קיימת ב-`DB` (המלכודת שכמעט נפלתי בה), ה-cron מדפדף עם `Range`, ו-`/health` מחזיר 503 לריצה שנכשלה או שבת 20 יום — במקום 'ok' תמיד |
| storage-scope-test.js | גבול ה-Storage: כל תמונת סיור נשמרת תחת `tru-ph-` (הכפתור מעביר את מזהה האזור כ-prefix, ו-`_fileUpload` מרכיב ממנו את שם הקובץ), 23 ה-prefixes האחרים לא מתנגשים (גם לא `tr-` של ההדרכות), מסך הנאמן מציג רק תמונות שלו, האפליקציה לא מוחקת מ-Storage בכלל, והמיגרציה מסכימה עם הקוד. נבדק בשבירה מכוונת של התחילית |
| iso-report-test.js | דוח המוכנות לביקורת: מערכת ריקה אדומה בכל סעיף ומנמקת למה, מרשם שהפסיקו לנהל (400 יום) אינו ראיה, שורה בלי תאריך לעולם לא נחשבת עדכנית, סעיף 8.1 סופר גם בדיקות שפגו וגם כאלו שבלי תאריך כלל, 10.2 מסכים עם תור האימות, והגיליון עצמו — RTL, שורת חתימה, והסתייגות שהוא אינו תחליף לביקורת |
| notif-log-test.js | יומן ההתראות: הסנכרון מבקש חלון של 500 ולא את כל ההיסטוריה, אף טבלה עסקית לא נחתכה בטעות, ה-CSV מושך מהשרת את כל 2,000 השורות (גם אלו שהמכשיר כבר לא מחזיק), אופליין הוא אומר שזה העותק המקומי, וכפתור המחיקה שלא מחק — איננו |
| wiring-test.js | **הרשת ל«כפתור בלי כלום מאחוריו»** — הדפוס ששתי תכונות ה-AI נפלו בו. כל 237 ההנדלרים ב-markup מצביעים על פונקציה שקיימת, כל 58 המודאלים נפתחים ונסגרים, כל 28 כפתורי השמירה כותבים שורה (או מסרבים בקול עם סיבה מתועדת), וכל 39 העמודים מרונדרים בסדר גודל production. נבדק ששובר בכוונה כפתור מת ושומר שקט — ושניהם נתפסים בשם |
| capa-queue-test.js | תור אימות אפקטיביות (ISO 45001 §10.2): סופר בדיוק את ה-NCR הסגורים 30+ יום שטרם אומתו (לא צעירים, לא מאומתים, לא פתוחים, לא בלי תאריך סגירה), מופיע בשורת «היום», מסנן ייעודי שמציג גם סגורים וממוין מהוותיק, ✅ בשורה עצמה, ומשתמש שאינו אדמין לא רואה כלום |
| export-complete-test.js | ייצוא שלם: ה-CSV מדף התפוגות מכיל את כל 800 השורות ולא את ה-200 שצוירו (רגרסיה מ-#617), שורת «מוצגות 200 מתוך» לא נכנסת לקובץ, המסך חוזר לתקרה אחרי הייצוא, ו«הצג את כולן» שהמשתמש בחר לא נדרס. נבדק בהחזרת הרגרסיה |
| render-caps-test.js | תקרת רינדור: עמוד המשימות והתפוגות מציירים 200 שורות ולא את כל מה שמצאו (16,548 → 3,662 אלמנטים), המונים שמעל נשארים נכונים, שום שורה דחופה לא נופלת, כפתור «הצג את כולן» מצייר את השאר, וחזרה ללשונית כבר לא יורה 38 בקשות בכל פעם |
| ai-surface-test.mjs | ה-proxy מתרגם במקום לזרוק: `system` מגיע למודל (ההנחיה של legAskAI נגד המצאת נתונים), קובץ למודל-טקסט נדחה ב-400 במקום «[object Object]», `stream:true` מחזיר SSE שהלקוח יודע לקרוא, ומודל הראייה עדיין מקבל `{prompt,image}` |
| eqi-pdf-test.js | ייבוא תסקירי PDF וצילום חכם, מקצה לקצה: ה-proxy האמיתי מייצר את הבייטים ב-node, והקורא האמיתי ב-Chromium מפענח אותם. התסקיר נקרא משכבת הטקסט (20 עמודים, לא 3), סריקה בלי טקסט אומרת זאת במקום «JSON parse failed», תמונה הולכת למודל הראייה, וסרטון מדווח שנקרא פריים אחד |
| anon-boundary-test.mjs | גבול הסשן האנונימי: `wa-send`, נתיב הבדיקה של `trustee-notify` ו-`/api/claude` דוחים סשן אנונימי וסשן ללא אימייל, משתמש אמיתי עובר, וכל 24 קריאות ה-AI נושאות טוקן |
| trustee-tasks-test.js | קטלוג המשימות: נפילה ל-8 המובנות לפני המיגרציה, שינוי נוסח בלי לשנות מספר, משימה תשיעית, סירוב למחוק משימה שיש עליה דיווחים, נעילת משימת הסגירה, ותקרה/סף-זכאות שעוקבים אחרי הקטלוג |
| touch-targets-test.js | סבב QA בטלפון: כפתורי השורה (👁 ✎ 🗑 🖨 ⋯) 44×44 ב-10 עמודים במגע ונשארים צפופים בעכבר, וכל 36 העמודים לא גולשים לצדדים ולא זורקים ב-375px |
| vm-notifier-test.py | `tools/vm-notifier/notify.py` עם requests/מיילר מדומים |
| offline-photo-test.js | תור תמונות אופליין: תמונה שנכשלה נשמרת ב-IndexedDB, הדיווח נשמר עם סימון `pending:`, ובחיבור הבא התמונה עולה והסימון מוחלף בכל מקום |
| outbox-race-test.js | `_obDrain` האמיתי: פעולות שנכנסו בזמן העלאה שורדות |
| sbsync-test.js | `sbSync`/`_sbMergePull` האמיתי: שרת = מקור אמת, outbox שורד |
| emp-topbar-test.js, login-label-test.js | טופ-בר נקי במצב נאמן, תווית הכניסה |
| home/nav/roles/print/modules/agents/eqi*/density/inv/ncr-* | רגרסיות של סבב ה-handoffs (#535–#558) |
