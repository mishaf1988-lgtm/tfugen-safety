# תור העבודה — תיק שדרוגים

> נבנה 2026-09-20. **הקובץ הזה הוא הכתובת הקבועה** — מעדכנים אותו במקום לפתוח קובץ חדש עם תאריך.
> פריט שנעשה: לסמן ~~קו חוצה~~ ולציין את מספר ה-PR.

> **כל 75 אומתו בקוד** (2026-09-20, סבב אימות של 13 מאמתים).
> **64 מאושרים** (✅) · **11 חלקית** (🟨 — אמיתי, אבל הניסוח מגזים) · **0 שגויים**.
> עמודת המאמץ עודכנה להערכה של מי שקרא את הקוד, לא של מי שהציע.

## איך הרשימה נבנתה

1. **שישה קוראים** עברו על הקוד האמיתי, כל אחד מזווית אחת: הלולאה היומית · שרשרת תאונה→NCR→פעולה מתקנת→אימות · מרשמי הציות והתפוגות · תוכנית הנאמנים · סנכרון/גיבוי/אבטחה · דוחות ומגמות. יצאו **69 פערים עם הפניות לשורות קוד**.
2. **חמישה מציעים** הציעו שדרוגים מזוויות שונות (בוקר בטלפון · ביקור המבקר · העובד ברצפה · "האם אנחנו בטוחים יותר" · מנופים קטנים). יצאו **52 הצעות**.
3. שלבי המיזוג והשיפוט האוטומטיים **לא רצו** — נגמרו הקרדיטים. המיזוג, האימות והדירוג כאן נעשו ידנית.

## מקרא

| סימן | משמעות |
|---|---|
| ✅ | **אומת בקוד** — נפתח הקובץ, נקראה הפונקציה, אושר |
| 🟨 | **חלקית** — יש שם משהו אמיתי, אבל הניסוח מגזים או מתאר לא מדויק. ראה הערה |
| ⬜ | טרם אומת |
| 🔴🟠🟡 | חומרה |
| S / M / L | פחות מ-60 שורות / פחות מ-300 / פרויקט |
| 👤 | דורש פעולה ממיכאל (מיגרציה, מפתח, החלטה) |

---

## 1. NCR ופעולות מתקנות

| # | מה | חומרה | מאמץ | |
|---|---|---|---|---|
| 1.1 | ~~**אין שדה עדיפות בטופס NCR.** `svNcr` קורא `gv('ncr-p')` ואין `id="ncr-p"`. כל NCR נשמר כ«בינונית» — מהטופס, מייבוא האקסל, ומכמעט-נפגע חמור. התראת «NCR קריטי» **מעולם לא יכלה לירות**; 5 הקריטיות בסקירת ההנהלה תמיד ריקות~~ **#647** | 🔴 | S | ✅ |
| 1.2 | ~~**תוויות הצפייה וה-PDF שגויות.** `VIEW_CONFIG.ncr`: «סיבה»→`p` (עדיפות), «פעולה מתקנת»→`o` (אחראי). סיבת השורש (`rc`), הפעולה המתקנת (`c`), גילוי, סגירה ויעד — לא מופיעים כלל~~ **#647** | 🔴 | S | ✅ |
| 1.3 | ~~**סיבות שורש מייבוא האקסל בלתי נראות.** `xlParse` כותב ל-`root_cause`/`immediate`; הטופס, ה-AI וההחלה עובדים על `rc`/`c`~~ **#662** | 🟠 | S | ✅ |
| 1.4 | ~~**NCR שנפתח מחדש לא חוזר לתור האימות.** `_reopen` מאפס `s`/`cd` אבל לא `verified_by` — והתג «✅ אומת» נשאר על רשומה שנפתחה מחדש אחריו~~ **#647** | 🟠 | S | ✅ |
| 1.5 | ~~**ארבעה מקומות קוראים את התיאור כתאריך** (5262, 5263, 5314, 11882). «השבוע מול שבוע שעבר» מראה NCR 0/0; סיכום החודש סופר 0~~ **#647** | 🟠 | S | ✅ |
| 1.6 | ~~**אפשר לסגור NCR בלי סיבת שורש, בלי פעולה מתקנת ובלי אחראי.** `svNcr` מאמת רק את התיאור. שער סגירה = §10.2.1b–c~~ **#662** | 🟠 | S | ✅ |
| 1.7 | **אימות אפקטיביות הוא «עבר» בלבד.** אין תוצאה «לא אפקטיבי», ואין קריטריון הצלחה שנקבע **בסגירה**. §10.2.1e | 🟠 | M | ✅ |
| 1.8 | ~~**השרשור מכמעט-נפגע ל-NCR הוא טקסט בלבד.** רשומת ה-near_miss לא מתעדכנת — שתי משימות וירטואליות לאותו אירוע~~ **#662** | 🟡 | S | ✅ |
| 1.9 | ~~**מסך NCR לא מציג את המשימות שנוצרו ממנו** (`source_table='ncr'`)~~ **#662** | 🟡 | S | ✅ |
| 1.10 | ~~**`_ncrAI` לא שולח למודל** את סיבת השורש, ה-5-Why, הפעולה המתקנת או האחראי — ואז ההחלה דורסת אותם~~ **#662** | 🟡 | S | ✅ |
| 1.11 | **אין קטגוריית סיבה** (אדם/נוהל/ציוד/סביבה/ניהול) — `rc` הוא טקסט חופשי, אז אין זיהוי דפוסים | 🟡 | M | ✅ |
| 1.12 | ~~**הסריקה היומית ל«משימה בפיגור» סורקת רק `DB.tasks`** — NCR שעבר את היעד לא מייצר התראה~~ **#662** | 🟠 | S | ✅ |
| 1.13 | **אין סגירת מעגל עם המדווח.** מי שדיווח לא יודע שנפתח NCR ולא שנסגר. §5.4/§7.4 | 🟠 | M | ✅ |
| 1.14 | ~~**הישנות: באנר «N אי-התאמות דומות ב-12 חודשים»** בפתיחת NCR באותו מיקום/סוג~~ **#670** | 🟡 | M | ✅ |
| 1.15 | ~~**מונה «נפתחו מחדש»** — היום זו רק חותמת טקסט ב-`notes`, לא נספרת בשום דוח~~ **#670** | 🟡 | M | ✅ |

## 2. הלולאה היומית

| # | מה | חומרה | מאמץ | |
|---|---|---|---|---|
| 2.1 | ~~**«סבב בוקר» מ«היום» פותח מודאל ריק.** `_todayClick` קורא `openModal('m-round')` ישירות ומדלג על `_roundOpenNew()` — היחידה שממלאת נושאים ופריטים~~ **#649** | 🟠 | S | ✅ |
| 2.2 | ~~**«משימות להיום» מדלג על הפיגור.** המסנן הוא `r.due===today`~~ **#649** | 🟠 | S | ✅ |
| 2.3 | ~~**תג 📝 «המשימות שלי» תמיד 0.** שלוש דרכי ההשוואה משוות שם בעברית לכתובת אימייל~~ **#649** | 🟠 | S | ✅ |
| 2.4 | ~~**סריקת ההתראות היומית ב-`sessionStorage`.** באייפון כל פתיחה קרה = סשן חדש → אותן התראות שוב ושוב כל היום~~ **#649** | 🟠 | S | ✅ |
| 2.5 | ~~**«השבוע הקרוב» מדלג על בדיקות הציוד** — סורק `ppe/tr/docs/ctr` בלבד~~ **#649** | 🟠 | S | ✅ |
| 2.6 | ~~**סגירה בהקשה אחת מ«היום»** — היום: הקשה → מודאל מלא → select → שמור = 4 הקשות למשימה שכבר בוצעה~~ **#664** | 🟡 | S | ✅ |
| 2.7 | ~~**«היום» ממוין לפי עדיפות בלבד** — בתוך «פיגור» הסדר הוא סדר ההכנסה, לא הוותק~~ **#664** | 🟡 | S | ✅ |
| 2.8 | ~~**🔔 בטופ-בר פותח הגדרות, לא התראות.** אין «לא נקרא»; toast נעלם אחרי 2.5 שניות ואין לאן לחזור~~ **#670** | 🟠 | M | 🟨 |
| 2.9 | ~~**התראת WhatsApp נופלת למספר האחרון שנשלח אליו** (`tfgn_wa_last`) — יכול להיות כל אחד~~ **#664** | 🟠 | S | ✅ |
| 2.10 | ~~**«שלח אליי»** — שדה קבוע למספר של מיכאל בהגדרות ההתראות~~ **#664** | 🟡 | S | 🟨 |
| 2.11 | ~~**מתחת ל«היום» יש ~20 כרטיסי סטטיסטיקה.** לקפל תחת «📊 סטטיסטיקות ומגמות»~~ **#670** | 🟡 | M | 🟨 |
| 2.12 | ~~**תפריט ⋯ = 16 פריטים ברשימה שטוחה**, יומיומי לצד פעם-בשנה. ומספר הגרסה בו קבוע בקוד (`v2026.05.02`)~~ **#664** | 🟡 | S | 🟨 |

## 3. מרשמי ציות ותפוגה

| # | מה | חומרה | מאמץ | |
|---|---|---|---|---|
| 3.1 | ~~**עריכת הדרכה מוחקת את התעודה.** `svTr` כותב `file_url` תמיד; `_genEdit('tr')` לא משחזר אותו. עדכנת תוקף → התעודה נעלמה~~ **#648** | 🔴 | S | ✅ |
| 3.2 | ~~**גיבוי מרשם השמיעה כותב עמודות ריקות.** הקוד מייצא `r.n/r.d/r.res/r.e`; הרשומות מכילות `emp_name/test_date/...`. זה ה-CSV ש-`delAllHearing` מייצא **לפני מחיקת כל המרשם**~~ **#648** | 🔴 | S | ✅ |
| 3.3 | **אין פעולת «חידוש» באף מרשם.** הדרך היחידה היא לערוך ולדרוס — ההיסטוריה נמחקת | 🟠 | M | 🟨 |
| 3.4 | ~~**`med` (בדיקות רפואיות) לא בעמוד התפוגות ולא ב-VIEW_CONFIG** — אבל כן בלוח השנה, בסקירת ההנהלה ובהתראות~~ **#654** | 🟠 | S | ✅ |
| 3.5 | **ל-`hearing_tests` אין שדה תפוגה בכלל** — לא נסרק, לא מתריע, לא מופיע בתפוגות | 🟠 | M | ✅ |
| 3.6 | ~~**בדיקה חוזרת: `next_due` ידני** — שום דבר לא מקדם אותו לפי המחזור~~ **#654** | 🟠 | S | ✅ |
| 3.7 | **מאגר חוקי: «סטטוס עמידה» הוא ערך בודד** בלי תאריך, בלי מי קבע, בלי ראיה ובלי היסטוריה. §9.1.2 דורש רשומת הערכת ציות | 🟠 | M | 🟨 |
| 3.8 | ~~**לטופס מסמכים אין אזור צירוף, ואין ✎** — `svDoc` תמיד יוצר רשומה חדשה~~ **#654** | 🟠 | S | ✅ |
| 3.9 | ~~**לטפסי צמ"ג וקבלנים אין שדה צירוף** — אין איך לצרף אישור~~ **#654** | 🟡 | S | ✅ |
| 3.10 | ~~**`VIEW_CONFIG.tr` מציג את `c` כ«מדריך»** — בטופס זו קטגוריה. הדף וה-PDF מדפיסים «מדריך: בטיחות»~~ **#654** | 🟡 | S | ✅ |
| 3.11 | **שש פונקציות שמירה לא כותבות חותמת משתמש** — מי שמר ומתי קיים רק ב-`audit_log` | 🟡 | M | 🟨 |
| 3.12 | ~~**מצב ריק בעמוד התפוגות תמיד אומר «אין בדיקות ציוד»** — גם בלשונית «פג» ועל עמוד שמאגד 5 מרשמים~~ **#654** | 🟡 | S | ✅ |

## 4. נאמני בטיחות

| # | מה | חומרה | מאמץ | |
|---|---|---|---|---|
| 4.1 | ~~**«צלם אחרי» נעלם ב-1 לחודש.** `_truRender` מסנן לחודש הנוכחי — הנאמן לא יכול לסגור ליקוי שפתח בחודש שעבר~~ **#650** | 🟠 | S | ✅ |
| 4.2 | ~~**משימה מליקוי נוחתת על הנאמן שדיווח** כאחראי לתיקון~~ **#650** | 🟠 | S | ✅ |
| 4.3 | ~~**חזרה לקישור לא נוחתת על מסך הנאמן** — ענף ה-boot לא קורא ל-`_truEmpHome`~~ **#650** | 🟠 | S | ✅ |
| 4.4 | ~~**השלמת המשימה לא משנה את הממצא** — הוא נשאר «פתוח», וצריך לסמן «נסגר» גם בדף הנאמנים~~ **#652** | 🟠 | S | ✅ |
| 4.5 | **סגירה עם תמונת «אחרי» מיידית** — אין תור «סגירות לאישור» למנהל, וכל סגירה = +2 נקודות | 🟠 | M | 🟨 |
| 4.6 | ~~**שם עם שגיאת כתיב הופך לנאמן-רפאים** בלוח הניקוד לנצח. אין ✎ ואין מיזוג~~ **#652** | 🟠 | S | ✅ |
| 4.7 | ~~**הנאמן לא רואה מה פתוח באזור שלו** — רק את הדיווחים של עצמו~~ **#652** | 🟡 | S | ✅ |
| 4.8 | ~~**אין תזכורת להכריז זוכה** לחודש שהסתיים — ב-1 לחודש הדף קופץ לחודש הריק~~ **#652** | 🟡 | S | ✅ |
| 4.9 | ~~**«/8», «עד 80», «מ-5 משימות» קשיחים ב-UI, ב-CSV ובשרת** — בעוד שהמספרים האמיתיים עוקבים אחרי הקטלוג~~ **#652** | 🟡 | S | ✅ |
| 4.10 | ~~**הקיוסק לא מושך את `trustee_tasks`** — עדכון realtime על משימה אחת הופך את הקטלוג המקומי לאותה משימה בלבד~~ **#650** | 🟠 | S | ✅ |
| 4.11 | ~~**שורות הנאמנים ב«היום» מובילות לחודש הנוכחי** — והממצא הוותיק מחודש קודם לא שם~~ **#650** | 🟡 | S | ✅ |
| 4.12 | **כמעט-ונפגע מהנאמן**: הטופס מציג שדות מנהל, ואין תשובה «מה קרה עם מה שדיווחתי» | 🟡 | M | ✅ |

## 5. נתונים, סנכרון וגיבוי

| # | מה | חומרה | מאמץ | |
|---|---|---|---|---|
| 5.1 | ~~**`sbGet` בלי pagination.** Supabase מחזיר 1000 שורות לכל היותר, ו-`_sbMergePull` מתייחס אליהן כאל **כל** הטבלה — שורה מקומית שלא חזרה נמחקת. עם 375 NCR זה בסדר **היום**~~ **#657** | 🔴 | S | ✅ |
| 5.2 | ~~**PATCH/DELETE שנגע ב-0 שורות נחשב «הועלה בהצלחה»** — והפעולה יורדת מהתור~~ **#657** | 🟠 | S | ✅ |
| 5.3 | ~~**אין טיפול בפקיעת סשן.** משיכה שנכשלת ב-401 מסתיימת בשקט~~ **#657** | 🟠 | S | ✅ |
| 5.4 | ~~**הגיבוי לא כולל** `ncr_ai`, `ncr_comments`, `ncr_patterns`, `audit_log`, `equip_inspection_history`; ה-cron גם לא את טבלאות הנאמנים~~ **#646** | 🔴 | M | ✅ |
| 5.5 | ~~**כשה-cron מפסיק לרוץ אף אחד לא יודע** — `/health` מחזיר 'ok' תמיד~~ **#646** | 🟠 | S | ✅ |
| 5.6 | ~~**פתיחה קרה בלי רשת = מסך כניסה נעול.** supabase-js נטען מ-CDN וה-SW לא שומר אותו~~ **#657** | 🟠 | S | ✅ |
| 5.7 | **עריכה שולחת את כל השורה בלי גרסה** — שני אנשים באותה דקה, השני דורס את הראשון | 🟠 | M | ✅ |
| 5.8 | **`audit_log` ניתן לשינוי ולמחיקה** על ידי כל מנהל דרך REST — מיגרציה נכתבה (**#657**), טרם הורצה: [#658](../../issues/658) | 🟠 | S | ✅👤 |
| 5.9 | **אין שום בדיקה אוטומטית ל-RLS ול-Storage** — 78 מיגרציות עם «Verify» בהערות בלבד | 🟠 | M | 🟨 |
| 5.10 | **`2026-09-20_trustee_close_ownership.sql` — לא הורצה** | 🔴 | — | ✅👤 |
| 5.11 | **`2026-09-20_storage_trustee_scope.sql` — לא הורצה** (נכתבה היום) | 🔴 | — | ✅👤 |

## 6. דוחות ומגמות

| # | מה | חומרה | מאמץ | |
|---|---|---|---|---|
| 6.1 | ~~**`_exportXlsx` משתמש בשמות העמודות הגולמיים ככותרת** — `u`, `dy`, `sv`. מבקר שפותח את הגיליון לא יודע מה זה~~ **#660** | 🟠 | S | ✅ |
| 6.2 | ~~**זמן סגירת NCR מחושב בשתי דרכים שונות** (`cd-ts` מול `cd-sd`), ו-`ts` נדרס בכל שמירה~~ **#666** | 🟠 | M | ✅ |
| 6.3 | ~~**אין חלוקה לתקופות.** כל מדדי הזמן על כל ההיסטוריה — אי אפשר לומר «ברבעון הזה סגרנו מהר יותר»~~ **#666** | 🟠 | M | ✅ |
| 6.4 | ~~**סקירת ההנהלה בלי רבעון קודם ובלי Δ** — בעוד שהפרומפט מבקש מהמודל להשוות לרבעון קודם~~ **#666** | 🟠 | M | ✅ |
| 6.5 | ~~**גרף המגמה קיים אבל קבור** בתוך פאנל ה-NCR Agent — לא בבית, לא בסקירה, לא בהדפסה~~ **#660** | 🟡 | S | 🟨 |
| 6.6 | ~~**מגמת 12 חודשים: נפתח / נסגר / צבר** — SVG ידני, בלי ספרייה~~ **#660** | 🟠 | S | 🟨 |
| 6.7 | ~~**Sparklines לאינדיקטורים מובילים** — כמעט-נפגע, דיווחי נאמנים, סבבים, שיחות בטיחות~~ **#668** | 🟠 | M | ✅ |
| 6.8 | ~~**פס חום 6 חודשים לכל אזור** — CSS בלבד~~ **#668** | 🟡 | M | ✅ |
| 6.9 | ~~**כשירות הדרכות לפי מחלקה** — קישור `tr.w` ל-`emp.n`~~ **#668** | 🟡 | M | ✅ |
| 6.10 | ~~**אחוז משימות שנסגרו בזמן, לפי חודש** — דורש `closed_date` ב-`svTsk`~~ **#666** | 🟡 | M | ✅ |
| 6.11 | **פרוטוקול סקירת הנהלה שנשמר כרשומה.** §9.3 | 🟠 | M | ✅ |
| 6.12 | ~~**אריח «ימי אבדן» מתויג LTIF** ומציג ימים גולמיים — זה לא LTIF~~ **#660** | 🟡 | S | ✅ |
| 6.13 | **דף אחד להנהלה: «האם אנחנו נהיים בטוחים יותר?»** באותו דפוס של דוח ה-ISO | 🟠 | L | ✅ |

---

## 7. מה כבר תוקן היום

| PR | מה |
|---|---|
| #615 | סשן אנונימי עבר את כל שערי ה-API |
| #616 | ייבוא PDF והצילום החכם — שתיהן היו מתות חודשים |
| #617 | תקרת רינדור + חניקת סנכרון |
| #618 | תור אימות אפקטיביות |
| #620 | רשת ל«כפתור בלי כלום מאחוריו» |
| #621 | יומן ההתראות + כפתור מחיקה שלא מחק |
| #622 | דוח מוכנות לביקורת ISO |
| #624 | **Storage: הסשן האנונימי יכול היה לקרוא, לדרוס ולמחוק כל קובץ** + הקישור השבור במייל |
| #625 | **רגרסיה שלי מ-#617: ה-CSV מעמוד התפוגות הכיל 200 מתוך 800** |
| #647 | NCR: אף אי-התאמה מעולם לא סומנה קריטית, וה-PDF נקב בשדות הלא נכונים (1.1, 1.2, 1.4, 1.5) |
| #648 | מרשמים: עריכת הדרכה מחקה את התעודה, וגיבוי השמיעה יצא ריק (3.1, 3.2) |
| #649 | הלולאה היומית ×5 — כל אחת מהן ענתה על השאלה הלא נכונה (2.1–2.5) |
| #650 | נאמנים: חמישה מבויי סתום במסך שמתנדב מחזיק ביד (4.1, 4.2, 4.3, 4.10, 4.11) |
| #652 | נאמנים: עבודה שנעשתה ולא נרשמה, נאמן שקיים פעמיים, ופרס שאיש לא הכריז (4.4, 4.6–4.9) |
| #654 | מרשמים: הראיה שהמערכת מבקשת ממבקר להאמין לה בלי להראות (3.4, 3.6, 3.8–3.10, 3.12) · מיגרציה פתוחה: [#655](../../issues/655) |
| #657 | סנכרון: ארבע דרכים שבהן האפליקציה אמרה שהכל בסדר כשלא (5.1, 5.2, 5.3, 5.6) · מיגרציה פתוחה ל-5.8: [#658](../../issues/658) |
| #660 | דוחות: מה שהאפליקציה מוסרת למישהו מחוץ לה (6.1, 6.5, 6.6, 6.12) |
| #662 | NCR: הפער בין «הרשומה אומרת סגור» לבין «תראה לי מה נעשה» (1.3, 1.6, 1.8–1.10, 1.12) |
| #664 | הלולאה היומית: מסך הבוקר, ולאן באמת הולכות ההודעות שהוא שולח (2.6, 2.7, 2.9, 2.10, 2.12) |
| #666 | מדדים: המספרים שסקירת ההנהלה בנויה עליהם (6.2, 6.3, 6.4, 6.10) |
| #668 | מגמות: sparklines לאינדיקטורים · פס חום 6 חודשים לפי אזור · כשירות הדרכות לפי מחלקה (6.7, 6.8, 6.9) |
| #670 | מסך: באנר הישנות ב-NCR · מונה פתיחות מחדש · מרכז התראות עם לא-נקרא · קיפול הסטטיסטיקות (1.14, 1.15, 2.8, 2.11) |

## 8. לא מומלץ

| מה | למה |
|---|---|
| **דחיפת וואטסאפ לנאמנים** | מיכאל דחה 2026-09-19 — דורש מספר עסקי ועלות חודשית. התחליף בשימוש: השורה במסך הבית (#589) |
| **שינוי הארכיטקטורה** (framework / build step / קובץ HTML שני) | חוק קבוע ב-CLAUDE.md |
| **מחיקת שורות מ-`ncr` / `ncr_ai` / `trustee_reports`** | אסור גם עם אישור כללי |

---

## סדר מומלץ

**שבוע 1 — הכל S, הכל מאומת:** 1.1 · 1.2 · 3.1 · 3.2 · 1.4 · 1.5 — כולם באותו אזור (NCR ונתונים שנעלמים), פי-אר אחד.

**שבוע 2:** 2.1 · 2.2 · 2.3 · 2.4 · 2.5 · 4.1 · 4.2 — הלולאה היומית והנאמנים.

**שבוע 3 — הגנה על הנתונים:** 5.4 · 5.2 · 5.3 · 5.5 · 1.12.

**החודש:** 1.7 · 3.7 · 6.11 · 3.3 · 6.6 · 6.7 · 2.8 · 1.13.

**פרויקט:** 6.13.

**וממתין לך:** 5.10 · 5.11 (שתי מיגרציות), Vitre, connector של Supabase, Leaked-Password Protection.

---

## 🔗 GitHub Issues — 16 המאומתים

כל פריט מאומת הוא Issue שאפשר לסמן מהטלפון. התווית `week-1` היא ששת הראשונים.

| Issue | פריט | מה |
|---|---|---|
| [#628](../../issues/628) | 1.1 | אין שדה עדיפות ב-NCR |
| [#629](../../issues/629) | 1.2 | תוויות הצפייה וה-PDF |
| [#630](../../issues/630) | 3.2 | גיבוי השמיעה ריק |
| [#631](../../issues/631) | 3.1 | עריכת הדרכה מוחקת תעודה |
| [#632](../../issues/632) | 1.4 | פתיחה מחדש ותור האימות |
| [#633](../../issues/633) | 1.5 | תיאור נקרא כתאריך |
| [#634](../../issues/634) | 2.1 | סבב בוקר מ«היום» |
| [#635](../../issues/635) | 2.2 | «משימות להיום» בלי פיגור |
| [#636](../../issues/636) | 2.3 | תג 📝 תמיד 0 |
| [#637](../../issues/637) | 2.4 | סריקה יומית חוזרת |
| [#638](../../issues/638) | 2.5 | «השבוע» בלי ציוד |
| [#639](../../issues/639) | 4.1 | «צלם אחרי» ב-1 לחודש |
| [#640](../../issues/640) | 4.2 | משימה על הנאמן שדיווח |
| [#641](../../issues/641) | 1.3 | סיבות שורש מהאקסל |
| [#642](../../issues/642) | 5.11 | 👤 מיגרציית Storage |
| [#643](../../issues/643) | 5.10 | 👤 מיגרציית סגירת ליקויים |

**תוויות**: `P1`/`P2` חומרה · `week-1`/`week-2` סדר · `verified` אומת בקוד · `needs-michael` דורש אותך · `ncr`/`daily`/`registers`/`trustees`/`data`/`reports` תחום

59 הפריטים שלא אומתו **לא** נפתחו כ-Issues — לא נכון לפתוח משימה על משהו שעוד לא אישרתי שהוא נכון. הם למעלה בקובץ הזה.

---

## נספח — פסקי הדין המלאים (2026-09-20)

13 מאמתים, כל אחד קרא אזור אחד בקוד. לכל פריט: הראיה שהכריעה, ההשפעה, ומיקום התיקון.


### 1.6 — ✅ בוצע ב-**#662** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: index.html:4312 `function svNcr(){` — האימות היחיד בכל הפונקציה הוא שורה 4315: `if(!d){toast('תיאור הוא שדה חובה');return;}` (‏d = gv('ncr-d'), התיאור). אין שום בדיקה נוספת עד ה-return. הסטטוס נלקח כמו שהוא: 4316 `var s=gv('ncr-s')||'פתוח';` ונכתב ב-4330 `s:s,`; שדות הסגירה נכתבים ריקים בלי מחאה: 4326 `rc:gv('ncr-rc')||'',` · 4327 `c:gv('ncr-c')||'',` · 4328 `o:gv('ncr-o')||'',`. השורה היחידה שמגיבה ל'סגור' היא 4332 `cd:s==='סגור'?(gv('ncr-cd')||new Date()...):null,` — כלומר הקוד כן יודע שזו סגירה, ובכל זאת לא שואל דבר. בדקתי שאין מסלול סגירה אחר: היחיד שכותב `s:'סגור'` מלבד svNcr הוא ייבוא האקסל xlParse (index.html:15164). גם ncrStatusChange (9879 ו-9900 — מוגדרת פעמיים, אותו גוף) רק מציגה/מסתירה את שדה התאריך: `if(s&&w)w.style.display=(s.value==='סגור')?'block':'none';`

**השפעה**: מיכאל יכול לבחור «סגור» ולשמור NCR עם סיבת שורש ריקה, פעולה מתקנת ריקה ובלי אחראי — והמערכת תגיד «נשמר ✓». הרשומה הזאת נספרת כ«סגור» בדשבורד, בדוח מוכנות ISO ובממוצע ימי הסגירה. בביקורת 45001 §10.2.1 זה בדיוק הממצא: «הראה לי את הראיה שהפעולה המתקנת נקבעה» — ואין. בנוסף, רשומה סגורה בלי `rc`/`c` נכנסת גם לתור אימות האפקטיביות (‏_capaDue, 5978) אחרי 30 יום, כלומר מבקשים לאמת פעולה שמעולם לא נרשמה.

**מיקום התיקון**: svNcr — index.html:4312-4350. שער חדש אחרי שורה 4316 (אחרי חישוב s, לפני בניית rec): אם s==='סגור' ואחד מ-gv('ncr-rc')/gv('ncr-c')/gv('ncr-o') ריק → toast מפורט ו-return. ~15-25 שורות.

**הערה**: סיכון אמיתי בתיקון: 375 ה-NCR בייצור יובאו מאקסל עם s='סגור' (xlParse, 15164) והרבה מהם בלי rc/c/o. שער שחוסם **כל** שמירה עם סטטוס 'סגור' יינעל על רשומות ישנות — מיכאל יפתח NCR ישן כדי לתקן פסיק, וייחסם. הפתרון: להפעיל את השער רק כשהסטטוס **משתנה** ל'סגור' (להשוות מול DB.ncr.find(id).s), או רק על רשומות חדשות. עוד שני דברים שראיתי בדרך ונוגעים לאותו קוד: (1) `id="ncr-p"` **לא קיים** במודאל (הגריד ב-1998-2038 אין בו שדה עדיפות), ו-gv מחזיר '' לאלמנט חסר (2940: `return el?el.value.trim():'';`) — לכן שורה 4336 `p:gv('ncr-p')||'בינונית'` **מאפסת את העדיפות ל«בינונית» בכל עריכה** של NCR קיים, ו-editNcr (4280) גם הוא לא מחזיר את הערך; (2) כתוצאה מכך התנאי בשורה 4342 `rec.p==='קריטי'` לעולם לא מתקיים — התראת `ncr_critical` היא קוד מת. שווה לצרף לאותו PR.


### 1.7 — ✅ confirmed · מאמץ M

**ראיה**: ‏_capaVerify — index.html:13265-13281. כל מה שהיא כותבת: 13274-13276 `rec.verified_by=u; rec.verified_at=new Date().toISOString(); rec.verification_notes=notes;`. אין שדה תוצאה. השורה שמחליטה שאין «לא אפקטיבי» היא 13268: `if(rec.verified_by){toast('כבר אומת');return;}` — עצם קיום `verified_by` = «עבר», ואי אפשר לרשום תוצאה שנייה. המיגרציה migrations/2026-05-01_ncr_capa_verify.sql מוסיפה בדיוק שלוש עמודות: `verified_by text, verified_at timestamptz, verification_notes text` — אין `verification_result`. גם התור והתצוגה בינאריים: _capaDue (5978-5985) `if(r.verified_by)return false;` ו-showView (11408) `if(rec.verified_by){ ... '✅ אומת על ידי ...' }`. לגבי קריטריון הצלחה: עברתי על כל שדות מודאל ה-NCR (1998-2038) — d, a, sd, location_id, loc, project_id, issue_type_id, rc, c, o, u, s, cd, notes, five_why, sens. אין שדה קריטריון/מדד הצלחה, ואין אותו גם ב-VIEW_CONFIG.ncr (11198).

**השפעה**: כשהפעולה המתקנת **לא** עבדה, למיכאל אין איפה לרשום את זה. האפשרויות שנשארות: לא ללחוץ על הכפתור בכלל (ואז ה-NCR נשאר לנצח בתור «ממתינות לאימות» ונראה כמו הזנחה), או ללחוץ «אמת» ולכתוב בטקסט החופשי «לא עבד» — ואז הוא מסומן ירוק «✓ אומת» בכל מסך, כולל דוח מוכנות הביקורת. חמור יותר: בלי קריטריון הצלחה שנקבע בזמן הסגירה, האימות אחרי 30 יום הוא שאלת זיכרון («נראה לי שזה בסדר»), לא מדידה — וזה בדיוק מה ש-§10.2.1e דורש שיהיה מתועד.

**מיקום התיקון**: מיגרציה חדשה ל-ncr: `verification_result text` (+ אולי `success_criteria text`). index.html: _capaVerify (13265-13281) — מ-prompt חופשי לבחירה אפקטיבי/לא אפקטיבי + תיאור; _capaDue (5978) — התנאי `if(r.verified_by)return false;` צריך להיות לפי result ולא לפי קיום; showView (11408-11414) — badge ירוק/אדום; rNcr (9838-9840) — הכפתור בשורת הטבלה; ISO-readiness (6278-6286). שדה קריטריון: מודאל m-ncr (~2019) + svNcr (4318-4338) + VIEW_CONFIG.ncr (11198).

**הערה**: לא לבטל בטעות את #618 — הוא בדיוק מה שהפך את האימות לנגיש (ההערה ב-5960-5968 מסבירה שהכפתור היה קיים מאז מאי אבל קבור במודאל). התיקון הזה בונה עליו, לא מחליף אותו. תלות ב-1.6: קריטריון הצלחה «שנקבע בסגירה» חסר משמעות אם אפשר לסגור בלי כלום — לכן 1.6 צריך לרדת ראשון. כש«לא אפקטיבי» ייכנס, כדאי לחבר אותו ל-`_reopen` הקיים (13305) במקום להמציא מסלול פתיחה-מחדש חדש. שים לב לכלל 7 ב-CLAUDE.md: המיגרציה לא מסומנת [x] עד שמיכאל מאשר הרצה.


### 1.8 — ✅ בוצע ב-**#662** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: ‏_chainNmToNcr — index.html:13283-13304. הפונקציה קוראת את רשומת ה-near_miss בשורה 13284 (`var nm=(DB.near_miss||[]).find(...)`) ומשם רק **קוראת** ממנה: 13294-13299 ממלאות שדות טופס. השורה שמחליטה שזה טקסט בלבד היא 13299: `setIf('ncr-notes','נוצר מדיווח כמעט-נפגע #'+nmId);` — המזהה נכתב כמחרוזת לתוך שדה ההערות של ה-NCR. אין `nm.s=`, אין `sbUpd('near_miss',...)`, אין `sdb()` — הפונקציה מסתיימת ב-toast (13301). אין גם עמודת קישור: `grep ncr_id migrations/` מחזיר רק ncr_comments ו-ncr_ai, ו-VIEW_CONFIG.near_miss (11240-11242) ו-_EDIT_MODS.near_miss (4024) לא מכילים שדה קישור. על שתי המשימות הווירטואליות — _tskVirtual, index.html:8962-8971: `(DB.ncr||[]).forEach(... if(!_isOpenish(n.s))return; virt.push({id:'v:ncr:'+n.id,...}))` ומיד אחריו `(DB.near_miss||[]).forEach(... if(!_isOpenish(m.s))return; virt.push({id:'v:near_miss:'+m.id,...}))`. ‏_isOpenish (8950) = `s===_TSK_OPEN||s==='בטיפול'`, וברירת המחדל ב-select ‏nm-s (2377) היא «פתוח» — כך שהכמעט-נפגע נשאר פתוח וגם ה-NCR החדש פתוח. הסינון בסוף (8999-9000) מסיר כפילות רק לפי `source_table+':'+source_id` — שני מפתחות שונים, ולכן שתי השורות נשארות.

**השפעה**: מיכאל מדווח על כמעט-נפגע חמור, לוחץ «פתח NCR מהדיווח הזה», שומר — ומעכשיו יש לו בדף המשימות **שתי** שורות לאותו אירוע: «כמעט ונפגע: …» ו-«NCR-0xxx — …». הוא יטפל ב-NCR, והשורה של הכמעט-נפגע תישאר פתוחה לנצח ותטפטף לכל ספירה («כמעט-נפגע פתוחים» בעמוד המיקומים 10187, ב-KPI החודשי 5926, בסקירת ההנהלה 8575). ומהכיוון השני — מי שפותח את רשומת הכמעט-נפגע לא רואה שנפתח ממנה NCR; המידע היחיד קיים בכיוון ההפוך, כטקסט, בתוך שדה הערות.

**מיקום התיקון**: ‏_chainNmToNcr — index.html:13283-13304 (להעביר את nmId לשדה מוסתר חדש במודאל m-ncr) + svNcr — index.html:4339-4345 (אחרי השמירה: לסמן את nm כ«בטיפול» ולצרף את מספר ה-NCR). לחלופין להרחיב את VIEW_CONFIG.near_miss (11240) כדי להציג את הקישור.

**הערה**: התיקון הזול בלי מיגרציה: לשנות `nm.s` ל«בטיפול» + לצרף את `rec.num` ל-`nm.notes`, דרך `sbUpd('near_miss',nm)` + `sdb()` — זה לבדו מוריד את המשימה הווירטואלית הכפולה (כי _isOpenish כולל «בטיפול» — צריך אז 'סגור' או להוציא במפורש, שים לב). קישור אמיתי דו-כיווני דורש עמודה חדשה `ncr_id` ב-near_miss = מיגרציה + כלל 7. חובה לזכור: הפונקציה רק **ממלאת טופס** — היא לא שומרת NCR, ולכן אסור לעדכן את ה-near_miss ברגע הלחיצה (המשתמש עשוי לבטל); העדכון חייב לקרות ב-svNcr אחרי שמירה מוצלחת. בונוס באותה פונקציה: שורה 13300 `var prio=g('ncr-p');` — האלמנט לא קיים (ראה 1.6), ולכן כמעט-נפגע «חמור» לעולם לא הופך ל-NCR «קריטי».


### 1.9 — ✅ בוצע ב-**#662** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: ‏showView — index.html:11268-11460. עברתי על כל הפונקציה. הבלוקים הייעודיים ל-NCR הם: אימות אפקטיביות (11407-11414), «פתח מחדש» (11398-11402), WhatsApp (11426-11430), «+ פתח משימת מעקב (CAPA)» (11434) והערות (11437). השורה שמחליטה היא 11434 — כפתור **יצירה** בלבד: `html+='<div ...><button data-stbl="'+esc(tbl)+'" data-sid="'+esc(id)+'" onclick="openTskModal(this.dataset.stbl,this.dataset.sid)" ...>➕ פתח משימת מעקב (CAPA)</button></div>';` — ואין שום קריאה שמסננת את DB.tasks לפי הרשומה הנוכחית. הפונקציה נגמרת ב-11457-11459: `if(body){body.innerHTML=html;...} goPage('view'); if(tbl==='ncr')_ncrCommentsLoad(id);` — נטענות הערות, לא משימות. `grep "source_table==="` בכל הקובץ מחזיר שלוש תוצאות בלבד: 4970, 6488, ו-14616 `function _truTaskFor(id){return (DB.tasks||[]).find(function(x){return x.source_table==='trustee_reports'&&x.source_id===id;});}` — הדפוס ההפוך קיים, אבל **רק** לנאמני בטיחות. הקישור עצמו נשמר נכון ב-svTsk (8923: `source_table:gv('tsk-src-tbl')||null,source_id:gv('tsk-src-id')||null`) — המידע קיים, פשוט לא מוצג.

**השפעה**: מיכאל פותח NCR, לוחץ «+ פתח משימת מעקב», חוזר למחרת לאותו NCR — ורואה את אותו כפתור «+ פתח משימת מעקב» כאילו לא עשה כלום. אין סימן שכבר יש משימה, מי אחראי עליה, מתי היעד, או אם היא הושלמה. התוצאה המעשית: משימות כפולות לאותו NCR, ובביקורת אי אפשר להראות מ-NCR אחד את שרשרת הטיפול — צריך לעבור לדף המשימות ולחפש ידנית בין מאות שורות.

**מיקום התיקון**: ‏showView — index.html, להוסיף בלוק אחרי 11434 (לפני בלוק ההערות ב-11437): `(DB.tasks||[]).filter(function(t){return t.source_table===tbl&&t.source_id===id;})` ולרנדר שורות עם fd(t.due) + sb(t.status) + `showView('tasks',t.id)`. ~30 שורות.

**הערה**: לעשות את זה גנרי (לפי `tbl`, לא רק ncr) — אותו בלוק פותר מיד גם את inc / near_miss / rsk / equip_inspections שסובלים מאותו דבר. לעזרים קיימים: fd, du, sb, showView, goPage — לא לשכפל. **ממצא נוסף וחמור יותר שנתקלתי בו באותה פונקציה**: `VIEW_CONFIG.ncr` (index.html:11198-11201) ממפה שגוי — `['סיבה','p']` ו-`['פעולה מתקנת','o']`. השדה `p` הוא **עדיפות** ו-`o` הוא **אחראי**; סיבת השורש האמיתית היא `rc` והפעולה המתקנת היא `c` (ראה svNcr 4326-4328). כלומר מסך הצפייה ב-NCR מציג «סיבה: בינונית» ו-«פעולה מתקנת: ישראל כהן», ואת סיבת השורש והפעולה המתקנת האמיתיות **לא מציג כלל** — וגם לא את היעד u, תאריך הסגירה cd וההערות. זה תיקון של שתי שורות ושווה יותר מ-1.9 עצמו; להכניס לאותו PR.


### 1.10 — ✅ בוצע ב-**#662** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: ‏_ncrAI — index.html:15613-15682. ה-prompt נבנה ב-15619-15638 ומונה במפורש: `'NCR: '+(n.num||')`, `'Description: '+(n.d||'')`, `'Category: '`, `'Area: '+(n.a||'')`, `'Priority: '+(n.p||'')`, `'Location: '`, `'Source date: '`, `'Notes: '+(n.notes||'')`. אין `n.rc`, אין `n.five_why`, אין `n.c`, אין `n.o` — ואין בהם מחסור בנתונים: _ncrLoad (15440) מושך `'/rest/v1/ncr?select=*&limit=500'` ל-_nad, כלומר כל ארבעת השדות יושבים באובייקט ופשוט לא נכנסים למחרוזת. על הדריסה — _ncrApply (15578-15595), שורת ההחלטה 15582: `var patch={rc:an.root_cause||null,c:ca0||null,o:an.owner_suggested||null,u:an.due_suggested||null};` ומיד PATCH ל-`/rest/v1/ncr?id=eq.` בלי שום בדיקה אם השדה כבר מלא, ואחריו 15588 `n.rc=patch.rc||'';n.c=patch.c||'';n.o=patch.o||'';n.u=patch.u||'';`. זו התנהגות הפוכה מאחיותיה באותו קובץ: _ncrAIRcAction (11811-11813) כן מגן — `if(rcEl && parsed.rc && !rcEl.value.trim()){rcEl.value=parsed.rc;...}` — ובנוסף **כן** שולח את ה-5-Why ואת האחראי (11789, 11793-11794). כלומר הטענה נכונה ל-`_ncrAI` (סוכן ה-NCR) ולא ל-`_ncrAIRcAction` (הכפתור במודאל).

**השפעה**: מיכאל משקיע רבע שעה ב-5-Why של NCR, כותב סיבת שורש ופעולה מתקנת, ואז נכנס לסוכן NCR ולוחץ «Analyze with Claude». המודל לא רואה דבר מזה — הוא עובד על התיאור בלבד ויכול להחזיר סיבת שורש שסותרת את מה שנקבע בשטח. לחיצה על «✓ החל ניתוח על ה-NCR» מחליפה את הכל: סיבת השורש, הפעולה, האחראי והיעד. יש confirm שמזהיר — 15581: `if(!confirm('להחל את הניתוח על ה-NCR?\n(סיבת שורש + פעולה + אחראי + יעד יוחלפו)'))return;` — אבל בטלפון זה חלון קטן שנלחץ אוטומטית, ואין undo: הערך הקודם לא נשמר בשום מקום (ncr_ai שומר רק את פלט ה-AI).

**מיקום התיקון**: ‏_ncrAI — index.html:15619-15638: להוסיף ל-prompt `'Root cause (already recorded): '+(n.rc||'')`, `'5-Why: '+(n.five_why||'')`, `'Corrective action already recorded: '+(n.c||'')`, `'Owner: '+(n.o||'')` + משפט הנחיה «אל תסתור את מה שכבר נקבע — הצע שיפור או אמור שהקיים מספק». ‏_ncrApply — index.html:15578-15595: למלא רק שדות ריקים (כמו 11811), או להציג diff «קיים → מוצע» לפני ההחלה. ~40 שורות בשתיהן.

**הערה**: ‏DECISIONS.md, «2026-04-18 — NCR Agent Accept & Apply (PR 5b)» (שורות 1349-1366), מתעד את מיפוי 4 השדות כהחלטה מכוונת — לכן זו לא טעות קוד אלא החלטה שצריך לשנות במודע, ושורה ב-DECISIONS.md בסוף. שים לב שההחלטה ההיא גם כתבה במפורש «`corrective_actions[0]` ולא `immediate_action`» — לא לשנות את זה אגב אורחא. ההגנה מ-_ncrAIRcAction היא התקדים לחיקוי, לא המצאה חדשה. נקודה נוספת שראיתי ב-_ncrApply: אחרי ה-PATCH היא מעדכנת את DB.ncr בזיכרון (15589) אבל **לא קוראת ל-`sdb()`** — המטמון ב-localStorage נשאר עם הערכים הישנים עד הסנכרון הבא, כך שרענון אופליין מחזיר את מה שנדרס. שורה אחת.


### 1.11 — ✅ confirmed · מאמץ M

**ראיה**: index.html:2013 — השדה בטופס הוא input חופשי, לא select: `<div class="field"><label>סיבת שורש</label><input id="ncr-rc" type="text" class="inp" placeholder="מה גרם לאי התאמה?"></div>`. svNcr (index.html:4312, בלוק ה-rec 4318-4338) שומר את השורה המכריעה `rc:gv('ncr-rc')||'',` (4326) — וברשימת 19 השדות שהוא שולח אין שום שדה קטגוריה. בכל המיגרציות שנוגעות בטבלה (`ALTER TABLE ncr` ב-2026-04-22_ncr_columns.sql, 2026-04-24_ncr_rc_ts_columns.sql, 2026-04-30_external_ids.sql, 2026-05-02_issue_types.sql) אין עמודה של קטגוריית סיבה. grep ל-`\.rc\b` מחזיר 6 מופעים בלבד בכל הקובץ: 4290 (טעינה לטופס ב-editNcr), 4326 (שמירה), 11813 (מילוי מ-Smart Capture), 15462/15550/15586 (NCR Agent) — אף אחד מהם אינו קיבוץ, ספירה או סינון. הפילטרים ב-rNcr (9720-9727) הם `location_id` / `project_id` / `issue_type_id` בלבד.

**השפעה**: מיכאל לא יכול לענות לרואה חשבון בביקורת ISO על השאלה הקלאסית «מה הסיבה הכי שכיחה לאי-התאמות אצלכם?». עם 375 רשומות, סיבת השורש היא 375 מחרוזות חופשיות — אי אפשר לספור «12 מתוכן היו כשל בנוהל» בלי לקרוא הכל ידנית. הוא גם לא יכול להראות מגמה («ירדנו מ-40% אדם ל-25%») שהיא בדיוק מה ש-§10.2 מצפה לו.

**מיקום התיקון**: index.html:2013 (select חדש `ncr-rc-cat` ליד השדה הקיים) · svNcr index.html:4326 (הוספת `rc_cat` ל-rec) · editNcr index.html:4290 (טעינה חזרה) · _NCR_DRAFT_FIELDS index.html:4231 (אחרת הטיוטה תאבד אותו) · VIEW_CONFIG.ncr index.html:11190-11193 · rNcr index.html:9720-9727 (פילטר) + ויג'ט קיבוץ חדש. בנוסף: מיגרציה `ALTER TABLE ncr ADD COLUMN IF NOT EXISTS rc_cat text`.

**הערה**: שתי הסתייגויות שמשנות את התמונה: (א) כבר קיים עץ תיוג היררכי — `issue_types` (3 רמות, seed ב-migrations/2026-05-02_issue_types_seed.sql) ו-`ncr.issue_type_id`, עם פילטר בדף NCR. אבל הוא מסווג את **התסמין** («החלקות → שמן/מים על הרצפה»), לא את **הסיבה** (אדם/נוהל/ציוד/סביבה/ניהול), אז הוא לא תחליף — וגם אסור לשכפל אותו, צריך להוסיף מימד שני. (ב) «אין זיהוי דפוסים» מוגזם מעט: זיהוי דפוסים לפי מיקום כבר קיים (תובנת דשבורד ב-5182-5187 ו-כרטיס «לפי אזור» ב-5461-5493) ו-`ncr_patterns` שומר ניתוחי AI מצטברים. מה שלא קיים הוא דפוסים **לפי סיבה**. תלות: המיגרציה דורשת הרצה של מיכאל (כלל 7 ב-CLAUDE.md) — עד שתרוץ, ה-PATCH ייכשל בשקט בדיוק כמו שקרה ב-ncr_ai_feedback.


### 1.12 — ✅ בוצע ב-**#662** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: `_notifDailyScan` (index.html:11047). השורה המכריעה, index.html:11055: `var overdueTasks=(DB.tasks||[]).filter(function(t){` — וחמש שורות אחריה `return daysLate>=3;}).slice(0,3);`. `_collectVirtualTasks()` לא נקרא בפונקציה הזו בכלל. לשם השוואה, כל מסך אחר כן מאחד: `_renderToday` index.html:4963 (`allTasks = allTasks.concat(_collectVirtualTasks())`), ה-KPI ב-5938, וה-8558 ו-9517. וב-`_collectVirtualTasks` (index.html:8948) ה-NCR באמת נכנס עם היעד שלו — index.html:8963: `virt.push({id:'v:ncr:'+n.id, ..., due:n.u||null, ..., source_table:'ncr', ..., isVirtual:true});`. כלומר: הנתון קיים, הסריקה היומית פשוט לא מסתכלת עליו.

**השפעה**: NCR שהיעד שלו עבר לא שולח שום דבר החוצה — לא WhatsApp, לא רשומה ב-notifications_log. הטלפון של מיכאל שקט, ואם הוא לא פותח את הדשבורד בבוקר הוא לא יודע. **אבל** — וזה מרכך את החומרה — הדשבורd כן מראה אותו: _renderToday מוסיף אותו עם priority 1 ותג אדום «פיגור» ומספר הימים (index.html:4974). אז זה חור בערוץ ההתראה היוצא, לא עיוורון מוחלט. יש גם מעקף קיים: אם מיכאל יצר משימה אמיתית מה-NCR (כפתור «צור משימה» במסך הצפייה), היא שורה ב-DB.tasks והסריקה כן תופסת אותה.

**מיקום התיקון**: index.html:11054-11064 (`_notifDailyScan`, הוספת `_collectVirtualTasks()` ל-concat) **וגם** index.html:11129 (`_notifBuildWaArgs`, ענף `task_overdue`).

**הערה**: ⚠️ תלות שחייבים לתקן באותו PR, אחרת התיקון לא עושה כלום: `_notifBuildWaArgs` בענף task_overdue עושה (index.html:11129) `var t=(DB.tasks||[]).find(function(r){return r.id===payload.id;});if(!t)return null;`. מזהה של משימה וירטואלית הוא `'v:ncr:'+n.id` — הוא לעולם לא יימצא ב-DB.tasks, הפונקציה תחזיר null, ו-_notifyEvent (index.html:11167) ירשום `whatsapp_skipped` וישלח כלום. צריך או לחפש גם ב-_collectVirtualTasks() או לפצל לאירוע `ncr_overdue` נפרד עם טמפלט משלו (טמפלט WhatsApp חדש = אישור Meta, עלול לקחת ימים — עדיף לשמור על `tfugen_task_overdue_v2` הקיים). שים לב גם ל-`.slice(0,3)` בשורה 11061: ברגע שמוסיפים 375 NCR לבריכה, שלוש ההתראות עלולות להיבלע כולן ב-NCR ישנים — כדאי למיין לפי ימי איחור כמו שכבר עושים ב-expSoon.sort (index.html:11081).


### 1.13 — ✅ confirmed · מאמץ M

**ראיה**: אין שדה מדווח ב-NCR בכלל: רשימת השדות ב-svNcr (index.html:4318-4338) היא d/a/category/loc/location_id/project_id/issue_type_id/rc/c/o/u/s/sd/cd/notes/five_why/sens/p/ts — `o` הוא «אחראי», לא מדווח. grep ל-`reported_by|reporter_` על index.html + כל migrations/ מחזיר אפס. השרשור מכמעט-נפגע מאבד את המדווח לתוך שדה האחראי — index.html:13297: `setIf('ncr-o',nm.rep||'');` וב-13299 הקישור נשמר כטקסט חופשי בלבד: `setIf('ncr-notes','נוצר מדיווח כמעט-נפגע #'+nmId);`. בצד ההתראות, ההודעה היחידה על NCR (`ncr_critical`) הולכת לאחראי בלבד — index.html:11118: `var to=_waResolvePhone(n.o)||_waLastTo();`. אין שום אירוע `ncr_opened` / `ncr_closed` ברשימת האירועים (index.html:10479 ו-10539: ncr_critical, task_overdue, expiry_30days, round_missed, incident_critical, trustee_hazard). ובמסך העובד, `rEmpRecent` (index.html:15093) מציג «הדיווחים האחרונים שלי» אבל: לא כולל ncr, לא מסונן לפי מי דיווח, והתג הוא `'✓ '+when` בלבד — «נשמר לפני שעתיים», לא סטטוס.

**השפעה**: עובד שדיווח על מפגע רואה ✓ ירוק ואז שקט. הוא לא יודע אם נפתח NCR, אם מישהו טיפל, או שזה נסגר. זו בדיוק ההתנהגות שמייבשת דיווח — אחרי שלוש פעמים כאלה מפסיקים לדווח, והיחס near-miss:incident (ה-KPI בדשבורד, index.html:5020) נופל. בביקורת ISO §5.4 זו שאלה ישירה: «איך העובד יודע מה קרה עם מה שדיווח?» — כרגע התשובה היא «הוא יכול לפתוח את דף NCR ולחפש», כי `_REPORTER_PAGES` (index.html:3703) כן נותן למדווח גישה ל-ncr ו-view, ו-rNcr (index.html:9720) מסתיר ממנו רק רשומות `sens`. זה לא סגירת מעגל, זה חיפוש ידני ב-375 שורות.

**מיקום התיקון**: index.html:2016 (שדה «מדווח» חדש בטופס, עם `list="active-users-list"` כמו שאר שדות המדווח) · svNcr index.html:4318-4338 · _chainNmToNcr index.html:13297-13299 (להעביר את nm.rep לשדה החדש במקום לדרוס את האחראי, ולשמור `ncr_id` על רשומת ה-near_miss) · _notifBuildWaArgs index.html:11113-11120 (אירוע חדש) · rEmpRecent index.html:15093-15110 (להוסיף NCR + סטטוס במקום ✓ בלבד). מיגרציה: `ALTER TABLE ncr ADD COLUMN IF NOT EXISTS rep text`.

**הערה**: שלוש נקודות: (א) הבקלוג מעריך S — זה לא ריאלי. שדה חדש + מיגרציה + מסך העובד + התראה = M, והמיגרציה היא פעולה של מיכאל (כלל 7). (ב) תלות הדוקה בפריט 1.8 («השרשור מכמעט-נפגע הוא טקסט בלבד») — שניהם מתוקנים באותה נקודה בדיוק, index.html:13297-13299. לתקן אותם יחד, אחרת עושים את אותה עבודה פעמיים. (ג) יש תקדים שעובד בדיוק לזה, כדאי להעתיק ממנו ולא להמציא: מסלול הנאמנים כבר סוגר מעגל חלקי — הנאמן רואה את הליקויים שדיווח עם סטטוס פתוח/נסגר במסך שלו, ו-`_truMgrSetStatus` (index.html:14763) מעדכן אותו. מה שחסר שם זו התראה יוצאת בסגירה, בדיוק כמו ב-NCR. (ד) `_waResolvePhone` (index.html) מוצא טלפון רק אם השם תואם `full_name` או `username` ב-app_users עם `phone` — בייצור 12 המשתמשים הם כולם בתפקיד «מדווח», אז צריך לוודא שיש להם טלפון או שההתראה תיפול ל-whatsapp_skipped.


### 1.14 — ✅ בוצע ב-**#670** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: `openNewNcrModal` (index.html:4213-4225) — 12 שורות שכולן ניקוי שדות, `_locPopulate`, `openModal('m-ncr')` ובדיקת טיוטה. אין שום קריאה שסופרת רשומות קיימות. אותו דבר ב-editNcr (index.html:4279). המודאל עצמו (index.html:2001-2040) לא מכיל שום אלמנט באנר. זיהוי ההישנות היחיד בקובץ הוא של **בדיקות ציוד**, לא NCR: `_eqiDefSimilar` (index.html:11476) עם השורה המכריעה `return overlap/Math.min(ta.length,tb.length) >= 0.5;`, ו-`_eqiHistoryLoad` (index.html:11489) שמייצר באנר ב-11511: `var banner=anyRepeat?'<div ...>⚠ זוהו ליקויים חוזרים בהיסטוריה — מצריכים פעולה מתקנת</div>':'';`. הוא רץ רק על `equip_inspection_history` לפי `equip_id`, ולעולם לא על DB.ncr.

**השפעה**: מיכאל פותח NCR על נזילת שמן במילוי 3, ולא יודע שזו הפעם הרביעית השנה באותה עמדה. הוא כותב סיבת שורש נקודתית, סוגר, וזה חוזר. זה בדיוק ההבדל בין תיקון לפעולה מתקנת שה-§10.2 דורש. **אבל** — יש היום גרסה חלשה שכן עובדת: התובנה בדשבורד (index.html:5185-5187) מקפיצה «💡 N NCRs פתוחים באזור X — כדאי לבדוק» מ-3 ומעלה, והכרטיס «לפי אזור» (index.html:5461-5493) מדרג אזורים. שניהם סופרים **פתוחים בלבד** ובלי חלון זמן — אז NCR שנסגרו לא נספרים, וזו בדיוק ההישנות שמעניינת.

**מיקום התיקון**: index.html:2004 (div באנר חדש בראש `m-ncr`) · openNewNcrModal index.html:4213-4225 · editNcr index.html:4279 · פונקציית ספירה חדשה ליד `_eqiDefSimilar` index.html:11476 · אופציונלי: הרחבת `_ncrWhen` index.html:9734.

**הערה**: שלושה דברים שישברו מימוש נאיבי: (א) **מה משווים** — התאמה על `location_id` + `issue_type_id` היא זולה ואמינה; התאמת טקסט על התיאור צריכה לעשות שימוש חוזר ב-`_eqiNormalizeDef`/`_eqiDefSimilar` (index.html:11468-11488) ולא לשכפל אותן. (ב) **חלון 12 החודשים מסוכן** — svNcr שומר `sd:gv('ncr-sd')||null` (index.html:4331), כלומר תאריך הגילוי לרוב ריק, ו-375 הרשומות מהאקסל משתמשות ב-`src_date` (xlParse, index.html:15164) ולא ב-`sd`. יש כבר שתי גרסאות סותרות בקובץ: `_ncrWhen` ב-rNcr (index.html:9734) קורא `r.sd||r.ts` בלבד, והדוח החודשי (index.html:8569) קורא `n.sd||n.src_date||n.created_at||n.ts`. הגרסה של 8569 היא הנכונה — לאחד עליה, אחרת הבאנר יראה 0 על הרוב המוחלט של הרשומות. (ג) הבאנר חייב לספור **כולל סגורים** — אחרת הוא לא מודד הישנות אלא עומס.


### 1.15 — ✅ בוצע ב-**#670** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: `window._reopen` (index.html:13305-13326). שתי השורות המכריעות, index.html:13314-13315: `var stamp='\n[נפתח מחדש '+today+' על ידי '+u+': '+reason+']';` ואז `rec.notes=(rec.notes?String(rec.notes):'')+stamp;`. אחריהן `rec.s='פתוח';rec.cd=null;` ו-`sbUpd(tbl,rec)` — ולא שום מונה. grep ל-`reopen` (case-insensitive) על index.html + functions/ + migrations/ + tests/ מחזיר בדיוק 4 שורות: 11398 ו-11400-11401 (תנאי הכפתור וה-HTML שלו) ו-13305 (הפונקציה). אין עמודה: אף `ALTER TABLE ncr` בתיקיית migrations לא מוסיף `reopen_count`. ואין ספירה בשום דוח — הסעיף 10.2 בדוח מוכנות ה-ISO (`_isoClauses`, index.html:6279-6285) מודד `ncrN` / `ncrOpen` / `capaDue` / `verified` / `incN` / `nmN` בלבד, והדוח החודשי (index.html:8569-8595) מודד נפתחו / נסגרו / top-5 קריטיים.

**השפעה**: NCR שנסגר, נפתח מחדש, נסגר ונפתח שוב נראה בכל דוח כמו כל NCR אחר שנסגר — ההצלחה המדומה נספרת פעמיים. מדד «% נסגרו תוך 30 יום» (index.html:5018) והממוצע ימי פתיחה (5022) מחושבים מ-`ts` עד `cd`, ו-`_reopen` מעדכן את `ts` לזמן הפתיחה מחדש (index.html:13317) — כלומר פתיחה מחדש **משפרת** את המדד. בביקורת, «כמה פעולות מתקנות שלכם לא עבדו?» היא שאלה שמיכאל לא יכול לענות עליה בלי לקרוא 375 שדות notes.

**מיקום התיקון**: index.html:13314-13319 (`_reopen` — הגדלת מונה + addLog) · index.html:6279-6285 (סעיף 10.2 בדוח ה-ISO) · index.html:8569-8595 (הדוח החודשי) · index.html:11190-11193 (VIEW_CONFIG.ncr, להציג את המונה במסך הצפייה). מיגרציה: `ALTER TABLE ncr ADD COLUMN IF NOT EXISTS reopen_count integer DEFAULT 0`.

**הערה**: ארבע הערות: (א) **יש מסלול S בלי מיגרציה** — לספור את מופעי החותמת ב-notes ברג'קס (`/\[נפתח מחדש /g`), כי הפורמט קבוע מאז שהפיצ'ר נכתב. זה לא דורש הרצה של מיכאל ומספיק לדוח. חיסרון: שביר אם מישהו עורך notes ידנית. המיגרציה נקייה יותר אבל חוסמת את הפריט עד שמיכאל ירוץ (כלל 7). (ב) ממצא נלווה שלא היה בטענה: `_reopen` **לא קורא ל-addLog** — כל פונקציית sv* אחרת כן (svNcr ב-4345, svAud, svDoc). כלומר פתיחה מחדש לא מופיעה גם ביומן הביקורת `audit_log`, לא רק בדוחות. זו שורה אחת, כדאי להוסיף אותה באותו PR. (ג) תלות בפריט 1.4 (מאומת ✅): `_reopen` גם לא מאפס `verified_by`, אז רשומה שנפתחה מחדש ממשיכה להציג «✅ אומת» (index.html:11408). שלושתם באותן 20 שורות — לתקן בבת אחת. (ד) סיכון: `_reopen` משותף ל-tasks ול-ncr (index.html:11398). מונה על `ncr` בלבד ידרוש הסתעפות לפי `tbl`, או עמודה מקבילה ב-tasks.


### 2.6 — ✅ בוצע ב-**#664** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: index.html:4996-5002 — שורת «היום» נבנית כך: `'<div class="today-item" ... onclick="_todayClick(this.dataset.fn,this.dataset.a1,this.dataset.a2)">'` ואחריה רק `ti-ic` / `ti-body` / `ti-badge` / `ti-chev`. אין כפתור ✓, אין checkbox — כל השורה היא onclick אחד. ב-`_todayClick` (4898-4904) השורה שמכריעה היא 4902: `if(fn==='edit'){editTsk(a1);return;}`. `editTsk` (8930-8947) מסתיים ב-`openModal('m-tsk')`. במודאל m-tsk הדרך היחידה לסגור משימה היא `<select id="tsk-status"><option>פתוח</option>...<option>הושלם</option></select>` (2517) ואז `<button class="btn btn-p" onclick="svTsk()">שמור</button>` (2523). כלומר: הקשה על השורה (1) → פתיחת ה-select (2) → בחירת «הושלם» (3) → «שמור» (4). בדיוק 4 הקשות, כמו שנטען.

**השפעה**: מיכאל רואה בבוקר בטלפון משימה שכבר בוצעה אתמול בשטח, וכדי למחוק אותה מהרשימה הוא עובר מודאל מלא עם 8 שדות. במקום לסמן שלוש משימות תוך שלוש שניות — שלושה מודאלים. התוצאה המעשית היא שהרשימה לא מתנקה, ואז «היום» מציג פיגור שלא קיים.

**מיקום התיקון**: `_renderToday` index.html:4993-5002 (להוסיף כפתור ✓ לשורה כשהפריט אינו virtual) + `_todayClick` index.html:4898-4904 (ענף `if(fn==='done')`)

**הערה**: העזר כבר קיים — אסור לשכפל: `_tskQuickStatus(id,_TSK_DONE)` ב-index.html:8692-8702 עושה בדיוק את זה (מעדכן status, ממלא `closed_date`, `sbUpd`, `sdb`, ואז `rTasks()+rDash()`). הוא כבר מחובר לתפריט ⋯ בדף המשימות (8726: `item('✓','סגור (הושלם)',function(){_tskQuickStatus(taskId,_TSK_DONE);},'#16a34a')`). מכיוון ש-`rDash()` מריץ שוב את `_renderToday`, השורה תיעלם מעצמה — אין צורך בקוד רענון. שתי הסתייגויות: (א) זה תקף רק למשימות אמיתיות; פריטים וירטואליים ב«היום» הולכים ל-`fn==='view'` (4901) ואין להם שדה status לכתוב אליו, וכך גם «סבב בוקר», שורות הנאמנים ותור אימות ה-CAPA — הכפתור חייב להופיע רק כש-`it.fn==='edit'`. (ב) המאמץ שנטען (M) גבוה מדי בגלל (א) — בהיקף של משימות אמיתיות בלבד זה S.


### 2.7 — ✅ בוצע ב-**#664** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: index.html:4982-4983 — ההערה מבטיחה דבר אחד והקוד עושה אחר:
```
  // Sort by priority, then by due date
  items.sort(function(a,b){return a.priority-b.priority;});
```
אין שובר-שוויון. יתרה מזו, אובייקט הפריט בכלל לא נושא תאריך: `baseItem` ב-4969 הוא `{icon,title,fn,a1,a2}` ואחריו `Object.assign(baseItem,{priority:1,sub:...,badge:'פיגור',badgeCls:'red'})` (4972) — אין `due`. מכיוון ש-`Array.sort` יציב, הסדר בתוך priority:1 הוא סדר `allTasks` (4962: `var allTasks=(DB.tasks||[]).slice();`), כלומר סדר `DB.tasks` — שמגיע מהשרת ממוין `ts.desc` (3009: `tasks:'ts'`, 3024: `var ord=(_sbTsCol[tbl]||'created_at')+'.desc';`).

**השפעה**: בתוך «פיגור» הסדר הוא לא רק שרירותי — הוא הפוך: המשימה שנגעו בה לאחרונה עולה לראש, והמשימה שתקועה הכי הרבה זמן יורדת למטה. ומכיוון ש-`var TOP=6;` (4984) חותך את הרשימה לשישה פריטים, פיגור של חודשיים יכול פשוט לא להופיע על המסך — הוא מוסתר מאחורי «→ ראה הכל». זה בדיוק ההפך ממה שמנהל בטיחות צריך לראות ראשון.

**מיקום התיקון**: `_renderToday` index.html:4966-4983 — להוסיף `due:t.due` לשלושת ה-`items.push` (4972/4974/4976) ולהחליף את המשווה ב-4983

**הערה**: שתי מלכודות: (1) הפריטים שאינם משימות (סבב בוקר 4914, ליקויי נאמנים 4921/4938/4950, תור אימות CAPA 4930) לא יקבלו `due` לעולם — המשווה חייב fallback שלא יזרוק אותם לסוף/להתחלה באקראי; ההיגיון הנכון הוא `a.priority-b.priority || String(a.due||'9999').localeCompare(String(b.due||'9999'))`. (2) אל תשתמש ב-`du()` בתוך המשווה — היא נקראת O(n log n) פעמים ובונה `new Date` בכל קריאה; השוואת מחרוזת YYYY-MM-DD מספיקה ונכונה. הערה על ניסוח הטענה: «סדר ההכנסה» אינו מדויק — הסדר האמיתי הוא ts.desc מהשרת (ומשימות שנוצרו בסשן הזה נדחפות בסוף ב-`DB.tasks.push` ב-svTsk 8925). זה לא מרכך את הטענה אלא מחמיר אותה. ב-DECISIONS.md:1084 מתואר פאנל «היום» כ«משימות בפיגור / תפוגות / סבב ממתין» — אין שם שום החלטה שקובעת מיון לפי עדיפות בלבד, כלומר זה באג ולא כוונה.


### 2.8 — ✅ בוצע ב-**#670** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: שני השלישים הראשונים נכונים, השליש האחרון לא. (א) index.html:766: `<button class="bell" id="notif-btn" title="הגדרות התראות" onclick="openNotifSettings()">&#128276;</button>` — הפעמון אכן פותח הגדרות. (ב) «לא נקרא» באמת לא קיים בשום מקום: `migrations/2026-05-02_notifications_log.sql` מגדיר `notifications_log(id, user_email, event_type, channel, payload, ts)` — אין עמודת read/seen; grep ל-unread ב-index.html מחזיר רק הערת CSS בשורה 46; ל-`notif-btn` אין badge (בניגוד ל-`my-tasks-btn` בשורה 767 שיש לו `my-tasks-badge`). (ג) toast: `setTimeout(function(){if(t.parentNode)t.parentNode.removeChild(t);},2500);` — 2.5 שניות, מדויק. (ד) אבל **«ואין לאן לחזור» שגוי**: אותו מודאל עצמו שהפעמון פותח מכיל את ההיסטוריה — index.html:2650-2653: `<summary>📋 היסטוריית התראות (20 אחרונות)</summary><div id="notif-log-list">` + כפתור `_notifLogExport()` ל-CSV, והיא מרונדרת ב-`openNotifSettings` 10531-10561 כולל קישור «הצג את כל ה-N הרשומות» (10553).

**השפעה**: מיכאל מפספס toast (נסיעה, מסך כבוי, כיס) ואין לו סימן שמשהו קרה — הפעמון נראה זהה תמיד. הוא כן יכול לשחזר: פעמון → לגלול למטה → לפתוח את האקורדיון «היסטוריית התראות». כלומר הנתון קיים אבל קבור בשלוש הקשות בתוך מסך ששמו «הגדרות», בלי מונה ובלי אבחנה בין ישן לחדש. הפער האמיתי הוא «אין מרכז התראות ואין מצב נקרא», לא «המידע אבוד».

**מיקום התיקון**: index.html:766 (יעד הפעמון + badge), `openNotifSettings` index.html:10531-10561 (להוציא את היומן מה-`<details>` למסך משלו), index.html:2650-2653 (ה-markup של האקורדיון)

**הערה**: לא נדרשת מיגרציה: סימן «נקרא עד» יכול לשבת ב-localStorage כ-watermark של `ts` (כמו `tfgn_alert_dism_<date>` ב-4881-4888), ואז ה-badge הוא ספירת שורות ב-`DB.notifications_log` עם `ts` גדול ממנו. שתי מגבלות שחייבים לדעת מראש: (1) `notifications_log` נמצא ב-`window._SDB_SKIP` — הוא אף פעם לא נשמר ל-localStorage ומגיע רק מהשרת עם `_sbLimit={notifications_log:500}` (3023), כלומר בטלפון בלי רשת מרכז ההתראות יהיה ריק; (2) ה-RLS הוא `notifications_log_admin_manager_all` (private.is_admin_manager()) — לנאמן/מדווח אין שם כלום, אז ה-badge חייב להיות מוסתר לפי `_role()` ולא להראות 0 תמידי (זו בדיוק התקלה של פריט 2.3). תלות: ההתראות ב-`_notifyEvent` נרשמות ליומן דרך `_notifLog` (11100-11107) שכבר כותב `ts` — אין מה לשנות בצד הכתיבה.


### 2.9 — ✅ בוצע ב-**#664** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: `_notifBuildWaArgs` (index.html:11108-11150) נופל ל-`tfgn_wa_last` בכל ארבעת האירועים, והשורות המכריעות הן: 11118 `var to=_waResolvePhone(n.o)||_waLastTo();` · 11126 `var to2=_waResolvePhone(i.r||i.rep)||_waLastTo();` · 11133 `var to3=_waResolvePhone(t.assignee)||_waLastTo();` · 11143 `var to4=_waResolvePhone(r.w||r.vendor||r.o||r.c)||_waLastTo();` — ו-`round_missed` ב-11147 הוא `var to5=_waLastTo();` בלי שום ניסיון זיהוי. `_notifyEvent` ב-11160 שולח ישירות: `_waSend(args.to,{template:args.template,language:args.language,params:args.params});` — **בלי confirm**, בניגוד למסלול הידני `_waNcrAlert`/`_waTaskAlert`/`_waExpiryAlert` (15254-15285) שעובר דרך `_waConfirm` ומציג את המספר. ההערה ב-11164 `// Couldn't resolve phone — fall back to log (no spam to default number)` מבטיחה בדיוק את ההיפך ממה שקורה — הענף הזה נגיש רק כאשר גם `_waLastTo()` ריק. מקור הערך: 15245 `function _waLastTo(){return localStorage.getItem('tfgn_wa_last')||'';}`, ונכתב במקום אחד בלבד — 15303 `try{localStorage.setItem('tfgn_wa_last',to);}catch(e){...}` בתוך `_waTestSend`. והמסלול רץ אוטומטית: `rDash` 5135 `setTimeout(_notifDailyScan,1500)` → `_notifDailyScan` (11047-11095) → `_notifyEvent`.

**השפעה**: אם ערוץ WhatsApp מסומן בהגדרות, כל התראה שלא הצליחה לזהות טלפון לפי שם — וזה המצב הרגיל, כי `_waResolvePhone` (15246-15251) מתאים רק מול `DB.app_users` לפי `full_name`/`username` מדויק, בעוד «אחראי» במשימות ובציוד הוא טקסט חופשי — נשלחת בשקט למספר שמיכאל הקליד לאחרונה בכרטיס «בדיקת WhatsApp». «סבב בוקר חסר» הולך לשם **תמיד**. אם הוא בדק פעם אחת מול מספר של קבלן או של עובד, אותו אדם מקבל מעכשיו התראות על NCR קריטי, תפוגות ומשימות בפיגור של המפעל. אין דיאלוג אישור ואין דרך לדעת — היומן רושם `whatsapp` בלי המספר (11161 מעביר `payload` בלבד).

**מיקום התיקון**: `_notifBuildWaArgs` index.html:11118, 11126, 11133, 11143, 11147 (חמישה אתרים) + `_notifyEvent` index.html:11158-11168

**הערה**: תלוי ישירות ב-2.10 — אלה שני חצאים של אותו תיקון. אם מוחקים את `||_waLastTo()` בלי יעד מוגדר, `round_missed` מת לגמרי (אין לו מקור אחר) ושאר האירועים יירשמו `whatsapp_skipped` — שיפור באבטחה אבל נסיגה בתפקוד, ומיכאל יחווה את זה כ«ההתראות הפסיקו». הסדר הנכון: קודם 2.10 (שדה «המספר שלי»), ואז כאן `_waResolvePhone(...)||_myPhone()` ו-`round_missed` → `_myPhone()` בלבד. כדאי גם להוסיף את `to` ל-payload ב-11161 כדי שהיומן יראה לאן נשלח בפועל. לא לגעת ב-`_waConfirm` (15250-15253) — שם הוא כבר תקין. הערה: כרטיס הבדיקה `dash-wa-test` מוצג רק לאדמין (13879-13880), אז המספר תמיד הוקלד על ידי מיכאל עצמו — «יכול להיות כל אחד» נכון, אבל במובן של «כל מי שמיכאל בדק מולו פעם», לא של מספר זר אקראי.


### 2.10 — ✅ בוצע ב-**#664** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: הפער אמיתי: אין בשום מקום «המספר שלי». `_notifBuildWaArgs` (11108-11150) לא קורא את ההעדפות בכלל לצורך נמען — הוא מכיר רק `_waResolvePhone(...)` ו-`_waLastTo()`. **אבל** שדה טלפון כבר יושב בדיוק במודאל שהפריט מבקש: index.html:2644 `<div class="field"><label>📱 WhatsApp למספר</label><input id="notif-tru-wa" placeholder="9725XXXXXXXX" inputmode="tel" dir="ltr"></div>`, נשמר ב-`saveNotifSettings` 10727 `newPrefs.trustee_hazard.whatsapp_to=gv('notif-tru-wa').replace(/[^0-9]/g,'');` ונטען חזרה ב-10520 `var _wa=g('notif-tru-wa');if(_wa)_wa.value=_th.whatsapp_to||'';`. הוא משויך לאירוע אחד בלבד (`trustee_hazard`) ונצרך רק בצד השרת ב-`/api/trustee-notify` (10708). כלומר העבודה היא להרחיב שדה קיים, לא להוסיף אחד מאפס.

**השפעה**: היום, אם מיכאל רוצה לוודא שהתראה תגיע אליו, הדרך היחידה היא לפתוח את כרטיס האדמין «בדיקת WhatsApp» בדשבורד, להקליד את המספר וללחוץ «שלח» — ורק כתופעת לוואי זה נשמר כברירת המחדל של כל ההתראות. זו התנהגות נסתרת לגמרי: שום דבר במסך לא אומר «המספר שהקלדת כאן הוא מעכשיו היעד של כל התראה שלא זוהתה».

**מיקום התיקון**: index.html:2640-2650 (להוציא שדה «המספר שלי» מתוך `#notif-trustee-box`), `openNotifSettings` index.html:10519-10521, `saveNotifSettings` index.html:10720-10730, `_notifEnableRecommended` index.html:10685-10692, `_notifBuildWaArgs` index.html:11118-11147

**הערה**: מלכודת שתשבור את התיקון אם מפספסים אותה: **שני** כותבים בונים את `prefs` מאפס ודורסים את הקיים — `saveNotifSettings` (10720-10730) ו-`_notifEnableRecommended` (10685-10692, `newPrefs[ev.key]={inapp:true,...}` בלולאה על NOTIF_EVENTS, ואז `var rec={id:u,prefs:newPrefs,...}`). כל מפתח חדש ברמה העליונה (למשל `prefs.my_phone`) ייעלם ברגע שמיכאל ילחץ «✓ הפעל התראות בתוך האפליקציה» בבאנר. חייבים להוסיף אותו לשני המקומות. אין צורך במיגרציה — `notification_prefs.prefs` הוא `jsonb` (migrations/2026-05-01_notification_prefs.sql) וה-RLS הוא `id = auth.jwt()->>'email'`, כלומר המספר פרטי למשתמש. חובה לשמור בפורמט שהשרת מצפה לו: 9 ספרות אחרי 972, כמו שמאמת `_waTestSend` ב-15298 (`/^\+?972[0-9]{9}$/`) — ולהשתמש באותה ולידציה, לא להמציא חדשה. הסימון 👤 בתור העבודה נכון: מיכאל צריך למסור את המספר בפועל.


### 2.11 — ✅ בוצע ב-**#670** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: index.html:830-900 is the whole `pg-dash` markup. I counted the card divs in source order: 29 `id="dash-*-card"` / admin-tool divs. Every one of them is born hidden and shown only when it has data — e.g. index.html:5246 `var daCard=g('dash-auds-card');if(daCard)daCard.style.display=ra.length?'':'none';` and the same pattern repeats 24 more times inside rDash (index.html:5282, 5327, 5335, 5368, 5423, 5463, 5498, 5520, 5541, 5582, 5602, 5622, 5644, 5663, 5697, 5720, 5751, 5774, 5795, 5823, 5863, 5897). So ~20 visible cards on a production DB is a fair count — the claim's number is not inflated.

BUT the deciding line the claim missed is index.html:881, which already exists verbatim:
`<div class="dash-section-title" id="dash-stats-title">&#128202; &#1505;&#1496;&#1496;&#1497;&#1505;&#1496;&#1497;&#1511;&#1493;&#1514; &#1493;&#1502;&#1490;&#1502;&#1493;&#1514;</div>`
which decodes to exactly `📊 סטטיסטיקות ומגמות` — the label the claim proposes creating. And it already self-hides when the section is empty, index.html:5123-5133 `window._dashHideEmptySection=function(){ var t=g('dash-stats-title');if(!t)return; ... t.style.display=any?'':'none'; }`.

Two further corrections. (a) The section title sits at line 881, so only 19 of the cards are under it; 5 live stat cards (dash-weekly-card 872, dash-month-card 874, dash-tsk-bd-card 875, dash-ncr-time-card 876, dash-totals-card 880) sit ABOVE it and folding "under" the title would not catch them. (b) Two of the 29 are dead code, not cards a user ever sees: index.html:5293-5295 `var dttc=g('dash-today-tasks-card'); if(dttc)dttc.style.display='none'; var dtt=null; if(dtt){` — the whole 'tasks due today' block is unreachable behind `var dtt=null`; and dash-recent-card carries the comment "פעילות אחרונה hidden — clutters the home screen".

**השפעה**: Real. Michael opens the app and, below the actionable 🎯 היום list and the quick-action tiles, scrolls past roughly 20 cards before the page ends. On the iPhone that is a long thumb-scroll every single morning. What is NOT true is that the cards are ungrouped — there is already a divider headed 📊 סטטיסטיקות ומגמות at the top of the long tail, and on a phone (single column) it is visible. What is missing is that the divider does not collapse: it labels the pile, it does not fold it.

**מיקום התיקון**: index.html:881 (turn `dash-stats-title` into a `<details class="more-accordion">` summary), index.html:882-900 (the 19 cards to wrap), index.html:5123-5133 `_dashHideEmptySection`, index.html:557 (the wide-screen column CSS)

**הערה**: Three things change the picture before anyone edits this. (1) `_dashHideEmptySection` (index.html:5123-5133) walks `var any=false,el=t.nextElementSibling; while(el&&el.classList&&el.classList.contains('card')){...}` — it depends on the cards being the title's SIBLINGS. Wrap them in a `<details>` and that walk finds nothing, so the section header would hide itself even when full. Rewrite it in the same commit. (2) index.html:557 targets `#pg-dash > .dash-section-title` and index.html:558 `#pg-dash > .card` with `column-span:all` / `break-inside:avoid` for the 2-column (≥1080px) and 3-column (≥1500px) desktop layout — those are direct-child selectors, so a wrapper silently kills the desktop masonry. (3) Reuse the existing `.more-accordion` class (index.html:264-269) — it is already the fold pattern used by `dash-kpi-acc` at index.html:871, already has the ▼ chevron and is already in the print rule at index.html:653/673. Do not invent a second accordion style. Nothing in DECISIONS.md forbids folding this section; the P1#1 decision (DECISIONS.md:1084) explicitly set the hierarchy "🎯 היום actionable at the top → KPI strip → accordion for additional metrics", so folding the statistics tail is a continuation of that decision, not a reversal of it.


### 2.12 — ✅ בוצע ב-**#664** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: The menu is `window._topMoreMenu` at index.html:8737-8781, opened from the topbar at index.html:768 `<button class="bell" id="more-menu-btn" title="עוד פעולות" onclick="_topMoreMenu(this)">&#8943;</button>`. I counted the `item(` calls between 8737 and the closing `document.body.appendChild(menu)`: **15, not 16** (lines 8759, 8760, 8761, 8764, 8765, 8767, 8768, 8769, 8770, 8771, 8772, 8773, 8775, 8776, 8777). Three of those are gated — 8760 and 8765 by `if(typeof _isAdminUser==='function'&&_isAdminUser())`, 8767 by `if(typeof _role==='function'&&_role()!=='reporter')` — so an admin/manager like Michael sees all 15, a reporter sees 12.

"רשימה שטוחה" is also not quite right: there are 4 `sep();` calls (8762, 8766, 8774, 8776... precisely 8762, 8766, 8774 and the one before 8777) splitting the list into 5 groups — sync/trustees, AI, reports+exports, accessibility/help, about. They are hairlines with no headings, so the grouping reads weakly, but it is not a flat list.

The version half of the claim is CONFIRMED and is worse than stated — it is hardcoded in three independent places:
- index.html:8777 `item('ℹ','אודות (v2026.05.02)',function(){...});`
- index.html:13008 `alert('TFUGEN Safety Management System\nגרסה: 2026.05.02\n...')` inside `_showAbout`
- index.html:13012 `var snapshot={ts:new Date().toISOString(),app:'tfugen-safety',version:'overnight-2026-05-02',data:DB};` inside `_exportBackup`
There is no `APP_VER` constant anywhere — `grep -n "APP_VER"` returns nothing. STATUS.md:473 records `- [x] **Version label (PR #208)** — v2026.05.02 בtopbar`, so the label was deliberate; it has simply never been touched since, and today is 2026-09-20 — it is 4.5 months stale.

One real defect the claim does NOT mention, and it is the one that bites on a phone: index.html:8741 sets the menu box to `'position:fixed;z-index:9999;background:#fff;border:1px solid #dde0e6;border-radius:10px;box-shadow:0 6px 20px rgba(0,0,0,0.18);padding:6px 0;min-width:200px;font-family:inherit;direction:rtl'` — **no max-height, no overflow-y** — and index.html:8744 positions it with `menu.style.top=(rect.bottom+4)+'px';` with **no viewport clamp**. Compare `_tskMenu` at index.html:8709 which flips (`if(top+260>window.innerHeight)top=Math.max(8,rect.top-260);`) and the generic `_popMenu` at index.html:8878-8880 which clamps horizontally. `_topMoreMenu` does neither.

**השפעה**: Two different things. (1) Navigation: 15 rows, each `padding:11px 14px` at `font-size:13px`, so ~41px per row plus 4 hairlines ≈ 660px of menu hanging from a topbar ~60px down. On Michael's iPhone that is fine on a 844pt-tall 13/14/15, but on a 667pt screen (SE / 8) the last rows — including אודות and עזרה — fall below the fold with no way to scroll to them, because the box has no `overflow-y`. (2) Version: the About dialog tells him the system is version 2026.05.02 when he is running 2026-09-20 code. For a safety manager that matters more than it looks — the version is what he would quote to an ISO auditor asking which revision of the system produced a record, and it is wrong.

**מיקום התיקון**: index.html:8737-8781 `_topMoreMenu` (add group headings and/or `max-height:calc(100vh-80px);overflow-y:auto` to the cssText at 8741), index.html:8777, index.html:13007-13009 `_showAbout`, index.html:13012 `_exportBackup`

**הערה**: Reuse, do not duplicate: `window._popMenu` (index.html:8790-8889) is the generic anchored dropdown the rest of the app already standardised on — `_eqiMoreMenu`, `_truMgrMenu` (index.html:15029), `_legRowMenu`, `_itypeRowMenu` all use it, and it already has the viewport clamp `if(mr.left<8){...}else if(mr.right>window.innerWidth-8){...}`. `_topMoreMenu` predates it and is a copy-paste of the same 40 lines. The clean fix is to reimplement `_topMoreMenu` on top of `_popMenu` — its `item(emoji,label,fn,danger)` signature is call-compatible — but **keep the menu id `'top-more-menu'`**, because the print stylesheet at index.html:653 hides it by that exact id (`.overlay,.modal,#top-more-menu,#eqi-more-menu,...{display:none!important}`) and PR #555 relies on that so the menu does not leak onto printed pages. Risk if you reorder or nest items: STATUS.md records five separate sessions that added entries here (#563 renamed the trustee entry, #597 moved search into ☰ not here, #622 added the ISO readiness report, #619/#599 added entries to the *trustees* ⋯ which is a different menu at index.html:15029) — check none of those labels is the one Michael was told to look for. For the version, introduce one `var APP_VER='2026.09.20'` near the top and read it in all three sites; changing only the menu label would leave `_showAbout` contradicting it.


### 3.3 — 🟨 partly · מאמץ M

**ראיה**: אין שום כפתור «חידוש» ידני — grep ל-renew/חידוש מחזיר רק את זרימת ה-PDF של הציוד. עריכה אכן דורסת: `_svPut` (index.html:4029-4042) — `arr[k]=Object.assign({},arr[k],r);sbUpd(tbl,arr[k]);` — ו-`svEqi` (6931, שורה 6956) — `if(id){DB.equip_inspections=DB.equip_inspections.map(function(r){return r.id===id?rec:r;});sbUpd('equip_inspections',rec);}`. אבל הטענה מגזימה בשלוש נקודות: (א) ל-equip_inspections *יש* מסלול חידוש עם היסטוריה שמורה — `_eqiPdfClassify` (7479-7489) `return {tier:'renewal',candidates:byPrev.map(_eqiPdfWrapCand)};`, הבאדג' ב-7612 `badge='♻ חידוש (לפי תסקיר קודם)'`, ו-`_eqiHistoryWrite` (7893) כותב שורה ל-`equip_inspection_history`; היא נקראת מ-7861 ומ-7889 בלבד (מסלול ה-PDF), לעולם לא מ-`svEqi`. ההיסטוריה מוצגת ב-`_eqiHistoryLoad` (11489) דרך `extra:'_eqiHistorySection'` ב-VIEW_CONFIG (11248). (ב) ל-`docs` אין בכלל מסלול עריכה: `svDoc` (4199-4202) תמיד `var r={id:gid(),...};DB.docs.push(r);sbIns('docs',r);` — חידוש מסמך יוצר שורה שנייה, והשורה הפגה נשארת ומתריעה לנצח (זה פריט 3.8). (ג) `_aud('upd',tbl,row)` (3219-3235, דרך `sbUpd` ב-3238) כן רושם ל-`audit_log` מי עדכן מה ומתי — `var entry={user_email:u,table_name:tbl,record_id:record_id,op:op,title:title};` — אבל בלי ערכי השדות הישנים.

**השפעה**: מיכאל מחדש תעודת הדרכה או תוקף צמ"ג: הוא פותח ✎, משנה את התאריך, שומר — ותאריך התפוגה הקודם נעלם. מול מבקר ISO הוא יכול להראות ב-audit_log ש«מישהו עדכן את TR-17 ב-3/9» אבל לא מה היה התוקף לפני כן ולא מתי בוצעה ההדרכה הקודמת. עבור ציוד — אם הוא מעלה תסקיר PDF, ההיסטוריה כן נשמרת ואף מזהה ליקויים חוזרים; אם הוא עורך ידנית ב-✎, היא לא. עבור מסמכים — לא נמחק כלום, אבל מצטברות כפילויות ודף התפוגות מציג לנצח את הגרסה הישנה כ«פג».

**מיקום התיקון**: פעולת «חידוש» חדשה + כתיבת שורת היסטוריה: `_svPut` (index.html:4029-4042), `svEqi` (6931-6960, בעיקר 6956), `svDoc` (4199-4205). התבנית הקיימת לחיקוי: `_eqiHistoryWrite` (7893-7920) + `_eqiHistoryLoad` (11489).

**הערה**: לפני שבונים טבלת היסטוריה כללית — כדאי להחליט אם משכפלים את `equip_inspection_history` לכל טבלה או מוסיפים טבלה אחת `record_history`; DECISIONS.md שורות 1383-1390 קובע ל-`ncr_ai` את דפוס ה-`version` (כל ניתוח = גרסה חדשה) ודוחה במפורש עמודת jsonb יחידה «אין היסטוריה, קשה ל-query» — אותו שיקול חל כאן. תלוי ב-3.8: אין טעם להוסיף «חידוש» ל-docs לפני שיש שם בכלל עריכה. חובה לעבור דרך `_EDIT_MODS` (4024) ולא לשכפל טפסים, ולשמור על כלל 2 של CLAUDE.md — תאריך ריק → `null`, לא `''`.


### 3.4 — ✅ בוצע ב-**#654** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: כל שלושת החלקים מאומתים, והתמונה גרועה ממה שנטען. **לא בתפוגות**: `_expCollect` (5986-5993) מונה חמש טבלאות בלבד — `docs`, `tr`, `ppe`, `ctr`, `equip_inspections`; `med` איננה. אותו דבר ב-`_expNoDate` (5996-6011) — חמש קריאות `take()`, בלי med. **לא ב-VIEW_CONFIG**: המפתחות ב-11191-11266 הם docs/auds/ncr/inc/tr/rsk/emp/ppe/ins/drl/ctr/wst/hzm/env/leg/near_miss/equip_inspections/rounds/hearing_tests/tasks/toolbox/env_aspects/projects — אין `med`, וגם `_VIEW_EDITOR_FALLBACK` (11268) לא מכיל אותה, לכן `showView('med',id)` נופל ל-11274: `console.warn('showView: no VIEW_CONFIG for',tbl);toast('לא ניתן להציג — טבלה "'+tbl+'" אינה נתמכת');return;`. **כן בלוח השנה**: `_calCollect` שורה 8457 — `['ppe','tr','docs','ctr','equip_inspections','med'].forEach(...)`, ו-`_calOpenDay` (8516) מרכיב שורה עם `onclick="showView(this.dataset.vtbl,this.dataset.vid)"` — כלומר הקליק בלוח השנה מוביל בדיוק ל-toast השגיאה. **כן בסקירת ההנהלה**: `med_pct:pctValid('med')` (11998), וגם `_aiWeeklySummary` (11898), `_aiTodayPlan` (12157, עם `label={...,med:'בדיקה רפואית'}`), ה-snapshot של הצ'אט (12375), וה-ICS (13133). **כן בהתראות**: 11072 — `['ppe','tr','docs','ctr','drl','ins','med','equip_inspections']` לאירוע `expiry_30days`. **מעבר לטענה**: אין בכלל עמוד `med` — אין `pg-med` ב-HTML, אין `rMed` במפת `rPage` (16464-16466), ואין אף `goPage('med')` בקובץ, למרות ש-`_MANAGER_PAGES` (3701) כולל `med:1`. רשומות מגיעות ל-`med` רק דרך Smart Capture (17981-17984: `DB.med=DB.med||[];DB.med.push(rec);sbIns('med',rec);`) ו-OneDrive (17457, 18195).

**השפעה**: בדיקה רפואית שנסרקה נכנסת ל-DB, מקפיצה נקודה צהובה בלוח השנה ומייצרת התראת «פג בעוד 30 יום» — ומיכאל לוחץ עליה ומקבל «טבלה med אינה נתמכת». היא לא מופיעה בדף התפוגות, אין לה עמוד רשימה, אין איך לערוך אותה ואין איך למחוק אותה מה-UI. בדוח ISO §9.1 (6266) היא בכל זאת נספרת — הדוח יכתוב «12 בדיקות רפואיות · 3 פגו» על נתונים שמיכאל לא יכול לפתוח ולבדוק. מול מבקר זה הפער הכי מביך: המערכת מכריזה על מעקב רפואי שאין לו מסך.

**מיקום התיקון**: S לחלק שהטענה מתארת: להוסיף שורת `med` ל-`_expCollect` (5986-5993) ולקריאת `take()` ב-`_expNoDate` (5996-6011), ולהוסיף מפתח `med` ל-`VIEW_CONFIG` (אחרי 11221, ליד `ppe`). העמוד החסר עצמו — `pg-med` + `rMed` + רישום במפת `rPage` (16465) — הוא M נפרד.

**הערה**: לתקן קודם את `VIEW_CONFIG.med` ואז את `_expCollect` — בלי ה-VIEW_CONFIG כל שורה חדשה בדף התפוגות תיתן 👁 שמוביל לאותו toast שגיאה, כי `rExp` (6089) ממפה `var _tbl={docs:'docs',tr:'tr',ppe:'ppe',ctr:'ctr',eqi:'equip_inspections'}` וקורא `showView`. שדות מוצעים ל-VIEW_CONFIG לפי מה ש-Smart Capture כותב בפועל (17981-17984): `n` (שם), `t` (סוג), `d` (תאריך), `e` (תפוגה), `file_url`. שם העמוד ב-`back:` חייב להיות עמוד קיים — אם לא בונים `pg-med`, להפנות ל-`exp`. `med` כבר ב-`_BACKUP_TABLES` (16780), ב-syncTbls (3550) וב-`_MANAGER_PAGES` (3701), ולכן לא צריך migration ולא שינוי הרשאות.


### 3.5 — ✅ confirmed · מאמץ M

**ראיה**: ה-schema לא מכיל שדה תפוגה: `migrations/2026-04-20_hearing_tests.sql` שורות 12-26 — `id, emp_name, emp_id, dob, age, gender, role, dept, category, year, notes, test_date, inspector, ts`. אין `e`. החיפוש היחיד ב-migrations שנוגע שוב לטבלה הוא `ALTER TABLE hearing_tests ADD COLUMN IF NOT EXISTS location_id text` (2026-05-02_location_fk_inc_hearing.sql:13) — כלומר מעולם לא נוסף שדה תוקף. **לא נסרק**: `_expCollect` (5986-5993) לא מזכיר את הטבלה; `_calCollect` (8457) לא; ה-ICS (13130-13134) לא; רשימת `expiry_30days` (11072) לא. **לא מתריע**: `rHearing` (13514-13531) מדפיס `'<td>'+fd(r.test_date)+'</td>'` — `fd` בלבד, בלי `eb()`, כך שאין ולו באדג' צבע אחד בכל העמוד. **בדוח ISO**: שורה 6266 — `var medX=_isoExpiry('med'),hearN=_isoCount('hearing_tests');` — `_isoCount` הוא `(DB[tbl]||[]).length` (6184), ספירה גולמית. הטקסט שנבנה ב-§9.1 כותב `hearN+' בדיקות שמיעה'` והמשפט של הפער מסתכל רק על `medX.gone`.

**השפעה**: מרשם האודיומטריה — הנתון שפקח משרד העבודה מבקש ראשון בחשיפה לרעש — הוא רשימה סטטית. בדיקה מ-2019 ובדיקה מהשבוע נראות זהות: אותו טקסט שחור, בלי באדג', בלי «פג». מיכאל לא יקבל התראה, הבדיקה לא תופיע בלוח השנה ולא בדף התפוגות, והדוח ל-ISO יכריז «187 בדיקות שמיעה» כאילו כולן בתוקף. זו הטבלה היחידה שיש לה מעמד סטטוטורי מפורש ואין לה שום מנגנון מועד.

**מיקום התיקון**: migration חדש `ALTER TABLE hearing_tests ADD COLUMN IF NOT EXISTS e date;` + `_expCollect` (5986-5993) + `_expNoDate` (5996-6011) + `_calCollect` (8457) + `expiry_30days` (11072) + ה-ICS (13130) + `rHearing` (13514-13531, להוסיף עמודה עם `eb(r.e)`) + `VIEW_CONFIG.hearing_tests` (11251-11255, להוסיף `['תפוגה','e']`) + הוספת `hearing_tests` למיפוי `_tbl` ב-`rExp` (6089).

**הערה**: מלכוד אמיתי: אין שום טופס לבדיקת שמיעה. הרשומות נכנסות רק דרך ייבוא ה-Excel — `hearXlOk` (13507-13512): `data.forEach(function(r){DB.hearing_tests.push(r);sbIns('hearing_tests',r);});` — ואין `m-hearing` ואין `svHearing`. לכן הוספת עמודה לבדה לא תמלא כלום: או שמחשבים `e = test_date + מחזור` (התקנות בדרך כלל שנתי/דו-שנתי לפי רמת חשיפה — שאלה למיכאל), או שמוסיפים עמודה לגיליון הייבוא. הדרך הזולה והבטוחה: פונקציית עזר `_hearExpiry(r)` שמחזירה `test_date + 12 חודשים` ולהזין אותה ל-5 הסורקים, בלי migration בכלל — אז אין סיכון ל-schema ואין תלות בהרצה ידנית של מיכאל (כלל 7 ב-CLAUDE.md). לשים לב: `hearing_tests` היא טבלה מוגנת ב-`_DEL_PROTECTED` (3599) — אל תיגע בשורות קיימות.


### 3.6 — ✅ בוצע ב-**#654** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: `svItp` (10805-10813), שורה 10808 היא ההכרעה: `var r={id:id,name:name,recur_count:parseInt(gv('itp-recur-count'))||1,recur_unit:gv('itp-recur-unit')||'year',next_due:gv('itp-next-due')||null,responsible:...};` — `next_due` נלקח כפשוטו משדה `<input type="date">`. `recur_count`/`recur_unit` נכתבים ונקראים במקום אחד בדיוק: תווית התצוגה ב-`rItp` שורה 10774 — `'<span style="font-size:11px;color:var(--t3)">כל '+esc(String(r.recur_count||1))+' '+esc(unitLbl[r.recur_unit]||r.recur_unit)+'</span>'` — ועוד פעמיים ב-`editItp` (10794-10795) כדי למלא את הטופס בחזרה. grep ל-`recur_count|recur_unit|next_due` על כל ה-repo (index.html + workers/ + functions/ + tools/) לא מחזיר שום חישוב, שום `setMonth`, שום cron. `_collectVirtualTasks` (8953-8960) קורא את `next_due` אבל רק קורא: `if(!it.active||!it.next_due)return; var d=du(it.next_due); if(d>30)return; ... var prefix=d<0?'פג ב-'+(-d)+' ימים: ':...`. והמשימה הווירטואלית היא לקריאה בלבד — ב-`rTasks` שורות 9613-9617 היא מקבלת שלושה כפתורים: `srcViewBtn` (👁), `promoteBtn` (➕ openTskModal) ו-`srcDelBtn` (🗑 askDel על טבלת המקור). אין «הושלם», ואין checkbox (9625: `var cb=r.isVirtual?'':'<input type="checkbox"...`).

**השפעה**: סוג בדיקה תקופתית שמיכאל הגדיר «כל שנה» עם מועד 1/9/2026 יופיע במשימות ב-2/8, ואז יישאר שם לנצח: «פג ב-1 ימים», «פג ב-19 ימים», «פג ב-77 ימים» — גם אחרי שהבדיקה בוצעה. אין לו דרך לסמן שבוצעה; הדרך היחידה היא לזכור לפתוח מודולים → סוגי בדיקה → ✎ → להקליד תאריך חדש ידנית. בפועל זה אומר שהמונה האדום בדף המשימות מזדהם בפריטים שכבר טופלו, ומיכאל מפסיק להאמין לו — וזו הפגיעה האמיתית, כי זה המונה שאמור להגיד לו מה דחוף באמת.

**מיקום התיקון**: `svItp` (index.html:10805-10813) — פונקציית `_itpAdvance(rec)` שמחשבת `next_due` חדש מהמחזור; `rItp` (10774) — להוסיף כפתור «✓ בוצע» לצד ה-✎; `_collectVirtualTasks` (8953-8960) ו-`rTasks` (9613-9617) — אם רוצים שהסימון יהיה זמין גם משורת המשימה הווירטואלית.

**הערה**: ממצא נלווה שמצאתי תוך כדי ושווה לתקן באותה נגיעה: `_eqiCycleResolve` (7551-7558) מחפש מחזור לציוד לפי שם ב-`inspection_types` — `if(hit.cycle_months)return parseInt(hit.cycle_months,10)||null; if(hit.frequency_months)return parseInt(hit.frequency_months,10)||null;` — אבל `svItp` לא כותב אף אחד משני השדות האלה (הוא כותב `recur_count`/`recur_unit`), ולכן הענף הזה מת: מיכאל יכול להגדיר «מלגזה — כל 6 חודשים» ומסלול ה-PDF לא ימצא את זה לעולם ויפול לברירת המחדל 12 חודשים ב-`_eqiResolveExpiry` (7524). תיקון נקי: `_itpCycleMonths(rec)` אחת שממירה recur_count+recur_unit לחודשים, ושתי הנקודות (קידום next_due ו-_eqiCycleResolve) יקראו לה. חובה להשתמש ב-`du()` הקיים ולא לחשב ימים ידנית.


### 3.7 — 🟨 partly · מאמץ M

**ראיה**: «ערך בודד» — מאומת: `leg-c` הוא `<select>` עם ארבע אופציות (index.html:2811 — מציית / חלקית / אינו מציית / לא רלוונטי), ו-`svLeg` שורה 4606 כותב `c:gv('leg-c')||'מציית'`. «בלי היסטוריה» — מאומת: `svLeg` שורות 4617-4619 — `if(id){DB.leg=DB.leg.map(function(x){return x.id===id?r:x;});sbUpd('leg',r);` — החלפה מלאה של האובייקט, ואין שום טבלת `leg_*` היסטורית ב-migrations. «בלי ראיה» — מאומת: המודאל (2800-2818) לא מכיל אזור צירוף כלשהו, ואין `file_url` ל-`leg` באף migration (`2026-04-25_leg_full_content.sql:18-24` מוסיף topic/law_type/law_num/src_url/summary/full_text/last_review בלבד); `src_url` מפנה לנוסח החוק בנבו, לא לראיית עמידה. **אבל** «בלי תאריך» ו«בלי מי קבע» מגזימים: `last_review DATE` קיים בפועל — שדה במודאל (2813 `<input type="date" id="leg-last-review">`), נכתב ב-4615 (`last_review:gv('leg-last-review')||null`), ומזין מנגנון סקירה שלם — `_legReviewMeta` (13745-13757) עם `var _LEG_REVIEW_CYCLE_DAYS=365;` ו-`_legReviewBadge` (13758-13761), עמודת «סקירה» ב-`rLeg` (13800) ופילטר `legReviewFilter` (13805). וגם `o` (אחראי) קיים — 2814 + 4607. מה שנכון הוא שאין שום קשר בין ה-שלושה: שינוי `c` ב-4606 לא נוגע ב-`last_review` ולא רושם מי שינה — כך שאפשר להפוך «אינו מציית» ל«מציית» ותאריך הסקירה יישאר של לפני שנה.

**השפעה**: מיכאל מסמן חוק כ«מציית» — ומול מבקר ISO §9.1.2 הוא לא יכול להראות על סמך מה, איזו ראיה נבדקה, ומי קבע. הבאדג' «✅ בעוד 340 ימים» שהעמוד מציג לצידו נראה כאילו הוא מגבה את הקביעה, אבל הוא מגיע משדה נפרד שמיכאל הקליד ידנית ואף אחד לא מסנכרן — אפשר להקליד «מציית» היום ולהשאיר את הסקירה על 2025. וכשהחוק ייבדק שוב בשנה הבאה, הקביעה של השנה הזאת פשוט תיעלם, ואין איך להוכיח שהייתה הערכת ציות קודמת בכלל.

**מיקום התיקון**: מודאל `m-leg` (index.html:2800-2818) — שדות «נקבע בתאריך», «נקבע על ידי», «ראיה» (קובץ); `svLeg` (4598-4628, בעיקר 4606 ו-4615-4619); טבלה חדשה `leg_compliance` (או עמודות `c_date` / `c_by` / `c_evidence_url` ב-`leg`) ב-migration; `VIEW_CONFIG.leg` (11232-11245) להצגת הרשומה; `rLeg` (13800).

**הערה**: מסומן בבקלוג ⬜👤 — צריך החלטה של מיכאל לפני קוד: האם הערכת ציות היא שורה בטבלה נפרדת (כמו `ncr_ai` עם `version`, DECISIONS.md:1383-1390, שם נדחתה במפורש עמודת jsonb יחידה כי «אין היסטוריה, קשה ל-query») או ארבע עמודות נוספות ב-`leg` בלי היסטוריה. באג נלווה שמצאתי ושווה לתקן באותו PR, כי הוא נוגע באותה פונקציה: `svLeg` שורה 4603 כותב `d:gv('leg-d')||null`, אבל **אין אלמנט `leg-d` ב-DOM** (בדקתי — `id="leg-d"` לא קיים בקובץ), ו-`gv` מחזיר `''` לאלמנט חסר (2940: `function gv(id){var el=g(id);return el?el.value.trim():'';}`). כלומר כל עריכה של חוק מאפסת את `d` — «תיאור ישן» — שאותו `VIEW_CONFIG.leg` (11245) מדפיס למסך ול-PDF. כלל 4 של CLAUDE.md: אסור להוסיף טופס HTML נפרד, הכל ב-index.html; ולפני שינוי schema ל-`leg` — גיבוי, יש תקדים `backup_ops_20260918`.


### 3.8 — ✅ בוצע ב-**#654** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: שלושת החלקים נפתחו ואומתו. (1) **תמיד רשומה חדשה** — `svDoc` ב-`index.html:4199-4205`, שורת ההכרעה 4201: `var r={id:gid(),n:n,c:gv('d-c'),v:gv('d-v')||'1.0',o:gv('d-o'),u:gv('d-u'),e:gv('d-e')||null,s:gv('d-s'),i:gv('d-i')||null,nt:gv('d-nt')};` — `gid()` בלי `_svEditId('docs')||`, בניגוד לכל שאר הפונקציות (למשל `svPpe:4531` `id:_svEditId('ppe')||gid()`). (2) **אין ✎** — `_EDIT_MODS` (שורה 4024) מכיל 14 טבלאות: rsk, tr, ppe, emp, ctr, ins, drl, wst, hzm, env, toolbox, ptw, near_miss, auds. `docs` **לא** ביניהן, ולכן ב-`showView` שורה 11292: `edBtn.style.display=_editableTbl(tbl)?'':'none';` — כפתור העריכה מוסתר בדף הצפייה של מסמך. `_svEditing['docs']` לא נקבע באף מקום (ההשמה היחידה היא `_svEditing[tbl]=id` ב-`_genEdit:4063`). (3) **אין אזור צירוף** — המודאל `m-doc` (שורות 1963-1978) מכיל רק d-n, d-c, d-v, d-o, d-u, d-e, d-s, d-i, d-nt; אין `<div id="doc-attach-area">` ואין קריאה ל-`_attachPick`. `rDocs:9664` מייצר רק 👁 ו-🗑. אין עמודת `file_url` ל-`docs` באף מיגרציה (`grep file_url migrations/` → inc, tr, env_aspects, equip_inspections, toolbox, page_files בלבד).

**השפעה**: טעות הקלדה בשם מסמך או תאריך תוקף שהשתנה = אין תיקון, רק מחיקה והקלדה מחדש (ואז ה-id מתחלף, וכל הפניה/משימה אליו נשברת). ומסמך ה-ISO עצמו — הנוהל החתום, תעודת התקן, הרישיון — לא נשמר באפליקציה בכלל: הרשומה אומרת «ISO 45001:2018 · בתוקף עד 12/2026» ואין לה קובץ. מבקר שמבקש לראות את הנוהל מקבל שורה בטבלה. חמור במיוחד כי יש במודאל כפתור «🔎 קרא מסמך (OCR)» (`_docOcrCert:12450`) — מיכאל מצלם את המסמך, ה-AI ממלא שדות, **והתמונה נזרקת** (`_ocrAnalyze(f,prompt)` רק קורא, לא מעלה).

**מיקום התיקון**: index.html:4024 (`_EDIT_MODS` — להוסיף רשומת `docs` עם `m:'m-doc'` ומיפוי d-n→n, d-c→c, d-v→v, d-o→o, d-u→u, d-e→e, d-s→s, d-i→i, d-nt→nt); index.html:4201 (`svDoc` — `id:_svEditId('docs')||gid()`); index.html:1976 (מודאל m-doc — `<div id="doc-attach-area">` + `_attachPick`); index.html:4201 + 4203 (`file_url:_attachUrls['doc-attach-area']||null` + `_attachClear`); index.html:11193 (`VIEW_CONFIG.docs` — להוסיף `photo:'file_url'`).

**הערה**: שתי אזהרות מהקוד עצמו. (א) **חלק הצירוף דורש מיגרציה** — `ALTER TABLE docs ADD COLUMN IF NOT EXISTS file_url text` — כלומר פריט 👤 שמיכאל צריך להריץ; חלק ה-✎ הוא קוד טהור וניתן לעשות אותו לבד. (ב) **אל תחזור על באג 3.1.** `svTr:4461` כותב `file_url:_attachUrls['tr-attach-area']||null` **תמיד**, ו-`_genEdit:4044-4065` ממלא רק את `cfg.f` — הוא לא מחזיר את הקובץ לאזור הצירוף, ולכן `_svPut` (4034, `Object.assign({},arr[k],r)`) דורס את התעודה ב-null. פריט 3.1 מסומן ✅ ב-BACKLOG, אבל ✅ שם אומר «אומת בקוד», לא «תוקן» — הבאג עדיין שם. אם מוסיפים ל-docs גם ✎ וגם צירוף באותו PR, חובה למלא את `_attachUrls['doc-attach-area']` ב-`_genEdit` (התבנית הנכונה קיימת ב-`editEasp`, שורות 10160-10161). שימוש חוזר: `printReport:13374` כבר מדפיס «קובץ מצורף» לכל רשומה שיש לה `file_url` — אין מה להוסיף שם.


### 3.9 — ✅ בוצע ב-**#654** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: **צמ"ג**: המודאל `m-ppe` (שורות 2264-2276) — שישה שדות בלבד (ppe-ty, ppe-w, ppe-m, ppe-d, ppe-e, ppe-c) ושורת כפתורים 2275: `<button class="btn btn-s" onclick="closeModal('m-ppe')">ביטול</button><button class="btn btn-p" onclick="svPpe()">שמור</button>` — אין `_attachPick`, אין OCR. `svPpe:4531`: `var r={id:_svEditId('ppe')||gid(),ty:ty,w:gv('ppe-w'),m:gv('ppe-m'),d:gv('ppe-d')||null,e:gv('ppe-e')||null,c:gv('ppe-c')};` — אין `file_url`. **קבלנים**: המודאל `m-ctr` (2730-2743), שבעה שדות, אותה שורת כפתורים (2742). `svCtr:4558`: `var r={id:_svEditId('ctr')||gid(),n:n,ty:gv('ctr-ty'),c:gv('ctr-c'),ph:gv('ctr-ph'),e:gv('ctr-e')||null,tr:gv('ctr-tr'),s:gv('ctr-s')};` — אין `file_url`. `VIEW_CONFIG.ppe` (11216) ו-`VIEW_CONFIG.ctr` (11228) הם ללא מפתח `photo:` (להשוואה: `toolbox` ב-11254 יש `photo:'file_url'`). גם אין נתיב עקיף: `_pageFileRender` (הבאנר «קובץ רשמי לכל הטופס») נקרא בדיוק פעם אחת בקובץ — שורה 10100, `_pageFileRender('env_aspects','easp-pagefile')`. אין עמודת `file_url` ל-`ppe`/`ctr` באף מיגרציה.

**השפעה**: ל-`ctr` יש שדה «תוקף הסכם» ושדה «הדרכות בטיחות: הוכשר» — ואי אפשר לצרף את אישור הביטוח, את ההסכם החתום או את תעודת ההדרכה שעליהם הסטטוס הזה מסתמך. כשמבקר 45001 שואל «תראה לי שהקבלן הזה מבוטח ומודרך», התשובה היא dropdown שמישהו בחר בו ידנית. ב-`ppe` אותו דבר לתעודת בדיקה תקופתית של רתמה/מתלה — פריטי צמ"ג שהתקן דורש להם תסקיר. שני אלה מהמרשמים שדף התפוגות סורק (`_expCollect:5990-5991`), כלומר הם כן מתריעים בזמן — רק בלי הראיה.

**מיקום התיקון**: index.html:2274 ו-2741 (הוספת `<div id="ppe-attach-area">` / `<div id="ctr-attach-area">` עם `_attachPick`); index.html:4530-4533 (`svPpe` — `file_url:_attachUrls['ppe-attach-area']||null` + `_attachClear` אחרי השמירה); index.html:4557-4560 (`svCtr` — אותו דבר); index.html:11216 ו-11228 (`VIEW_CONFIG.ppe`/`.ctr` — `photo:'file_url'`); index.html:4051-4055 (`_genEdit` — להחזיר את הקובץ בעריכה).

**הערה**: **דורש מיגרציה** לשתי טבלאות: `ALTER TABLE ppe ADD COLUMN IF NOT EXISTS file_url text;` ו-`ALTER TABLE ctr ADD COLUMN IF NOT EXISTS file_url text;` — פריט 👤 שמיכאל מריץ ומאמת (כלל 7). תלוי ב-3.8: זו בדיוק אותה עבודה, ועדיף PR אחד שמוסיף צירוף ל-docs+ppe+ctr מאשר שלושה. סיכון זהה ל-3.8: `_genEdit` לא משחזר קבצים, ו-`ppe`/`ctr` **כן** נמצאות ב-`_EDIT_MODS`, כלומר יש להן ✎ פעיל — אם מוסיפים `file_url` בלי לתקן את `_genEdit`, מייצרים להן מיידית את באג 3.1 (עריכת תאריך תוקף מוחקת את האישור). לכן התיקון ב-`_genEdit` הוא **תנאי מוקדם**, לא תוספת. עזרים קיימים לשימוש חוזר: `_attachPick`/`_attachUrls`/`_attachRender`/`_attachClear`/`_attachBusy`, והתבנית המלאה כבר כתובה ב-`svEasp` (10135-10146) ו-`editEasp` (10160-10161).


### 3.10 — ✅ בוצע ב-**#654** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: `VIEW_CONFIG.tr` ב-`index.html:11206`, שורת ההכרעה: `['עובד','w'],['הדרכה','n'],['מדריך','c'],` — כלומר `['מדריך','c']`. מול זה, המודאל `m-tr` שורה 2074: `<div class="field"><label>קטגוריה</label><select id="t-c"><option>בטיחות</option><option>ISO</option><option>כיבוי אש</option><option>ציוד מגן</option><option>נהלים</option></select></div>`. `_EDIT_MODS.tr` (4024) ממפה `"t-c":"c"`, ו-`svTr:4461` כותב `c:gv('t-c')`. שלושת נתיבי התצוגה מדפיסים את `f[0]` מילה במילה: `showView:11315` (`...'>'+esc(f[0])+'</span><strong>'...`), `printReport:13370` (`rows+='<tr><th scope="col">'+f[0]+'</th><td>'+...`), ו-`_odBuildReportHtml:18218` (זהה). לכן רשומת הדרכה מציגה ומדפיסה «מדריך: בטיחות». לטבלת `tr` אין בכלל עמודת מדריך — `svTr` כותב w, n, c, d, e, sc, s, file_url וזהו.

**השפעה**: רק בדף הצפייה וב-PDF — טבלת ההדרכות עצמה (`tb-tr`, כותרות בשורה 1517: עובד · הדרכה · תאריך · תוקף · סט.) לא מציגה את `c` כלל. אבל ה-PDF הוא בדיוק מה שנשלח למבקר או נתלה בתיק העובד, ושם כתוב «מדריך: כיבוי אש». מבקר שקורא את זה מבין שהמדריך הוא «כיבוי אש», ושם המדריך האמיתי — שהתקן דורש — לא מופיע בשום מקום במערכת.

**מיקום התיקון**: index.html:11206 — להחליף את `'מדריך'` (מדריך) ב-`'קטגוריה'` (קטגוריה). שינוי של מחרוזת אחת; התיקון חל אוטומטית על showView, printReport ו-_odBuildReportHtml כי כולם קוראים את אותו `f[0]`.

**הערה**: שתי הערות. (א) זו אותה משפחת באגים של פריט 1.2 (`VIEW_CONFIG.ncr` מתייג «סיבה»→p ו«פעולה מתקנת»→o) שכבר אומת ✅ — שווה לתקן את שניהם באותו PR, כי זו אותה שורת קוד ואותה בדיקה. (ב) אם מיכאל באמת רוצה שם מדריך במרשם ההדרכות זה **לא** S: `tr` תצטרך עמודה חדשה (מיגרציה 👤) + שדה במודאל + `_EDIT_MODS.tr` + `svTr` + `VIEW_CONFIG`. עדיף לפצל: קודם לתקן את התווית (מיידי, בלי DB), ואם עולה צורך בשם מדריך — פריט נפרד. כלל 1 של הפרויקט: המחרוזת החדשה חייבת להיכתב כ-`\uXXXX`. אגב, תוך כדי הקריאה נמצא ש-`_trOcrCert:12513` ו-`_docOcrCert:12473` בודקים `if(cEl && !cEl.value && ...)` על `<select>` — ל-select תמיד יש ערך (האופציה הראשונה), ולכן הקטגוריה שה-OCR זיהה **אף פעם לא נכתבת**. לא חלק מהפריט הזה, אבל זה אותו שדה `t-c`.


### 3.11 — 🟨 partly · מאמץ M

**ראיה**: העובדה נכונה, המספר לא. סרקתי את כל 46 הפונקציות `sv*` בקובץ: **אף אחת מהן לא כותבת חותמת משתמש** — לא `created_by`, לא `updated_by`, לא `user_email`. 15 מהן כותבות רק `ts` (svNcr:4312, svPtw, svEqi, svNm, svRound, svToolbox, svTsk, svEasp, svLoc, svItp, svItype, svPrj, svTru), ו-**15 פונקציות שמירה של מרשמים לא כותבות אפילו `ts`**: svDoc(4199), svAud(4206), svInc(4431), svTr(4457), svRsk(4467), svEmp(4489), svPpe(4529), svIns(4535), svDrl(4550), svCtr(4556), svWst(4562), svHzm(4568), svEnv(4574), svLeg(4598), svUser(9040). כלומר «שש» הוא חסר מאוד — אלא אם הכוונה הייתה לשש הפונקציות של האזור הזה בלבד (svDoc/svTr/svPpe/svCtr/svIns/svDrl). החצי השני של הטענה **מדויק**: `_aud:3233` — `var entry={user_email:u,table_name:tbl,record_id:record_id,op:op,title:title};` — נקרא מ-`sbIns`/`sbUpd`/`sbDel` (3235-3237), וזה המקום היחיד שבו «מי ומתי» נשמר.

**השפעה**: כשמיכאל פותח רשומת צמ"ג או קבלן ורואה תאריך תוקף שנראה לו לא נכון, אין בדף הצפייה שום רמז מי הזין אותו ומתי. כדי לברר צריך ללכת לעמוד «יומן ביקורת» (`rAudit:8619`), שמושך `limit=200` שורות אחרונות מכל הטבלאות ביחד (שורה 8623) — כלומר בפועל היסטוריה של יום-יומיים, ובלי חיפוש לפי רשומה. בטופס נייר מודפס לא מופיע כלום. עבור 45001/14001 §7.5.3 זה הפער האמיתי: מסמך מבוקר אמור לשאת את מי עדכן אותו.

**מיקום התיקון**: אם מצמצמים לחמשת מרשמי התפוגה: index.html:4201 (svDoc), 4461 (svTr), 4531 (svPpe), 4558 (svCtr), 6954 (svEqi) — להוסיף `updated_by` + `updated_at`; index.html:11193/11205/11216/11228 (`VIEW_CONFIG` — שורת «עודכן ע"י»); index.html:3273 (`_currentUser`) כמקור השם.

**הערה**: שלוש נקודות שמשנות את התמונה. (א) **יש כבר תקדים עובד בקוד** — `ncr.verified_by`/`verified_at` (שורות 13273-13275: `var u=(typeof _currentUser!=='undefined'&&_currentUser&&_currentUser.username)||'admin'; rec.verified_by=u; rec.verified_at=new Date().toISOString();`) ותצוגה מוכנה ב-`showView:11408-11409`. זו בדיוק התבנית לשימוש חוזר, כולל נוסח ההצגה. (ב) **DECISIONS.md:1247** דחה במפורש «created_by + row-level ownership — דורש migration לכל הטבלאות» — אבל בהקשר של **RLS ובעלות על שורות** (Vuln #7, 2026-04-21), לא בהקשר של חותמת ביקורת. זה לא סותר להוסיף `updated_by` כשדה תצוגה, אבל כדאי להזכיר את ההחלטה ב-PR כדי שלא ייראה כסתירה. (ג) **כל טבלה דורשת מיגרציה** (`ALTER TABLE ... ADD COLUMN updated_by text, updated_at timestamptz`) — 👤, וזו הסיבה ל-M ולא S. ההמלצה: לא לרדוף אחרי כל 15 הפונקציות, אלא לצמצם לחמשת מרשמי התפוגה שהאזור הזה עוסק בהם — משם מגיע ה-PDF שהמבקר רואה.


### 3.12 — ✅ בוצע ב-**#654** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: `rExp` ב-`index.html:6047`, שורת ההכרעה 6080: `if(!rows.length){tb.innerHTML='<tr><td colspan="6" class="et" data-label="">🔧<br>אין בדיקות ציוד<br><button class="et-cta" onclick="openModal(\'m-eqi\')">+ בדיקה ראשונה</button></td></tr>';return;}` — 🔧 «אין בדיקות ציוד» + «+ בדיקה ראשונה» שפותח את `m-eqi`. השורה יושבת **אחרי** הסינון לפי לשונית (6062-6072, `var f=window._expFilter||'all'`) ואחרי המיון, כלומר היא רצה על התוצאה של כל אחת מששת הלשוניות שמגדיר `expFilter:6013-6020`: all · exp («פג») · 7 · 30 · 90 · none («ללא תאריך»). והעמוד באמת מאגד חמישה מרשמים — `_expCollect:5988-5992` אוסף מ-`DB.docs` (מסמך), `DB.tr` (הדרכה), `DB.ppe` (צמג), `DB.ctr` (קבלן) ו-`DB.equip_inspections` (בדיקת ציוד); `_expNoDate:6006-6010` אותם חמישה. המיפוי בשורה 6084 מאשר: `var _tbl={docs:'docs',tr:'tr',ppe:'ppe',ctr:'ctr',eqi:'equip_inspections'};`.

**השפעה**: זה המצב הנפוץ, לא הקצה: מיכאל לוחץ על «פג» כדי לוודא ששום דבר לא פג — המצב הטוב — והמסך אומר לו «אין בדיקות ציוד» עם כפתור שפותח טופס בדיקת ציוד חדשה. ההודעה הנכונה הייתה «אין תפוגות שפגו — הכל בתוקף ✓». במקום זה הוא מקבל אזהרה שנשמעת כאילו מרשם הציוד ריק, ולוחץ על כפתור שמתחיל להזין ציוד שאין בו צורך. אותו דבר בלשונית «ללא תאריך» כשהכל מתוארך, ובלשונית «90 יום» בחודש שקט.

**מיקום התיקון**: index.html:6080 (`rExp`) — להחליף את ההודעה היחידה במפת הודעות לפי `f` (`window._expFilter`): all → «אין רשומות עם תאריך תוקף בחמשת המרשמים» + CTA לדף הרלוונטי; exp → «אין תפוגות שפגו — הכל בתוקף ✓» בלי CTA; 7/30/90 → «אין תפוגות ב-X הימים הקרובים ✓»; none → «לכל הרשומות יש תאריך תוקף ✓».

**הערה**: שני דברים. (א) **הכפתור שגוי פעמיים** — גם כשהמצב באמת ריק, `openModal('m-eqi')` מפנה למרשם אחד מתוך חמישה; אין סיבה שדף מאגד יציע דווקא בדיקת ציוד. עדיף `goPage` למרשם, או בכלל בלי CTA בלשוניות «פג»/«7»/«30»/«90», כי שם ריק = חדשות טובות. (ב) לא לגעת ב-`_capNote:6036` וב-`_capRows:6031`: הם רצים רק כשיש שורות (6086-6092), ומונים מ-`rows.length` המלא — התקרה של 200 שורות (#625/#617) לא נוגעת למצב הריק, והמצב הריק לא צריך לדעת עליה. (ג) אין החלטה ב-DECISIONS.md שמצדיקה את ההודעה הנוכחית — היא פשוט הודעת המצב-הריק של דף הציוד שהועתקה לכאן.


### 4.3 — ✅ בוצע ב-**#650** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: index.html:15138-15142, ענף `}else if(emp){` בתוך `proceed()` ב-IIFE של ה-boot. שלוש השורות במלואן: `document.body.classList.add('emp-mode');` / `ldb();CUR='emp-home';` / `SB_ON=true;_obDrain();_rtStart();` — אין קריאה ל-`_truEmpHome`. להשוואה, ענף האדמין שורה למעלה (15137): `ldb();if(empIsOn()){CUR='emp-home';if(typeof _truEmpHome==='function')_truEmpHome(true);}else rDash();sbSync();_rtStart();`. הקישור שמיכאל משתף הוא `?emp=1` (`_truLinkUrl`, 15007), וה-IIFE ב-15115-15122 שומר `EMP_KEY='1'` ומוסיף `emp-mode` לפני שה-boot רץ — לכן **כל** פתיחה של הקישור, גם הראשונה ולא רק «חזרה», נופלת בדיוק לענף הזה. בלי `_truEmpHome` המחלקה `tru-only` לא נוספת, ו-CSS 601 `#emp-trustee{display:none}` משאיר את מסך הנאמן מוסתר, בעוד CSS 580 `body.emp-mode #pg-emp-home{display:block!important}` מציג את בית העובד הישן. שלוש דרכי הכניסה האחרות כן קוראות: `_finishLogin` 14124, `empToggle` 15049, `doEmpLogin` 15074. ההערה מעל `_truEmpHome` (14227-14228) אומרת מפורשות "Runs on every way into employee mode (login button, toggle, reload with the mode remembered)" — כלומר הענף הזה נשכח, לא הוחרג.

**השפעה**: הנאמן פותח את הקישור מוואטסאפ ורואה את מסך העובד עם 4 אריחי הדיווח, לא את מסך הנאמן — בניגוד להחלטה המפורשת בקוד (14223-14224 + CSS 604-606: «בית העובד הוא מסך הנאמן בלבד»). צריך הקשה נוספת על «🦺 נאמן בטיחות» (796). בנוסף `_truPull` לא רץ בכניסה, אז הניקוד והסטטוס שיוצגו אחרי ההקשה מגיעים מהמטמון עד רענון ידני.

**מיקום התיקון**: index.html:15141 — להוסיף `if(typeof _truEmpHome==='function')_truEmpHome(true);` אחרי `SB_ON=true;_obDrain();_rtStart();` בענף `else if(emp)`

**הערה**: לשים את הקריאה **אחרי** `_obDrain()` כדי שה-outbox ינוקז לפני המשיכה מהשרת. אין צורך להוסיף `_sbAuth()` — `_truPull`→`sbGet` (3023, שורה `return _sbAuth().then(...)`) מטפל באימות בעצמו. תלוי-שכן בפריט 4.10: `_truPull` (14274) מושך רק `trustee_reports`,`trustees`,`trustee_winners` — לא `trustee_tasks`, אז קטלוג המשימות בקיוסק עדיין יגיע מברירת המחדל בקוד.


### 4.4 — ✅ בוצע ב-**#652** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: `_tskQuickStatus` (8692-8701) — כל מה שהיא כותבת: `DB.tasks[idx].status=newStatus;` ... `sbUpd('tasks',DB.tasks[idx]);sdb();toast(...);rTasks();rDash();`. אין נגיעה ב-`source_table`/`source_id`. אותו דבר ב-`_tskBulkAction` (8674-8690) וב-`svTsk` (8917-8930). חיפוש `source_table` על כל הקובץ מחזיר קריאות בלבד (8999, 9614, 14616) ואף כתיבה חזרה לרשומת המקור. המשימה אכן נקשרת: `_truMgrSpawnTask` (14790) קורא `openTskModal('trustee_reports',id)`, ו-`_truTaskFor` (14616) `return (DB.tasks||[]).find(function(x){return x.source_table==='trustee_reports'&&x.source_id===id;});` מוצא אותה. אבל `trustee_reports.s` משתנה רק ב-`_truMgrSetStatus` (14763) — שנקראת ידנית מתפריט השורה (14754: `item('✅','סמן נסגר (תוקן בשטח)',...)`) — או ע"י טריגר ה-DB כשנאמן שולח דיווח t=8. סגירת המשימה אינה אף אחד מהשניים.

**השפעה**: המנהל מנתב ליקוי למשימה, המחלקה מתקנת, המשימה נסגרת — ובדף הנאמנים הליקוי עדיין אדום «פתוח». צ'יפ הגיל (`_truAgeChip`, 14682) ממשיך לרוץ, ואחרי 30 יום הליקוי נכנס להתראת «דווח, נותב, עדיין לא תוקן» במסך הבית (`_truStale`, 4918-4925) עם תג אדום — על משהו שכבר תוקן. והנאמן לא מקבל את 2 הנקודות, כי `_truScore` שורה 14182 סופרת `_truIsHazard(r)&&r.s===TRUSTEE_S_CLOSED`. זו סיבה ישירה לכך שהניקוד החודשי נמוך ממה שקרה בשטח.

**מיקום התיקון**: index.html:8692-8701 (`_tskQuickStatus`), 8674-8690 (`_tskBulkAction`), 8917-8930 (`svTsk`) — הוק משותף אחד כשהסטטוס החדש הוא `_TSK_DONE` ו-`source_table==='trustee_reports'`

**הערה**: הטענה מעריכה M; בפועל S. **לא** לסגור אוטומטית בשקט: DECISIONS.md 2026-09-18 §3 קובע שסגירה = דיווח t=8 עם תמונת «אחרי», ומשימה שנסגרה אינה הוכחה שמישהו צילם. הכיוון הבטוח — לשאול את המנהל «לסמן גם את הממצא נסגר?» ולקרוא ל-`_truMgrSetStatus(id,TRUSTEE_S_CLOSED)` הקיים (הוא כבר עושה `sbUpd`+`addLog`+`rTrustees`+`rDash`). RLS בסדר: UPDATE על `trustee_reports` מותר ל-admin/manager, והמנהל הוא זה שסוגר משימות. שווה גם להציג את סטטוס המשימה על שורת הממצא — כרגע `tskHtml` (14738) מציג רק את הכותרת כקישור, בלי סטטוס. שים לב: +2 ייזקפו לחודש שבו **דווח** הליקוי (14179), לא לחודש הסגירה.


### 4.5 — 🟨 partly · מאמץ M

**ראיה**: העובדות נכונות. `svTru` 14566 `function row(...)` קובע `s:ok?TRUSTEE_S_OK:TRUSTEE_S_OPEN`; בסגירה נדחפת שורת t=8 עם `ref`, ומיד אחריה 14592: `if(ref)(DB.trustee_reports||[]).forEach(function(x){if(x.id===ref&&_truIsHazard(x)&&x.s!==TRUSTEE_S_CLOSED)x.s=TRUSTEE_S_CLOSED;});` — והטריגר בשרת (`migrations/2026-09-18_trustee_reports.sql`, AFTER INSERT → `SET s='נסגר'`) עושה את אותו הדבר. הניקוד: 14182 `closed=...r.s===TRUSTEE_S_CLOSED` ו-14185 `ptsClosed=Math.min(closed*TRUSTEE_PTS_CLOSED,TRUSTEE_MAX_CLOSED_PTS)`. בסכמה (אותה מיגרציה, שורות 40-55) אין שום שדה אישור: id,u,m,t,d,location_id,loc,ok,f,photo_url,s,ref,mgr_note,ts.
מה שהטענה מחמיצה: (א) DECISIONS.md 2026-09-18 §3 — זו החלטה מפורשת של מיכאל («הנאמן סוגר משימה שהוא פתח»), במכוון בלי UPDATE ובלי RPC כדי שתעבוד אופליין דרך ה-outbox, ושם כתוב במפורש «המנהל יכול לפתוח מחדש». (ב) הכפתור הזה קיים: `_truRowMenu` 14755 `item('↩','פתח מחדש',...)`. (ג) המנהל רואה את הראיה על השורה — `afterHtml` 14733 מציג «✓ תמונת "אחרי" DD/MM/YYYY (שם)» עם קישור לתמונה, ויש פילטר «נסגרו» (14727). (ד) תמונת «אחרי» היא חובה: `svTru` 14574 `if(!okPh){toast('תמונת "אחרי" היא חובה כדי לסגור ממצא');return;}`. (ה) התקרה 20 נק' (TRUSTEE_MAX_CLOSED_PTS) = 10 סגירות, לא אינסוף.

**השפעה**: נאמן יכול לסמן ליקוי כמתוקן ולקבל +2 בלי שאיש בדק, והמנהל יגלה רק אם ייכנס לפילטר «נסגרו». מה שבאמת חסר הוא לא «תור» אלא **סימון «סגירה שטרם נבדקה»** — אין דרך להבדיל בין ממצא שהמנהל אישר לבין אחד שנסגר ע"י הנאמן ואיש לא הסתכל עליו. הרשימה, התמונה וכפתור הפתיחה-מחדש כבר קיימים.

**מיקום התיקון**: מיגרציה חדשה (עמודות `verified_by`/`verified_at` ל-`trustee_reports`), index.html:14566+14592 (`svTru`), 14182-14185 (`_truScore`), 14710-14745 (`rTrustees` — פילטר רביעי «נסגרו, טרם נבדקו»), 14752-14762 (`_truRowMenu` — «אשר סגירה»)

**הערה**: לפני שנוגעים במנגנון הזה: `migrations/2026-09-20_trustee_close_ownership.sql` **טרם הורצה** (כתוב בראש הקובץ ובפריט 5.10 בבקלוג). בלעדיה כל מבקר בקישור הציבורי יכול לסגור כל ממצא בלולאה — זה פער חמור יותר באותו מנגנון בדיוק, וצריך לרוץ קודם. עוד נקודה לפני שינוי ניקוד: +2 נזקפים לחודש שבו **דווח** הליקוי (`_truScore` 14179 מסנן `_truRows(m)` לפי `r.u===u`), כך שסגירה ב-3 באוקטובר משנה למפרע את לוח ספטמבר — גם אחרי שהוכרז זוכה. פעולה ידנית של מיכאל נדרשת (מיגרציה), ולכן זה לא פריט שנסגר בתוך ריצה אחת.


### 4.6 — ✅ בוצע ב-**#652** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: `_truRosterRender` (14845-14854) מייצר לכל נאמן בדיוק שני כפתורים — `_truRosterToggle(...)` («השבת»/«הפעל») ו-`askDel('trustees',this.dataset.rid)`. אין ✎, אין מיזוג. המודאל `m-tru-roster` (2404-2415) מכיל רק `tru-roster-n` + `tru-roster-dep` שמזינים את `_truRosterAdd` (14855). ו-`trustee_reports` **לא קיים** ב-`VIEW_CONFIG` (11191) ולא ב-`_EDIT_MODS` (4024) — כלומר אין בשום מקום באפליקציה מסך שעורך את `u` של דיווח שכבר נשמר. למה זה נדבק לנצח: הנאמן מקליד שם חופשי דרך «אחר (הקלד שם)» (`_truNameVal` 14248 מחזיר את `tru-u-other`), הוא נשמר כ-`u` בכל שורה (`row()` 14566), ואז — `_truNameOptionsHtml` 14243: `(DB.trustee_reports||[]).forEach(function(r){if(r&&r.u&&!seen[r.u]){seen[r.u]=1;h+='<option ...>'}})` מוסיף את הטעות כאופציה קבועה בבורר של **כל** המכשירים, ו-`_truBoard` 14209: `_truRows(m).forEach(function(r){if(r.u)names[r.u]=1;});` פותח לה שורה משלה בלוח הניקוד. מחיקת השורה מה-roster לא עוזרת — השם מגיע מהדיווח, לא מה-roster. `_truRosterAdd` 14857 חוסם רק שם זהה בדיוק (`some(t.n===n)`), לא וריאציה.

**השפעה**: «משה כהן» מול «משה כהן » (רווח) או «מ. כהן» = שני מתחרים נפרדים. הניקוד מתפצל, אף אחד מהשניים לא מגיע ל-5 משימות ולכן שניהם מסומנים «לא זכאי לפרס», ודף הנאמנים וה-CSV שיוצא לוועדת הבטיחות (`_truExportCsv` 14812) מציגים נאמן שלא קיים. גם `_truBehind` (4947-4957, תזכורת במסך הבית) ו-`_truCoverage` (14648) מצליבים לפי שם, אז האזור של אותו נאמן ייראה כאילו לא סויר. וגרוע מזה — הטעות מופיעה בבורר, אז נאמן אחר יבחר אותה בטעות.

**מיקום התיקון**: index.html:14845-14854 (`_truRosterRender` — כפתור ✎ + עריכה inline בסגנון `_truMgrNote` 14771), ופונקציית שמירה חדשה ליד 14864 שעושה `sbUpd('trustees',t)` **וגם** rename על `DB.trustee_reports`: `forEach(r=>{if(r.u===old){r.u=nw;sbUpd('trustee_reports',r);}})`

**הערה**: שינוי שם ב-roster בלבד לא פותר כלום — חייבים לעדכן גם את שורות `trustee_reports`, כי משם מגיע הרפאים. `trustee_reports` נמצא ב-`_DEL_PROTECTED` (3599) ולפי CLAUDE.md אסור למחוק שורות היסטוריות, אז «מיזוג» = UPDATE של `u`, לעולם לא מחיקה+יצירה. ה-UPDATE עובר שורה-שורה דרך ה-outbox — אצל נאמנים מדובר בעשרות שורות, סביר. השלמה זולה שכדאי לעשות באותו PR: ב-`_truRosterAdd`/בבורר, לחסום «אחר» או להזהיר כשהשם החדש נבדל מקיים רק ברווח/ניקוד.


### 4.7 — ✅ בוצע ב-**#652** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: `_truRender` (14307) — שורה 14312 היא המקור היחיד לרשימת הממצאים במסך הנאמן: `var mine=_truRows(m).filter(function(r){return r.u===me;}).sort(...)`, והכותרת ב-14348 היא «📝 הדיווחים שלי החודש (n)». `m` הוא `_truThisMonth()` (14309) ו-`_truRows(m)` (14174) מסנן לחודש אחד. כל מה שהמסך מציג על אחרים הוא שמות וסכומי נקודות בלבד — `_truBoard` ב-14340-14345 (מדליה, שם, `x.total`) — בלי ממצא, בלי סטטוס ובלי אזור. אין בקובץ שום בלוק אחר שמרנדר ל-`tru-body` ממצאים של מישהו אחר.

**השפעה**: שני נאמנים שחולקים אזור — מקרה שהקוד עצמו מכיר (`_truCoverage` 14648: «Two trustees can share an area») — לא רואים אחד את הליקויים של השני; נאמן שחוזר ממחלה לא יודע מה פתוח אצלו באזור. וזה חותך בדיוק את התפקיד שהמסך מגדיר לו בעצמו (טקסט ההסבר, שורה 818: «לאתר, לדווח, ולבדוק שתוקן»): משימה 8, «מעקב סגירה», אפשרית רק על מה שהוא עצמו דיווח החודש.

**מיקום התיקון**: index.html:_truRender — בלוק חדש אחרי 14356 (לפני `tru-danger`), על בסיס `_truAreaOf(me)` (14642) ו-`(DB.trustee_reports||[]).filter(r=>_truIsHazard(r)&&r.s!==TRUSTEE_S_CLOSED&&String(r.loc||'').indexOf(area)===0)`

**הערה**: (1) חופף חלקית לפריט 4.1 שכבר אומת — 4.1 מחזיר לנאמן את הממצאים שלו מחודשים קודמים, 4.7 מוסיף את של האחרים באזור. שווה לתקן כבלוק אחד «פתוחים באזור שלי», בלי חלוקה לחודשים. (2) הנתונים כבר על המכשיר: policy `emp_select USING (true)` (migrations/2026-09-18_trustee_reports.sql) ו-`_truPull` (14274) מושך את כל `trustee_reports`, אז זה UI בלבד — אין מיגרציה ואין שינוי RLS. (3) **אסור** להצמיד «📷 צלם אחרי» לממצא של נאמן אחר: `migrations/2026-09-20_trustee_close_ownership.sql` מוסיף `AND u IS NOT DISTINCT FROM NEW.u`, כך שאחרי הרצתה ה-INSERT יצליח אבל הסטטוס לא ישתנה — בעוד ש-`svTru` 14592 כבר מסמן «נסגר» מקומית, כלומר המסך ישקר עד ה-pull הבא. להציג read-only. (4) ההצלבה מול האזור חייבת `indexOf` ולא `===`, כי `row()` 14566 כותב `loc:loc+(detail?' · '+detail:'')`.


### 4.8 — ✅ בוצע ב-**#652** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: index.html:14606-14608 — `var _truMgrM=null;` then `function _truMgrMonth(){return _truMgrM||_truThisMonth();}`, and 14170 `function _truThisMonth(){return new Date().toISOString().substring(0,7);}`. So the trustees page opens on the calendar month, always. In rTrustees (14683-14703) the winner strip reads only that month: 14699 `var win=_truWinnerOf(m),winHtml='';` — with no winner and nobody eligible yet it falls to 14707 `winHtml='<div ...>אין עדיין זכאים לפרס (≥'+_truMinTasks()+' משימות)</div>'`. I then searched the whole file for any nudge: `_truWinnerOf`/`_truLatestWinner`/`_truWinnersDesc` appear only at 14191-14193 (definitions), 14330 (the trustee's gold banner), 14699 (this strip), 14968/14995 (the announce modal). `_renderToday` (4906-5005) pushes exactly three trustee items — 4917 stale hazards, 4941 awaiting routing, 4947 behind-on-tasks — and none concerns a winner. Grep for previous-month logic (`getMonth()-1`, prevMonth, 'חודש שעבר') hits only 5016 and 12835, both unrelated (NCR/AI windows).

**השפעה**: ב-1 לחודש הדף נפתח על החודש החדש והריק, ולא כתוב בשום מקום שלחודש שהסתיים עדיין אין זוכה. הפרס מוכרז רק אם מיכאל זוכר ללחוץ ‹ אחורה חודש. מהצד של הנאמנים אין תלונה שתזכיר לו — `_truLatestWinner()` ב-14330 מציג את הזוכה **האחרון שהוכרז** בלי קשר לחודש, אז הבאנר הזהוב של אוגוסט ממשיך להופיע בספטמבר וכולם מניחים שהכל תקין. חודש שלם של עבודה של הנאמנים נשאר בלי פרס בשקט.

**מיקום התיקון**: index.html:4941-4960 (להוסיף פריט ל-`_renderToday`, באותו דפוס של 1b/1c — `_role()!=='reporter'`, מוסתר כשאין מה להכריז), ובתוך `_todayClick` 4908-4915 + `_truMgrMonth`/`_truMgrShift` 14606-14612 כדי לפתוח את הדף על החודש הנכון.

**הערה**: תלוי ב-4.11 ופותר איתו: שניהם צריכים דרך אחת לפתוח את `pg-trustees` על חודש מסוים — helper אחד (`_truGoMonth(m)` שמציב `_truMgrM` ואז `goPage('trustees')`) משרת את שניהם, אז כדאי לתקן אותם יחד. אין החלטה ב-DECISIONS.md שסותרת: השורות 92-95 עוסקות רק בהקפאת `pts` ובספירת זכיות מחודשים קודמים, לא בתזכורת. סיכון בתיקון: אל תחשב «אין זוכה» לפני שהחודש נגמר — התנאי חייב להיות «החודש הקודם הסתיים ו-`_truWinnerOf(prev)` ריק ויש בו לפחות זכאי אחד», אחרת השורה תופיע כל חודש מה-1 ותהפוך לרעש.


### 4.9 — ✅ בוצע ב-**#652** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: המספרים האמיתיים באמת עוקבים אחרי הקטלוג — 14165 `function _truMaxTaskPts(){return _truTasksActive().length*TRUSTEE_PTS_TASK;}` ו-14168 `function _truMinTasks(){return Math.min(TRUSTEE_MIN_TASKS,_truTasksActive().length);}`. מולם, קשיח: index.html:818 «10 לכל משימה (עד 80) + 2 לכל מפגע שדיווחתם ונסגר (עד 20). מועמדות לפרס מ-5 משימות.» · 1207 «10 לכל משימה (מקס׳ 80) · 2 לכל מפגע שנסגר (מקס׳ 20) · זכאות לפרס מ-5 משימות» · 815 «8 משימות בחודש» · 796 «8 המשימות החודשיות» · 4952 `var trNames=behind.slice(0,3).map(function(sc){return sc.u+' ('+sc.nTasks+'/8)';})` · 14318 `'<div><div class="n">'+sc.nTasks+'<span ...>/8</span></div>'` · 14317 גם `/100` (התקרה האמיתית היא `_truMaxTaskPts()+TRUSTEE_MAX_CLOSED_PTS`). ב-CSV: 14823 `lines.push(['נאמן','משימות (מתוך 8)',...,'זכאות (≥5)','פתוחים']...)`. בשרת: functions/api/trustee-notify.js:34-38 `const TASKS = {1:'סיור מפגעים באזור', ... 8:'מעקב סגירה'};`, בשימוש ב-112 ו-148 `TASKS[row.t] || ('משימה ' + row.t)`. באותו מסך עצמו כבר משתמשים בעזרים נכון — 14322 `'עוד '+(_truMinTasks()-sc.nTasks)+' משימות'`, 14707 `'(≥'+_truMinTasks()+' משימות)'`, 14709 `x.nTasks+'/'+_truMinTasks()` — כך שהסתירה היא בתוך אותה שורה בדף.

**השפעה**: היום יש בדיוק 8 משימות פעילות, אז המספרים במקרה נכונים ואף אחד לא רואה כלום. ברגע שמיכאל משבית משימה או מוסיף תשיעית — בדיוק מה שטבלת `trustee_tasks` נבנתה בשבילו — אותו מסך יציג שתי אמיתות: «6/8» ליד «עוד 0 משימות לזכאות (≥5)», וה-CSV שהולך לוועדת הבטיחות יכריז «משימות (מתוך 8)» על קטלוג של 9. בוואטסאפ/מייל שיוצאים מהשרת, שם משימה ששונה יגיע בנוסח הישן.

**מיקום התיקון**: index.html:796, 815, 818, 1207 (טקסט סטטי ב-HTML — להפוך ל-span שממולא ב-`_truRender`/`rTrustees`), 4952, 14317, 14318, 14823; functions/api/trustee-notify.js:34-38 + 112 + 148.

**הערה**: אין מה להמציא — העזרים קיימים: `_truMaxTaskPts()` (14165), `_truMinTasks()` (14168), `_truTasksActive()` (14159), `TRUSTEE_MAX_CLOSED_PTS` (14140). הערה אחת מדויקת על ניסוח הממצא: בשרת אין «/8» או «80» או «5» — מה שקשיח שם הוא **מפת שמות המשימות** 1..8, קטלוג במלוא מובן המילה. התיקון בצד הלקוח הוא S טהור; תיקון השרת יקר יותר (קריאת `trustee_tasks` עם ה-service key בכל notify) ואפשר גם פשוט להשאיר את ה-fallback `'משימה '+row.t` ולמחוק את המפה. שים לב ל-STATUS.md:244 — המיגרציה `2026-09-19_trustee_tasks.sql` עדיין `[ ]`, כלומר עד שהיא תרוץ `_truTasks()` מחזיר את `TRUSTEE_TASKS` וכל המספרים הקשיחים נכונים; זה בדיוק למה הממצא בלתי נראה היום. DECISIONS.md:119 מתעד שהקטלוג היה בקוד «עד שיהיה צורך לערוך בלי קוד (שלב 2)» — שלב 2 נכתב, אז הטקסט הקשיח הוא שארית ולא החלטה.


### 4.10 — ✅ בוצע ב-**#650** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: שני חצאים, שניהם בקוד. (א) `_truPull` — index.html:14274-14286, שורה 14276: `var tbls=['trustee_reports','trustees','trustee_winners'],obBefore={};` — אין `trustee_tasks`. זו הדרך היחידה שמצב עובד מושך מהשרת (ההערה ב-14270 אומרת מפורשות «Employee mode never runs sbSync»), ו-`sbSync` — שכן כולל אותה, 3285 — לא רץ שם. (ב) הקיוסק כן מריץ realtime: 15142 `ldb();CUR='emp-home'; SB_ON=true;_obDrain();_rtStart();` ב-ענף `else if(emp)`, ו-`_rtStart` (3340) נרשם לכל 38 הטבלאות כולל `trustee_tasks`. ואז `_rtApply` 3385-3388: `DB[tbl]=DB[tbl].map(function(r){if(r.id===nr.id){found=true;return nr;}return r;}); if(!found)DB[tbl].unshift(nr);` — על מערך ריק `found` נשאר false והשורה היחידה נדחפת פנימה. ו-`_truTasks()` 14152-14156: `var rows=(DB.trustee_tasks||[]).filter(function(t){return t&&t.n;}); if(!rows.length)return TRUSTEE_TASKS; return rows.slice().sort(...)` — שורה אחת היא «לא ריק», אז ה-fallback של 8 המשימות כבר לא נכנס. ה-RLS מאשר שהאירוע באמת מגיע: migrations/2026-09-19_trustee_tasks.sql — `CREATE POLICY trustee_tasks_read ON public.trustee_tasks FOR SELECT TO authenticated USING (true);` + `ALTER PUBLICATION supabase_realtime ADD TABLE public.trustee_tasks`.

**השפעה**: שתי תוצאות. הראשונה כבר היום: מיכאל עורך את הקטלוג, והקיוסק ממשיך להראות את 8 המשימות המובנות בקוד — העריכה פשוט לא מגיעה לנאמנים, בלי שום סימן. השנייה חמורה יותר: אחרי אירוע realtime בודד על `trustee_tasks`, הקיוסק נשאר עם משימה אחת — טופס הדיווח מציג צ׳יפ אחד (14491 `_truTasksActive().map(...)`), כרטיס הניקוד משימה אחת, ו-`_truMinTasks()` הופך ל-`Math.min(5,1)=1` כך שכל נאמן «זכאי לפרס» אחרי דיווח אחד. `sdb()` (3405) כותב את זה ל-localStorage, ו-`ldb()` (2998) טוען את זה בחזרה בפתיחה הבאה — אז ריענון לא מציל, ו-`_truPull` לעולם לא מושך את הטבלה מחדש.

**מיקום התיקון**: index.html:14276 — להוסיף `'trustee_tasks'` למערך `tbls` ב-`_truPull`. שורה אחת.

**הערה**: רדום היום: STATUS.md:244 מראה שהמיגרציה עדיין `[ ]`, ואם הטבלה לא קיימת `sbGet` מחזיר null ו-`_sbMergePull` מדלג (3280 `if(rows===null)return;` — null = לא נגיש, שומרים את המטמון), אז התיקון בטוח לשליחה **לפני** המיגרציה, וזה היתרון: היום שבו המיגרציה תרוץ הוא היום שבו הבאג מתעורר. שווה לשקול גם חגורה שנייה ב-`_rtApply` 3385-3388: דחיפת שורה לטבלה שמעולם לא נמשכה הופכת מטמון חלקי ל«כל הטבלה» — זה נכון לכל טבלה, לא רק לזו, אז זה תיקון נפרד וגדול יותר ולא חייב להיכנס עכשיו. אין ל-4.10 תלות ב-4.9 אבל הם נוגעים באותו אזור — כדאי לתקן באותו PR.


### 4.11 — ✅ בוצע ב-**#650** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: שתי שורות הנאמנים ב«היום» אוספות כל חודש: `_truStale` 14634-14641 רץ על `(DB.trustee_reports||[]).filter(...)` בלי סינון חודש (ההערה ב-14669 אף אומרת «Any month — an old finding still waiting is still waiting»), וכך גם `_truAwaitingRouting` 14671-14673. שתיהן דוחפות ניווט גנרי: 4925 `badge:oldAge+'י',badgeCls:'red',fn:'page',a1:'trustees',a2:''` ו-4943 אותו דבר. `_todayClick` 4909: `if(fn==='page'){goPage(a1);return;}` — `a2` (המזהה) נזרק. `goPage` 3726-3745 לא נוגע ב-`_truMgrM` בכלל, ומסתיים ב-`rPage()` → `rTrustees()`. ושם, 14683-14685: `function rTrustees(){ var m=_truMgrMonth(); ... var rows=_truRows(m);` וטבלת הממצאים 14724 `var list=rows.filter(function(r){ if(_truMgrF==='open')return _truIsHazard(r)&&r.s!==TRUSTEE_S_CLOSED; ...})` — מסוננת לחודש `m` בלבד.

**השפעה**: מיכאל לוחץ על «ליקוי נאמן פתוח מעל 30 יום · הוותיק: מחסן — מטף בלי פלומבה (47 יום)», נוחת בדף נאמני בטיחות על החודש הנוכחי, והממצא מאוגוסט פשוט לא בטבלה. גרוע מזה: הפילטר ברירת המחדל הוא `open` (14607 `var _truMgrF='open'`), אז 14730 מדפיס «אין ממצאים פתוחים ✓». התג בבית אמר שיש ליקוי בן 47 יום, הדף אומר שאין כלום. ליקוי בן חודשיים יכול להישאר פתוח לנצח כי כל ניסיון להגיע אליו נגמר במסך ריק.

**מיקום התיקון**: index.html:4908-4915 (ענף חדש ב-`_todayClick`, למשל `if(fn==='trumonth'){_truMgrM=a1;goPage('trustees');return;}`), שתי נקודות הדחיפה 4920-4925 ו-4943 (להעביר את חודש הממצא הוותיק), ו-`_truMgrMonth`/`_truMgrShift` 14606-14612.

**הערה**: אין רשומת `trustee_reports` ב-`VIEW_CONFIG` (בדקתי 11191-11270 — אפס התאמות), אז `showView('trustee_reports',id)` **אינו** הקיצור הזמין כאן — או שמוסיפים ערך ל-VIEW_CONFIG או שמעבירים חודש. תיקון משותף עם 4.8 (אותו helper). נקודה נוספת שלא בממצא: `_truMgrM` הוא משתנה מודול שנשמר לאורך הסשן (14606), אז אם מיכאל כבר גלש לחודש אחר קודם, השורה תנחית אותו על **החודש ההוא** — לא אפילו על הנוכחי. הזהירות בתיקון: `_truStale()` ממוין לפי גיל, ולכן יכולים להיות בו ממצאים מכמה חודשים שונים — אם השורה מקפיצה לחודש של הוותיק ביותר, שאר הממצאים ברשימה עדיין לא יהיו שם; שווה לשקול פילטר «הכל» או תצוגת כל-החודשים במקום.


### 4.12 — ✅ confirmed · מאמץ M

**ראיה**: שני החצאים נכונים. **שדות מנהל:** `_truRender` 14356 פולט `<button ... onclick="empReport('nm')">⛔ דווח כמעט ונפגע</button>`, ו-`empReport` 15084-15089 הוא `var m={nm:'m-nm',eqi:'m-eqi',round:'m-round',tr:'m-tr'}; var mid=m[type];if(!mid)return; openModal(mid);` — אותו מודאל בדיוק של המנהל, בלי שום התאמה. המודאל `#m-nm` (2365-2382) כולל `nm-sev` (חומרה), `nm-typ` (סוג), **`nm-s` עם `<option>פתוח/בטיפול/סגור`**, `nm-notes` (הערות), ו-`#nm-ai-btn` «🤖 הצע סיווג והמלצות». אין שום שער emp-mode: כל כללי ה-CSS `body.emp-mode` הם 409, 565, 572, 575, 579, 580 — topbar, ניווט ודפים, אף אחד לא נוגע בשדות מודאל. ו-`svNm()` 8069 כותב את זה ישירות: `s:gv('nm-s'),notes:gv('nm-notes'),rep:gv('nm-rep'),sev:gv('nm-sev')`. **אין «מה קרה»:** index.html:607 `#pg-emp-home.tru-only .emp-hero,#pg-emp-home.tru-only .emp-grid,#pg-emp-home.tru-only #emp-recent,#pg-emp-home.tru-only #tru-back-btn{display:none!important}` — פאנל «הדיווחים האחרונים שלי» מוסתר לגמרי בקיוסק (ו-`EMP_HOME_TRUSTEES_ONLY=true` ב-14225). גם אילו היה מוצג, `rEmpRecent` 15103-15111 מדפיס רק `'✓ '+esc(when)` — «נשמר לפני 12 דקות», לא סטטוס. ורשימת «הדיווחים שלי החודש» של הנאמן קוראת 14312 `var mine=_truRows(m).filter(function(r){return r.u===me;})` — `trustee_reports` בלבד; דיווח כמעט-ונפגע לא נכנס לשם לעולם, והיא המקום היחיד שמציג `_truStatusBadge(r)` ואת `r.mgr_note` (14354).

**השפעה**: נאמן שלוחץ «דווח כמעט ונפגע» מהמסך שלו מקבל טופס של ממונה בטיחות: לסווג חומרה, לסווג סוג, לקבוע סטטוס — כולל האפשרות להגיש אירוע כ«סגור» — לכתוב «הערות», ולהפעיל AI. שמו לא ממולא מראש אף שהמסך יודע אותו (`_truMe()`), וגם התאריך ריק. אחרי השליחה הדיווח נעלם: בקיוסק אין שום מסך שמראה מה קרה איתו. זה בולט במיוחד כי על ליקויי הנאמן האפליקציה כן עונה יפה — סטטוס, תג גיל, הערת ניתוב — אז אותו אדם, באותו מסך, מקבל תשובה על דבר אחד ושתיקה על השני.

**מיקום התיקון**: index.html:15084-15089 (`empReport` — גרסת מצב-עובד שמסתירה `nm-s`/`nm-sev`/`nm-typ`/`nm-notes`/`#nm-ai-btn` וממלאת `nm-rep` מ-`_truMe()` ו-`nm-d` מהיום), 2369-2381 (לעטוף את שדות המנהל ב-class אחד, למשל `nm-mgr-only`), ו-14348-14355 (להרחיב את «הדיווחים שלי החודש» גם לשורות `near_miss`).

**הערה**: מגבלה שמשנה את גודל העבודה, ובדקתי אותה: `near_miss` פתוח לקריאה ל-admin/manager בלבד — migrations/2026-04-24_rls_stage2_admin_manager.sql:151-157 `CREATE POLICY near_miss_admin_manager_all ON near_miss FOR ALL TO authenticated USING (public.is_admin_manager())`, וה-`emp_insert` הוא INSERT בלבד. כלומר הסשן האנונימי **לא יכול לקרוא בחזרה** את הדיווח שלו, ולכן «מה קרה עם מה שדיווחתי» עבור כמעט-ונפגע מוגבל למטמון המקומי של אותו מכשיר — סטטוס שהמנהל עדכן לא יגיע לשם לעולם בלי שינוי RLS. זה בדיוק ההבדל מ-`trustee_reports`, שה-SELECT שלו פתוח לכל authenticated (migrations/2026-09-18_trustee_reports.sql) — ובגללו המסך של הנאמן כן עונה. לכן: החצי של «שדות מנהל» הוא S ובטוח; החצי של המעקב דורש או policy חדש (פעולה ידנית ב-Supabase → כלל 7 ב-CLAUDE.md, אישור מפורש של מיכאל) או להסתפק ב«נשלח ✓» מקומי. שני סיכונים בתיקון: אל תסתיר את `nm-sev` בלי ברירת מחדל — `svNm` 8069 כותב `sev:gv('nm-sev')` ו-select מוסתר וריק ישמור `''`; ומיפוי הנאמן לדיווח דורש את `nm-rep` (ב-`near_miss` אין שדה `u`), אז מילוי מראש מ-`_truMe()` הוא תנאי מוקדם לרשימת המעקב, לא קישוט. לשימוש חוזר: `_truStatusBadge`, `fd()`.


### 5.1 — ✅ בוצע ב-**#657** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: `sbGet` (index.html:3023-3037) builds one URL and never pages: line 3034 — `return fetch(SBU+'/rest/v1/'+tbl+'?select='+encodeURIComponent(cols)+'&order='+ord+lim,{headers:sbH(),priority:'low'});`. No `Range` header, no `offset`, no loop, no `Prefer: count=exact`, so the response's `Content-Range` is never read and truncation is undetectable. `lim` comes from `_sbLimit` (3022) which holds only `{notifications_log:500}` — every business table sends no `limit` at all, which is exactly what `tests/harness/notif-log-test.js:86` asserts ("no business table asks for a limit"). But "no limit" is a client-side statement; PostgREST still applies the project's `db-max-rows`. The repo states the value itself in `workers/backup-cron.js:169-171`: "Supabase REST returns at most 1000 rows per request by default, so page through the table for safety" — the Worker pages with `Range: ${from}-${to}` (line 176), the browser does not. The second half of the claim is the deciding part, in `_sbMergePull` (3261-3283). `merged` is built only from the rows the server returned, and then only local rows that are still in the outbox are re-added: line 3277-3280 — `Object.keys(ob).forEach(function(id){ if(ob[id]==='del'||seen[id]||!local[id])return; merged.push(local[id]); });` followed by line 3282 `DB[t]=merged;`. A local row that was not in the returned page and has no pending op is silently dropped, and `sdb()` in `sbSync`'s `finish()` (3288) then writes the trimmed DB to localStorage.

**השפעה**: היום — כלום. אף טבלה עסקית לא מגיעה ל-1000 (ncr=375). ביום שבו טבלה תעבור את הרף: המיון הוא `created_at.desc` / `ts.desc`, אז **הרשומות הישנות ביותר הן שייעלמו** — בלי אזהרה, בלי שורת «מוצגות X מתוך Y», מהרשימות, ממוני ה-Dashboard, מהתפוגות ומה-CSV. הנתונים נשארים שלמים ב-Supabase — זה אובדן תצוגה, לא אובדן רשומות — אבל הגיבוי המקומי בונה מ-DB (ראה note), ובו זה כן הופך לאובדן אמיתי.

**מיקום התיקון**: index.html:3023-3037 `sbGet` — page with `Range: from-to` + `Prefer: count=exact` and loop until the page is short, exactly as `workers/backup-cron.js:169-200 fetchAllRows` already does. Optional belt-and-braces: `index.html:3261-3283 _sbMergePull` refuse to replace `DB[t]` when the answer was truncated.

**הערה**: שני דברים משנים את התמונה: (1) הפתרון כבר כתוב ב-repo — `fetchAllRows` ב-worker עושה בדיוק את זה; להעתיק את התבנית, לא להמציא. (2) להשתמש ב-`Range` ולא ב-`limit=` — `tests/harness/notif-log-test.js:86-87` מקבע שאף טבלה עסקית לא שולחת `limit=`, וזו החלטה מפורשת ב-DECISIONS.md (2026-09-20, «למה אף טבלה עסקית לא נחתכה») — pagination ב-Range לא שוברת את הבדיקה ולא סותרת את ההחלטה. ההחלטה ההיא הניחה ש«בלי limit = הכל» — זה בדיוק הפער. תלות עם 5.4: `_backupBuildSnapshot` (16795-16815) קורא `DB[t]`, אז DB חתוך → גיבוי חתוך. לתקן את 5.1 לפני שמרחיבים את הגיבוי.


### 5.2 — ✅ בוצע ב-**#657** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: `_obSend` (index.html:3139-3162). PATCH is built at 3143 — `url=SBU+'/rest/v1/'+op.tbl+'?id=eq.'+encodeURIComponent(op.row.id);opts.method='PATCH';opts.headers=sbH();` — and DELETE at 3144. The only test on the answer is line 3147: `if(!r.ok){return r.text().then(function(body){ ... throw ... });}`. Nothing else. `sbH()` (3007) sends `'Prefer':'return=representation'`, so PostgREST answers a 0-row PATCH/DELETE with **HTTP 200 and `[]`** — `r.ok` is true, the promise resolves, and in `_obDrainOps` (3174-3213) line 3186 `return _obSend(op).then(function(){_obMarkSent(op);})` marks it sent; the op is then excluded from `merged` at 3209 and written out of localStorage at 3211. The returned representation array is never parsed, so "updated 1 row" and "updated 0 rows" are indistinguishable. Real triggers exist in this schema: `migrations/2026-04-24_rls_stage2_admin_manager.sql` gives `FOR ALL TO authenticated USING (public.is_admin_manager())` — a user with role מדווח, or the anonymous emp/trustee session (`migrations/2026-04-21_rls_roles.sql`, INSERT-only on 4 tables), gets 0 rows on UPDATE/DELETE and a 200. Same blind spot again at `_eqiPdfSyncRow` (7924-7932), line 7930: `if(!r.ok)return r.text().then(...)`.

**השפעה**: עריכה שה-RLS דחה נראית על המסך כאילו נשמרה, הפעולה יורדת מהתור, הפעמון מציג ✓ «בענן» — והסנכרון הבא (עד 10 דקות) מחזיר את הערך הישן והעריכה נעלמת. למיכאל עצמו (אדמין) המקרה השכיח הוא מחיקה של שורה שכבר נמחקה — לא מזיק. הנזק האמיתי הוא למשתמש בלי הרשאה או למסך הנאמן/עובד: שומר — וזה פשוט לא קורה, בלי אף הודעה.

**מיקום התיקון**: index.html:3139-3162 `_obSend` — for `upd`/`del`, parse the returned representation (or send `Prefer: count=exact` and read `Content-Range`) and throw when the affected count is 0, so the op stays in the queue and `syncDiag()` (3508) can name it. Same three lines at index.html:7924-7932 `_eqiPdfSyncRow`.

**הערה**: סיכון בתיקון: אם נזרוק על 0 שורות, **מחיקה של שורה שכבר נמחקה תיתקע בתור לנצח** ותדליק את הפעמון האדום של «פעולות נכשלו». לכן: `del` שהחזיר 0 שורות = הצלחה (המטרה הושגה), `upd` שהחזיר 0 שורות = כישלון. אפשר להשתמש ב-`op.tries` שכבר קיים (3077) כדי לא להסתולל. `_obLastErr`/`syncDiag` כבר יודעים להציג את זה — להשתמש במנגנון הקיים, לא לבנות חדש.


### 5.3 — ✅ בוצע ב-**#657** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: `sbGet` (3023-3037) collapses every non-2xx into `null` at line 3035: `.then(function(r){return r.ok?r.json():null;})`. `sbSync` (3284-3311) then does, at 3301-3303: `// null = the fetch failed (offline, 401, 5xx…). Leave the cache alone` / `if(rows===null){finish();return;}`. So a 401 is handled identically to being offline — no re-auth, no toast, no log. Worse, `finish()` (3287-3289) still runs for all 38 tables and reaches `if(done===tbls.length){SB_ON=true;sdb();_sbRefresh(silent);_obDrain();}` — the app declares itself online. And `_obDrainOps` with an empty queue hits line 3178: `if(!q.length){_obBadge('ok');_obSetErr(null);return Promise.resolve();}` → `_obBadge('ok')` (3123) paints the pill green with ✓ בענן. `_sbAuth` (3006) memoizes in `_sbAuthPromise` and is never reset on a 401 — the only four places that clear it (14048, 14058, 14103, 15059) are login/logout paths. Reads fail silently; writes do surface (`_obSend` throws → `hadErr` → red pill), so the asymmetry the claim describes is exactly right.

**השפעה**: אם ה-refresh token נפסל (מכשיר שהיה זמן רב בלי רשת, סיסמה שהוחלפה, משתמש שהושבת) — האפליקציה מציגה את העותק המקומי הישן עם פעמון ירוק «✓ בענן». מנהל בטיחות שמסתכל על רשימת התפוגות או על ה-NCR הפתוחים רואה מצב מלפני ימים ואין לו שום סימן לכך. זה המצב הגרוע משלט: לא «שגיאה» אלא «ביטחון-שווא».

**מיקום התיקון**: index.html:3023-3037 `sbGet` — distinguish 401/403 from other failures (return a sentinel, not bare `null`); on 401 clear `_sbAuthPromise`/`_sbToken` and retry once via `_sbAuth()`; if it still fails, surface it. index.html:3284-3311 `sbSync` — count auth failures and pass the state to `_obBadge`. index.html:3123-3137 `_obBadge` — must not paint ✓ when the last pull was rejected.

**הערה**: הסיבה שזה לא נשרף עד היום: `_sbBoot` (3005) מגדיר `autoRefreshToken:true` ו-`onAuthStateChange` מעדכן את `_sbToken`, אז בדרך כלל הטוקן טרי. זה מצמצם את הסבירות, לא את הנזק. אנחנו בעבר כן נפלנו על בדיוק הדפוס הזה — STATUS.md:208 מתעד 5 קריאות שהחזירו 0 שורות במקום 375 אחרי RLS Stage 2, ואיש לא ידע עד שחפרו. לא לערבב עם אופליין אמיתי: ההתנהגות הקיימת (להשאיר עותק מקומי) נכונה לאופליין — הבעיה היא ש-401 נראה אותו דבר. לא למחוק את הקאש על 401.


### 5.4 — ✅ confirmed · מאמץ M

**ראיה**: Two independent backup paths, both short, exactly as claimed. (1) Cron: `workers/backup-cron.js:21-27 const TABLES = [...]` — 33 names ending `'inspection_types','issue_types'`. No `ncr_ai`, `ncr_comments`, `ncr_patterns`, `audit_log`, `equip_inspection_history`, `notifications_log`, **and no `trustee_reports`/`trustees`/`trustee_winners`/`trustee_tasks`**. (2) In-app OneDrive backup: `index.html:16780 var _BACKUP_TABLES=[...]` — 37 names; it *does* end with `'trustee_reports','trustees','trustee_winners','trustee_tasks'`, but it too omits `ncr_ai`, `ncr_comments`, `ncr_patterns`, `audit_log`, `equip_inspection_history`, `notifications_log`. So the cron is a strict subset — the claim's "ה-cron גם לא את טבלאות הנאמנים" is precisely right. A third path looks like it covers them and does not: `_exportXlsx` (13033) lists them at 13052 — `'audit_log','notifications_log','ncr_ai','ncr_comments','ncr_patterns',` — but line 13073 reads `var rows=DB[t];if(!rows||!rows.length)return;`, and `DB` (2936) has no `ncr_ai`/`ncr_comments`/`ncr_patterns`/`equip_inspection_history` key at all, `sbSync`'s table list (3285) never pulls them, and `rAudit` (8619-8623) paints `audit_log` straight into the DOM without ever writing `DB.audit_log`. Those five sheets are therefore always skipped — the Excel file names them in its code and never contains them.

**השפעה**: אם צריך לשחזר מגיבוי: כל ניתוחי ה-AI ל-NCR (`ncr_ai`, היסטוריה לפי version — מוגדרת ב-CLAUDE.md כ«אסור למחוק»), כל ההערות על אי-התאמות, הדפוסים, **וכל יומן הביקורת** — נעלמים. יומן ביקורת הוא בדיוק הדבר שמבקר ISO 45001 מבקש כשהוא שואל «מי שינה ומתי». ובמסלול האוטומטי היחיד שלא תלוי באפליקציה פתוחה (ה-cron) גם כל עבודת הנאמנים — הדיווחים, הרשימה, המשימות והזוכים — לא מגובה.

**מיקום התיקון**: workers/backup-cron.js:21-27 `TABLES` — add the 4 trustee tables + `ncr_ai`, `ncr_comments`, `ncr_patterns`, `audit_log`, `equip_inspection_history`, `notifications_log` (S; `fetchAllRows` already pages and already falls back when a table has no `ts`). index.html:16780 `_BACKUP_TABLES` + 16795-16815 `_backupBuildSnapshot` — this is the M part: those five tables are not in `DB`, so the snapshot must fetch them from the server instead of reading `DB[t]`. index.html:13046-13054 `_exportXlsx` tableOrder — either fetch them too or drop the dead names so the sheet list stops lying.

**הערה**: התיקון הזול והבטוח הוא **ה-cron בלבד** — שתי שורות ב-`TABLES`, צד שרת, service-role key, בלי RLS ובלי תלות ב-DB של הדפדפן. זה לבדו סוגר את רוב הפער. ⚙ ה-Worker לא מנוהל מה-repo — wrangler.toml אומר «הדבק את תוכן backup-cron.js ל-Worker ב-dashboard», אז שינוי ב-repo **לא מגיע ליעד לבדו** — יש לעדכן ידנית ולאמת לפי כלל 7. תלות עם 5.1: כל הרחבה של הגיבוי בדפדפן יורשת את התקרה של 1000 שורות. סיכון: `audit_log` ו-`notifications_log` הן append-only — הוספתן מגדילה את ה-snapshot היומי, וה-prune שומר 15 עותקים — לבדוק נפח bucket לפני.


### 5.5 — ✅ confirmed · מאמץ S

**ראיה**: `workers/backup-cron.js:40-42` is the whole endpoint: `if (url.pathname === '/health') { return new Response('ok', { status: 200 }); }` — a string literal. It never lists the `backups` bucket, never looks at the newest snapshot's timestamp, and nothing anywhere records when `scheduled()` last ran. `runBackup` (57-132) returns a rich result object (`ok`, `errors`, `upload_status`, `total_rows`) but the cron path throws it away: line 32-33, `async scheduled(event, env, ctx) { ctx.waitUntil(runBackup(env)); }` — the return value is never stored or sent. On the app side, the only status Michael sees is at index.html:16553-16565, and it reads `localStorage.getItem('tfgn_backup_last')` — which is written **only** by `_backupRunNow` (16864, the browser/OneDrive backup), never by the cron. `_backupSyncFromStorage` (16907-16972) does list the bucket, and at 16944 computes `missing`, but it only `dlog`s the counts (16943) and returns `{synced, total}` to nobody — no toast, no threshold, no "the newest snapshot is 9 days old". STATUS.md:54 records `health check ב-/health` as if it were monitoring; it is a liveness ping for the Worker process, not for the backup.

**השפעה**: אם ה-cron ייפול — service key שפג, bucket שנמחק, טבלה ששינתה סכמה, Worker שלא נפרס מחדש — מיכאל יגלה את זה רק ביום שבו ינסה לשחזר. וגרוע מזה: המסך מראה לו בירוק «✅ גיבוי אחרון: לפני שעתיים» — זה הגיבוי מהדפדפן, שדורש OneDrive מחובר ואת האפליקציה פתוחה. השכבה האוטומטית היחידה יכולה להיות מתה חודשיים והמסך ימשיך להיות ירוק.

**מיקום התיקון**: workers/backup-cron.js:40-42 — make `/health` list the `backups` bucket (the code for it already exists in `pruneOldBackups`, 137-165), read the newest filename's embedded timestamp, and return 503 + JSON when it is older than ~30h. index.html:16907-16972 `_backupSyncFromStorage` already has `supabaseFiles` sorted desc — surface the newest name's age. index.html:16553-16565 — split the status line into two: גיבוי באפליקציה vs גיבוי אוטומטי בענן, each with its own age and color.

**הערה**: השם «/health» הוא חלק מהבעיה — STATUS.md:54 ו-project-files/INFRA-2026-05-06.md:27 שניהם מתעדים אותו כאילו הוא בדיקת גיבוי; אם מתקנים את הקוד צריך לעדכן גם את שני המסמכים. שוב — ה-Worker לא נפרס מה-repo (wrangler.toml:4-6), אז שינוי ב-`backup-cron.js` דורש הדבקה ידנית ב-Cloudflare dashboard ואימות לפני `[x]`. הדרך הקצרה ביותר לתועלת מיכאל היא דווקא צד האפליקציה — `_backupSyncFromStorage` כבר מחזיק את רשימת הקבצים מה-bucket ורץ ~15 שניות אחרי טעינה (17042); להשתמש ב-`fd()` לתצוגת התאריך ולא לפרמט ידנית. לא להפוך את זה ל-alert() — מסך ה-OneDrive הוא המקום הנכון.


### 5.6 — ✅ בוצע ב-**#657** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: index.html:30 — `<script defer src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2.45.4/dist/umd/supabase.js"></script>` (cross-origin, no `crossorigin` attr → no-cors request → opaque response). functions/sw.js.js:66-69 bypasses only supabase.co / /api/ / anthropic.com / googleapis.com — jsdelivr is NOT bypassed, so the SW handles it; the deciding line is functions/sw.js.js:76 `if (resp && resp.ok && resp.type === 'basic') {` — an opaque response has ok=false and type='opaque', so it is never written to the cache. functions/sw.js.js:28 `const SHELL = ['/', '/index.html', '/manifest.webmanifest', '/icon.svg', '/logo.jpg'];` — the library is not pre-cached at install either. The repo's own SW test already locks this in: tests/harness/sw-cache-test.mjs:214 `check('a cross-origin (non-basic) response is never written to the cache', ...)`. Offline the fetch fails and functions/sw.js.js:101 falls back to `caches.match(req).then((r) => r || caches.match('/'))` — a text/html document returned for a script request, which _headers:6 `X-Content-Type-Options: nosniff` then refuses to execute. Result: `window.supabase` undefined → index.html:3005 `_sbBoot(){if(_sbClient||typeof supabase==='undefined')return;` leaves `_sbClient` null → boot IIFE index.html:15149-15150 `_sbBoot(); if(!_sbClient){proceed(null);return;}` → session null → index.html:15142 `showLogin()`. Pressing כניסה hits index.html:14007 `if(!_sbClient){...;_loginErr('Supabase אינו זמין');return;}`.

**השפעה**: מיכאל פותח את האפליקציה בשטח בלי קליטה. המעטפת נטענת מיידית מהמטמון — הוא רואה מסך כניסה מלא, מקליד סיסמה, ומקבל «Supabase אינו זמין». _loginErr (index.html:13797) מוחק את ההודעה אחרי 2.5 שניות ומרוקן את שדה הסיסמה, אז נשאר מסך כניסה ריק בלי הסבר. הנתונים שכבר על המכשיר (localStorage) קיימים ולא נגישים — גם הסשן השמור ב-`tfgn_sb_auth` לא נקרא, כי אין ספרייה שתקרא אותו. מצב עובד (EMP_KEY==='1') כן נכנס אופליין (index.html:15139) — הנעילה היא של המנהל דווקא.

**מיקום התיקון**: functions/sw.js.js:28 (SHELL) + functions/sw.js.js:75-80 (store) · index.html:30 (תג ה-script) · index.html:15149-15151 (מסלול ה-boot) · tests/harness/sw-cache-test.mjs:214 (הבדיקה שתצטרך לגדול)

**הערה**: התיקון הנקי ביותר הוא לא לגעת ב-SW בכלל: לשים עותק של supabase.js כקובץ סטטי ב-repo ולשנות את index.html:30 ל-`/supabase-2.45.4.js`. אז התגובה היא same-origin → `type:'basic'` → ה-store הקיים שומר אותה ללא שינוי, וצריך רק להוסיף את הנתיב ל-SHELL (functions/sw.js.js:28) כדי שגם הפתיחה האופליינית הראשונה תעבוד. זה לא סותר כלל 5/6 ב-CLAUDE.md (הכלל אוסר קבצי HTML נוספים ו-build step, לא קובץ vendor סטטי), ו-_headers:17 כבר מתיר `script-src 'self'`. ⚠ סיכון אם בוחרים במסלול השני — לשמור תגובות opaque במטמון: ל-opaque יש status 0, ואי אפשר להבדיל בין הצלחה ל-404, אז שגיאה חד-פעמית נתקעת במטמון לתמיד. ⚠ תלות שמגבילה את S: גם אחרי שהספרייה נטענת, `getSession()` (index.html:15151) עלול להיכשל אופליין אם ה-access token פג (ברירת מחדל שעה) — supabase-js ינסה refresh ברשת. כדי שהפתיחה הקרה באמת תפתח את האפליקציה צריך גם מסלול שסומך על הסשן השמור/`_currentUser` במקום לדרוש טוקן טרי. בלי זה התיקון פותר רק פתיחה בתוך השעה האחרונה.


### 5.7 — ✅ confirmed · מאמץ M

**ראיה**: index.html:3238 `function sbUpd(tbl,row){if(!row||!row.id)return;_aud('upd',tbl,row);_obPush({op:'upd',tbl:tbl,row:row});if(SB_ON)_obDrain();}` — השורה השלמה נכנסת לתור. השורה המכריעה בשליחה, index.html:3143: `else if(op.op==='upd'){url=SBU+'/rest/v1/'+op.tbl+'?id=eq.'+encodeURIComponent(op.row.id);opts.method='PATCH';opts.headers=sbH();opts.body=JSON.stringify(op.row);}` — PATCH לפי id בלבד, בלי `If-Match`, בלי תנאי על `updated_at`, והגוף הוא כל השורה. מה נשלח: index.html:4035 (_svPut) `if(k>=0){arr[k]=Object.assign({},arr[k],r);sbUpd(tbl,arr[k]);}` — כל העמודות שהלקוח מכיר; ו-index.html:4339 (svNcr) `if(id){DB.ncr=DB.ncr.map(function(r){return r.id===id?rec:r;});sbUpd('ncr',rec);}` — `rec` נבנה מאפס מהטופס (index.html:4318-4337) ומחליף את השורה כולה. `grep -n "If-Match|updated_at|xmin|row_version" index.html` מחזיר 0 תוצאות, ובמיגרציות `updated_at` קיים רק ב-2026-05-10_oauth_tokens.sql. 31 אתרי קריאה ל-sbUpd, כולם באותה צורה.

**השפעה**: מי ששומר אחרון מנצח, בשקט מוחלט — אין הודעה, אין סימן, אין דרך לדעת שמשהו נדרס. שני התרחישים הממשיים: (א) מיכאל עצמו עובד משני מכשירים (CLAUDE.md: «שני חשבונות, טלפון + מחשב») — עורך NCR בטלפון, המודאל פתוח, בינתיים הדסקטופ שינה סטטוס; השמירה בטלפון מחזירה את הסטטוס הישן. (ב) גרוע יותר — התור האופליין: עריכה שנשמרה בשטח בלי קליטה יושבת ב-outbox, וכשהקליטה חוזרת ה-PATCH נשלח עם ערכים בני שעתיים ודורס כל מה שנעשה בינתיים. ב-NCR זה אומר אי-התאמה שנסגרה נפתחת מחדש, או תאריך יעד שחזר אחורה — נתון שמבקר ISO קורא.

**מיקום התיקון**: index.html:3143 (ענף ה-upd ב-_obSend — שם מוסיפים את התנאי) · index.html:3238 (sbUpd) · index.html:4035 (_svPut) · index.html:4339 (svNcr) · מיגרציה חדשה להוספת `updated_at` + טריגר לטבלאות הנערכות

**הערה**: Realtime לא מציל ולא מחמיר את הסיכוי — הוא מחמיר את ההפתעה: _rtApply (index.html:3384-3386) `DB[tbl]=DB[tbl].map(function(r){if(r.id===nr.id){found=true;return nr;}return r;})` מחליף את השורה המקומית בזמן שהמודאל פתוח, אבל שדות הטופס נשארים עם הערכים הישנים — כך שהשמירה דורסת נתון שהמערכת כבר ידעה שהשתנה. הערה חשובה למימוש: ב-_svPut הנזק מוגבל לשדות שב-_EDIT_MODS (index.html:4024), כי `Object.assign({},arr[k],r)` לוקח את שאר העמודות מהשורה המקומית המעודכנת; ב-svNcr אין הגנה כזו כי `rec` נבנה מלא. פתרון מינימלי ובטוח: עמודת `updated_at` + `?id=eq.X&updated_at=eq.<מה שנקרא>`, ובדיקת התשובה — PATCH שנגע ב-0 שורות = התנגשות, להציג למשתמש. ⚠ זה תלוי ישירות בפריט 5.2 באותו קובץ («PATCH שנגע ב-0 שורות נחשב הצלחה») — בלי לתקן קודם את 5.2, מנגנון הגרסה יזהה התנגשות ויזרוק את השינוי בשקט במקום לדרוס בשקט. לתקן את 5.2 קודם.


### 5.8 — ✅ מיגרציה נכתבה ב-**#657**, טרם הורצה — [#658](../../issues/658)

**ראיה**: migrations/2026-04-24_audit_log.sql:45-48 — השורה המכריעה היא 46: `CREATE POLICY audit_log_admin_manager_all ON audit_log\n  FOR ALL TO authenticated\n  USING (public.is_admin_manager())\n  WITH CHECK (public.is_admin_manager());`. `FOR ALL` כולל UPDATE ו-DELETE. זו המיגרציה היחידה שנוגעת ב-audit_log — `grep -rn audit_log migrations/*.sql` לא מחזיר שום policy מאוחר יותר, ו-2026-05-04_rls_perf_wrap.sql ו-2026-09-18_rls_auth_jwt_initplan.sql שניהם לא מזכירים אותה. מי שעובר את השער: private/public.is_admin_manager() (migrations/2026-05-29_canonical_is_admin_manager_function.sql:32-37) — `auth.jwt()->>'email'='admin@tfugen.local' OR EXISTS (... u.role IN ('אדמין','מנהל'))`. באפליקציה עצמה אין מסלול כזה: index.html:8623 (rAudit) רק קורא (`select=*&order=ts.desc&limit=200`), ואין אף קריאה ל-`sbUpd('audit_log'...)` או `askDel('audit_log'...)`. ההגנה היחידה היא לקוחית ודקורטיבית — index.html:3599 `_DEL_PROTECTED` רק מוסיף שורת אזהרה במודאל.

**השפעה**: יומן הביקורת אינו ראיה. כל מי שיש לו role 'מנהל' או 'אדמין' יכול, מדפדפן, לשלוח `DELETE /rest/v1/audit_log?id=eq.N` או `PATCH` שמשנה `user_email`, `op` או `ts` — ולמחוק/לשכתב בדיוק את השורה שמתעדת מה הוא עשה. ומכיוון ש-index.html:3220 `if(tbl==='audit_log')return;` מוציא את audit_log מהרישום של עצמו, מחיקה של שורת יומן לא מייצרת שורת יומן. בביקורת ISO 45001/14001 זה ההבדל בין «יומן שינויים» לבין «רשימה שאפשר לערוך»: אין דרך להוכיח שמה שרשום הוא מה שקרה.

**מיקום התיקון**: migrations/2026-04-24_audit_log.sql:44-56 — מיגרציה חדשה שמחליפה את audit_log_admin_manager_all ב-policy נפרד ל-SELECT (is_admin_manager) ומשאירה את audit_log_authenticated_insert כפי שהוא, בלי שום policy ל-UPDATE/DELETE

**הערה**: התיקון בטוח מבחינת קוד: אימתתי שהאפליקציה לעולם לא מעדכנת ולא מוחקת audit_log — _aud (index.html:3219-3236) דוחף אך ורק `{op:'ins'}`, והייצוא ב-index.html:13052 רק קורא. לכן הסרת UPDATE/DELETE לא שוברת שום זרימה קיימת. ⚠ זו פעולה שדורשת אישור מפורש של מיכאל לפי CLAUDE.md סעיף 2 ב-«Supabase MCP»: «שינוי או מחיקה של RLS policy קיימת» מסווג כהרסני — ולכן הפריט מסומן 👤 גם ב-BACKLOG. ⚠ שתי הסתייגויות על הראיה: (1) הקוד ב-repo הוא המקור היחיד שיכולתי לקרוא — הרשת של הסביבה חוסמת את Supabase (CLAUDE.md), אז אי אפשר לאמת מול pg_policies החי שלא נוסף policy out-of-band; (2) המיגרציה קוראת ל-`public.is_admin_manager()` בעוד המיגרציות מ-2026-05 ואילך עברו ל-`private.` — שתי הגרסאות קיימות בפרויקט, וכדאי לקבע את החדשה על `private.` כמו השאר. לשלב את השינוי עם 5.4, שדורש ממילא לכלול את audit_log בגיבוי.


### 5.9 — 🟨 partly · מאמץ M

**ראיה**: הליבה נכונה, המספרים לא. ספירה בפועל: `ls migrations/*.sql | wc -l` → **80**, לא 78; ומתוכן רק **37** מכילות את המילה verify בכלל, ורק **19** מכילות בלוק `-- Verify` בתחילת שורה. כלומר 43 מיגרציות לא מציעות אפילו שאילתת אימות בהערה. על «אין שום בדיקה אוטומטית» — קיימות 54 סוויטות ב-tests/harness/, אבל אף אחת לא נוגעת ב-DB החי: כל ההופעות של `znhjtpcltrxxyfjczgvw.supabase.co` בהן הן קבועים ב-mock (למשל tests/harness/anon-boundary-test.mjs:27 ואחריו `globalThis.fetch = async (url, init) => {...}`). שתי ה«storage» מטעות: tests/harness/storage-full-test.js בודק localStorage מלא, לא Supabase Storage; ו-tests/harness/storage-scope-test.js אומר על עצמו בשורות 9-11 «That boundary is not a property of the database — it is a property of THIS CODE» — הוא מאמת את מוסכמת השמות `tru-ph-` בלקוח, לא את ה-policy. מה כן קיים ומפריך את ה«שום»: functions/api/_securityselftest.js מריץ 3 בדיקות RLS אמיתיות מול ה-DB החי עם ה-JWT של הקורא — `sens_ncr_visible_to_caller` (שורות 219-234), `app_users_visible_to_caller` (240-256), `password_reset_visible_to_caller` (258-274). אפס בדיקות Storage שם. ואין CI: `ls .github/workflows/` מחזיר קובץ אחד, daily-reminders.yml, שה-schedule שלו מבוטל — tests/harness/run.sh מורץ ידנית בלבד.

**השפעה**: מה שהפריט מצביע עליו אמיתי, ויש לו הוכחה בתיקייה עצמה: migrations/2026-09-20_storage_trustee_scope.sql:1-30 מתעד חור שחי בייצור מספטמבר — ארבע policies של Storage שניתנו ל-`authenticated`, ומאז שנוסף מסך הנאמנים שנכנס אנונימית, «authenticated» כולל כל מבקר ברחוב. הוא יכול לרשום, להוריד, לדרוס ולמחוק כל קובץ בדלי: תמונות תקריות, סריקות בדיקות שמיעה, מסמכים רפואיים. שום דבר בפרויקט לא התריע — זה נמצא כי אדם פתח את הקוד. והמיגרציה שסוגרת אותו עדיין לא הורצה (BACKLOG 5.11 מסומן «לא הורצה»), כלומר החור פתוח גם עכשיו. הדפוס: policy נכתבת פעם אחת, המשמעות של «authenticated» משתנה חצי שנה אחר כך, ואין כלום שמודד את הפער.

**מיקום התיקון**: functions/api/_securityselftest.js:218-283 (להרחיב את בלוק בדיקות ה-RLS ולהוסיף בדיקות Storage — list/download/upload/delete מול טוקן אנונימי, שהתשובה הצפויה שלהן היא 403) · tests/harness/run.sh (לחבר סוויטה חדשה) · .github/workflows/ (אין שום workflow שמריץ את ההארנס — זה החלק החסר האמיתי)

**הערה**: לא לתקן את זה כפי שנוסח. הניסוח «אין שום בדיקה» יוביל לבנות מאפס את מה שכבר קיים; הפער האמיתי צר וממוקד יותר: (א) אפס בדיקות Storage בכל צורה שהיא, (ב) 3 טבלאות בלבד מכוסות ב-RLS מתוך 35+, (ג) שום דבר לא רץ אוטומטית — _securityselftest הוא endpoint שמישהו צריך לפתוח, ו-run.sh הוא סקריפט שמישהו צריך להריץ. ההרחבה הזולה ביותר היא להוסיף ל-_securityselftest בדיקות Storage שליליות מול טוקן אנונימי — הן היו תופסות את החור של 2026-09-20 ביום שנפתח, והמשתמש כבר יודע לפתוח אותו (יש כפתור «🔒 בדיקת אבטחה» בדשבורד, STATUS.md, PR #532). ⚠ תלות: הבדיקות האלה ייכשלו כל עוד migrations/2026-09-20_storage_trustee_scope.sql לא הורצה — להריץ אותה קודם (היא דורשת אישור מפורש, כמו 5.8). ⚠ סיכון: _securityselftest חשוף בלי אימות ומחזיר CORS `*` (שורה 38) — בדיקות חדשות חייבות להישאר ספירות/סטטוסים בלבד, בלי שמות קבצים או תוכן, אחרת הכלי שנועד למדוד את החור יהפוך למפת דרכים אליו.


### 6.1 — ✅ בוצע ב-**#660** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: `window._exportXlsx` — index.html:13033-13096. The header comes from the raw DB keys and nothing translates them. index.html:13075-13076 collects the union of keys per table: `var keys={};rows.forEach(function(r){if(r&&typeof r==='object')Object.keys(r).forEach(function(k){keys[k]=1;});});` / `var cols=Object.keys(keys);` — and the deciding line, index.html:13085: `var ws=XLSX.utils.json_to_sheet(data,{header:cols});`. `cols` goes straight in as the header row. The only Hebrew in the function is the `sheetName` map (13056-13070), which names the TAB, not the columns. The claim's three examples all check out: `sv` and `dy` are real `inc` columns (VIEW_CONFIG inc, index.html:11206-11207: `['חומרה','sv'],['מיקום','l'],['ימי אבדן','dy']` = חומרה / ימי אבדן), and `u` is the NCR due date (index.html:2017: `<label>&#1497;&#1506;&#1491; &#1500;&#1489;&#1497;&#1510;&#1493;&#1506;</label><input id="ncr-u" type="date">`).

**השפעה**: מבקר ISO פותח את הגיליון ורואה בלשונית NCR שורת כותרת של 21 עמודות: id, num, d, a, category, loc, location_id, project_id, issue_type_id, rc, c, o, u, s, sd, cd, notes, five_why, sens, p, ts. בלשונית תקריות: dt, d, ty, sv, l, dy, r. הוא לא יכול לדעת ש-sv זה חומרה, dy זה ימי אבדן ו-u זה יעד לביצוע. זה קובץ שנשלח כראיה במבדק — הוא קריא רק למי שמכיר את הסכמה.

**מיקום התיקון**: index.html:13076 + 13085 — לבנות מערך כותרות מתורגם ולהעביר אותו ל-`XLSX.utils.json_to_sheet` (או `sheet_add_aoa` על שורה 1) במקום `cols`. אותו פגם בדיוק ב-`_backupXlsxBlob` index.html:16835 (`var ws=XLSX.utils.json_to_sheet(rows);`) — גיבוי OneDrive, שם גם שמות הלשוניות גולמיים (16834: `var sheetName=t.length>31?t.substring(0,31):t;`).

**הערה**: יש עזר קיים לשימוש חוזר: `VIEW_CONFIG` (index.html:11191-11268) מחזיק תוויות עברית כזוגות [label, key] ל-25 טבלאות. אזהרה: VIEW_CONFIG לא מכסה את כל 39 הטבלאות ב-`tableOrder` (13047), ואפילו בטבלאות שכן — לא את כל העמודות (id, ts, location_id, project_id, sens, five_why לא מופיעים ב-fields של ncr). לכן fallback לשם הגולמי הוא חובה, אחרת עמודות ייעלמו מהגיבוי — וזו רגרסיה גרועה יותר מכותרת לא ברורה. שימו לב גם: `_exportXlsx` כבר מכיל מחרוזות עברית גולמיות (13056-13070, וגם `toast('בונה קובץ אקסל...')` ב-13034) בניגוד לחוק 1 ב-CLAUDE.md — אל תוסיפו עוד, והמרה ל-\uXXXX תוך כדי היא שיפור. אין החלטה ב-DECISIONS.md שמצדיקה כותרות גולמיות.


### 6.2 — ✅ בוצע ב-**#666** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: שתי הנוסחאות קיימות, ו-`ts` אכן נדרס. נוסחה א' (cd מינוס ts) ב-`_renderKpiCard`, index.html:5037: `var days=(new Date(r.cd)-new Date(r.ts))/(1000*60*60*24);` ו-index.html:5043: `? Math.round(ncrClosed.reduce(function(s,r){return s+(new Date(r.cd)-new Date(r.ts))/(1000*60*60*24);},0)/ncrClosed.length)`; אותה נוסחה שוב בתיק המבדק ISO, index.html:12866: `var ageDays=n.cd?Math.round((new Date(n.cd)-new Date(n.ts))/(1000*60*60*24)):...`. נוסחה ב' (cd מינוס sd) בכרטיס «זמן טיפול NCR» בדשבורד, index.html:5379: `try{var a=new Date(n.sd),b=new Date(n.cd);var dd=(b-a)/86400000;if(!isNaN(dd)&&dd>=0){sum+=dd;cnt++;}}catch(e){}` ו-index.html:5392: `try{var d=(new Date(n.cd)-new Date(n.sd))/86400000;return d<=30;}catch(e){return false;}`. ו-`ts` — `svNcr` index.html:4337 בונה את האובייקט עם `ts:new Date().toISOString()` ללא תנאי, ואז index.html:4338: `if(id){DB.ncr=DB.ncr.map(function(r){return r.id===id?rec:r;});sbUpd('ncr',rec);}` — כלומר כל עריכה של NCR קיים כותבת ts=עכשיו. גם `_reopen` עושה זאת, index.html:13318: `rec.ts=new Date().toISOString();`.

**השפעה**: שני מדדים שונים באותו מסך-בית, ושניהם שקריים. האריח «ממוצע ימי סגירת NCR» (index.html:5063) מראה tooltip «זמן ממוצע מפתיחה ועד סגירה» אבל מחשב מהשמירה האחרונה. פתח NCR ישן מ-2025, תקן בו פסיק, שמור — `ts` קופץ להיום, `cd` נשאר בעבר, והחישוב יוצא שלילי: האריח «NCR נסגרו עד 30 יום» סופר אותו כסגירה מהירה (`days<=30` נכון גם ל-300-), והממוצע נגרר מטה עד למספר שלילי. מיכאל יראה שני מספרים סותרים — למשל «ממוצע 42 יום» בכרטיס הדשבורד ו-«ממוצע 12- יום» באריח ה-KPI — ואין לו דרך לדעת מי צודק. במבדק זה מספר שהוא צריך להגן עליו.

**מיקום התיקון**: לרכז ל-helper יחיד, למשל `_ncrOpenedAt(n)` שמחזיר `n.sd||n.src_date||n.created_at||n.ts`, ו-`_ncrCloseDays(n)`. אתרי תיקון: index.html:5035-5043 (`_renderKpiCard`), index.html:5373-5393 (כרטיס dash-ncr-time ב-`rDash`), index.html:12866 (תיק המבדק). בנוסף לעצור את דריסת `ts` — index.html:4337 ב-`svNcr` (לשמר את ה-ts המקורי בעריכה, או להוסיף `created_at` נפרד) ו-index.html:13318 ב-`_reopen`.

**הערה**: התמונה גרועה מהנטען — יש פער שלישי שהסקירה פספסה: שורה 5373 מסננת `n.s===_CLOS2&&n.sd&&n.cd` ללא fallback ל-`src_date`, בעוד ששאר הקוד כן נופל אחורה (index.html:5876 `n.sd||n.src_date||n.d`, 8569 `n.sd||n.src_date||n.created_at||n.ts`, 15794 אותו דבר). 375 ה-NCR שיובאו מאקסל נושאים `src_date` ולא `sd` — ראו `xlParse` index.html:15164 שכותב `rec.src_date=xlDate(...)` ולעולם לא `sd`, ו-DECISIONS.md:1022 שמתעד את הוספת העמודות `cd`/`sd` רק ב-2026-04-22. כלומר כרטיס «זמן טיפול NCR» בדשבורד מתעלם כמעט מכל ההיסטוריה. סיכון בתיקון: `svNcr` בונה אובייקט חדש ומחליף את הרשומה כולה (4338) — כדי לשמר `ts` צריך לקרוא את הרשומה הקיימת לפני ההחלפה. שימו לב ש-`created_at` מוזכר בקוד (8569) אבל לא ניתן לאמת מהסשן שהעמודה קיימת ב-Supabase — כל דומייני Supabase חסומים ברשת (CLAUDE.md). לפני שמסתמכים עליה — לבקש ממיכאל אימות ב-SQL Editor. אין החלטה ב-DECISIONS.md שמצדיקה את שתי הנוסחאות; זו סחיפה, לא בחירה.


### 6.3 — ✅ בוצע ב-**#666** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: שני מדדי הזמן היחידים במערכת רצים על כל ההיסטוריה, בלי שום חלון תאריכים. index.html:5035: `var ncrClosed=ncrAll.filter(function(r){return r.cd;});` — הפילטר בודק רק שקיים תאריך סגירה, אין בו תנאי תאריך; ממנו נגזרים `pctFastNcr` (5041) ו-`avgNcrDays` (5042-5044). אותו דבר בכרטיס הדשבורד, index.html:5373: `var closed=ncrAll.filter(function(n){return n.s===_CLOS2&&n.sd&&n.cd;});` — שוב בלי חלון. אין אף אלמנט UI לבחירת תקופה: `_mrPeriod()` (index.html:8533-8541) בונה את הרבעון מ-`var d=new Date();` בלבד ומחזיר `{q,y,start,end,label}`, ו-`mr-period` (index.html:1404) הוא `<span>` טקסט בלבד — אין select, אין כפתורים. grep ל-`date-from`/`dateFrom`/range על כל הקובץ לא מחזיר שום בורר תאריכים בדוחות.

**השפעה**: מיכאל לא יכול לענות על השאלה שנשאלת בכל ישיבת סקירת הנהלה: «השתפרנו?». הממוצע שהוא רואה מרוכך על פני כל 375 הרשומות מ-2023 ואילך, כך שגם רבעון מצוין או רבעון גרוע כמעט לא יזיזו אותו. בפועל המספר קפוא ולא ניתן להציג אותו כמגמה.

**מיקום התיקון**: index.html:5035-5044 (`_renderKpiCard`) ו-index.html:5373-5394 (כרטיס dash-ncr-time) — להוסיף פרמטר תקופה לחישוב. index.html:8533-8541 (`_mrPeriod`) — להכליל ל-`_mrPeriod(offset)`. index.html:1404 — להוסיף בורר תקופה ליד `mr-period`.

**הערה**: הכותרת המודגשת «אין חלוקה לתקופות» רחבה מדי ועלולה להטעות: חלוקה לתקופות כן קיימת — למונים. `_mrInPeriod` (index.html:8542-8546) מסנן לרבעון הנוכחי את «NCR שנפתחו/נסגרו/תקריות/כמעט-נפגע» (8569-8585), `_wkBucket` (index.html:5874) בונה 4 חלונות שבועיים לגרף נפתח-מול-נסגר, ויש חלונות חודש/7-ימים בדשבורד. מה שבאמת חסר הוא בדיוק מה שהמשפט השני בטענה אומר: (א) אף מדד זמן אינו מוגבל לתקופה, (ב) אין בורר תקופה — הכול נעול על הרבעון/השבוע הנוכחי. לתיקון יש להשתמש מחדש ב-`_mrPeriod`/`_mrInPeriod` הקיימים ולא לכתוב לוגיקת תאריכים שלישית. תלות קשה ב-6.2: אין טעם לפלח מדד זמן לרבעונים כל עוד הוא נמדד מ-`ts` שנדרס בכל שמירה — פילוח לפי רבעון רק יבליט את הרעש. לתקן את 6.2 קודם.


### 6.4 — ✅ בוצע ב-**#666** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: הפרומפט אכן מבקש השוואה, והנתונים אינם כוללים אותה. index.html:12086 בתוך `_mrAINarrative`: `'(2-3 משפטים — מצב ההתאמה הכללי, האם המגמה טובה/קבועה/מדאיגה ביחס לרבעון קודם)'` (בקובץ זה כתוב בעברית גולמית: «האם המגמה טובה/קבועה/מדאיגה ביחס לרבעון קודם»). ה-ctx שנשלח (index.html:12067-12083) נבנה כולו מ-`txt(...)` על אלמנטים של הרבעון הנוכחי בלבד — `ncrQNew`, `ncrQClosed`, `incQ`, `nmQ` — ואין בו אף מספר מרבעון קודם. ובאותו פרומפט, index.html:12097: `'- אל תמציא מספרים — השתמש רק בנתונים שהופקו'` — «אל תמציא מספרים». בצד הדף: קראתי את `rMr()` במלואה (index.html:8547-8630) — אין בה שום חישוב של רבעון קודם ושום Δ. `_mrPeriod()` (8533-8541) בנוי מ-`var d=new Date();` ומחזיר רבעון אחד בלבד. grep ל-`Δ` / `prevQ` / `_mrPrev` על כל הקובץ מחזיר אפס תוצאות מלבד שורת הפרומפט 12086.

**השפעה**: הפרומפט מציב את המודל בסתירה: «תגיד אם המגמה טובה ביחס לרבעון קודם» מול «אל תמציא מספרים», ובלי נתוני השוואה. התוצאה היא או משפט מעורפל שלא אומר כלום («המגמה נראית יציבה»), או — גרוע יותר — מספר שהמודל השלים בעצמו. הטקסט הזה מיועד להעתקה לפרוטוקול סקירת הנהלה שנחתם ונמסר למבקר. משפט מגמה מומצא בפרוטוקול חתום הוא ממצא במבדק, לא באג UI. ובנוסף, מיכאל עצמו לא רואה על הדף אם 14 NCR ברבעון זה טוב או רע.

**מיקום התיקון**: index.html:8533-8541 — להכליל את `_mrPeriod()` ל-`_mrPeriod(offset)` (offset=1 → הרבעון הקודם, כולל גלישה לשנה קודמת ב-Q1). index.html:8569-8585 בתוך `rMr()` — לחשב את אותם 5 מונים גם ל-`p1` ולהציג Δ ליד כל ערך. index.html:12053-12065 + 12067-12083 — להוסיף את ערכי הרבעון הקודם ל-`kpis` ול-`ctx`.

**הערה**: התיקון זול יחסית כי כל התשתית קיימת: `_mrInPeriod(d,p)` (8542) כבר מקבל אובייקט תקופה כפרמטר, אז מספיק לקרוא לו פעמיים. מלכודת: `_mrAINarrative` קורא ערכים מה-DOM דרך `txt(id)` (index.html:12047), כלומר Δ שיוצג על המסך חייב לשבת באלמנט נפרד — אם תדחפו «14 (+3)» לתוך `mr-ncr-q-new` הטקסט הזה ייכנס לפרומפט כמחרוזת ויבלבל את המודל. הוסיפו span נפרד. תלות: אם מיישמים גם 6.3, ה-generalization של `_mrPeriod` משותף לשניהם — לעשות פעם אחת. אזהרת סגנון: `_mrAINarrative` והפרומפט כולו (12084-12100) כתובים בעברית גולמית ב-JS בניגוד לחוק 1 ב-CLAUDE.md; אל תרחיבו את החוב — טקסט חדש ב-\uXXXX. לא נמצאה החלטה ב-DECISIONS.md שמצדיקה את היעדר ההשוואה.


### 6.5 — ✅ בוצע ב-**#660** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: החלק על הקבורה נכון, החלק על «לא בבית» לא. `_ncrTrendSvg()` מוגדר ב-index.html:15786 ונקרא בדיוק פעם אחת בכל הקובץ — index.html:15839: `P.innerHTML=_ncrTrendSvg()+legend+summary;` בתוך `_ncrShowTrend`, ש-`P` שלו הוא `document.getElementById('ncr-agent-panel')` (15827), פאנל ברוחב 360px בתוך מודאל ה-NCR Agent. הכניסה היחידה היא הכפתור בכותרת המודאל, index.html:15184: `<button onclick="_ncrShowTrend()" ... title="&#1490;&#1512;&#1507; 12 &#1495;&#1493;&#1491;&#1513;&#1497;&#1501;">&#128200; &#1502;&#1490;&#1502;&#1493;&#1514;</button>`, והמודאל עצמו נפתח רק מ-`openNCRAgent()` — שני אתרי קריאה בלבד: index.html:1470 (כפתור «🤖 סוכן AI» בדף NCR) ו-index.html:3937. יתרה מזו, הגרף אוכל מ-`_nad` (index.html:15187), שמתמלא רק ב-`_ncrLoad` (index.html:15445: `_nad=raw;`) — fetch נפרד ל-REST, כך שבלי לפתוח את הסוכן ולחכות לטעינה אין גרף. אבל: בדשבורד כן יושב גרף מגמה — index.html:898: `<div class="card" id="dash-ovc-card" style="display:none"><div class="card-title">&#128202; NCR &#1504;&#1508;&#1514;&#1495; &#1502;&#1493;&#1500; &#1504;&#1505;&#1490;&#1512; &#8212; 4 &#1513;&#1489;&#1493;&#1506;&#1493;&#1514;</div>`, בתוך `<div id="pg-dash" class="page on">` (index.html:830), ומצויר ב-`rDash` index.html:5862-5885 עם עמודות SVG-less (divים) לכל שבוע.

**השפעה**: הגרף הפורמלי — 12 חודשים בפילוח בטיחות מול סביבה, בדיוק מה שמבקר ISO מבקש כראיה למגמה — נגיש רק במסלול של 3 לחיצות (דף NCR ← 🤖 סוכן AI ← 📈 מגמות), מצויר על רקע כהה ברוחב 360px, ולא נכנס להדפסה: `_printPage` (index.html:13212) מדפיס `.page.on`, והגרף חי במודאל. בסקירת ההנהלה ובתיק המבדק אין לו זכר. מצד שני, מיכאל כן רואה מגמת NCR בבית — ארבעה שבועות אחרונים, נפתח מול נסגר. אז «לא בבית» שגוי; הנכון הוא «בבית יש רק 4 שבועות, והגרף השנתי קבור».

**מיקום התיקון**: אם מחליטים לחשוף — `_ncrTrendSvg` index.html:15786 כבר מחזיר SVG עצמאי ואפשר לשתול אותו בדף MR (ליד index.html:1417) או בתיק המבדק (index.html:12940). דורש שתי התאמות: (א) הוא קורא `_nad` ולא `DB.ncr` — לפרמטר את מקור הנתונים, (ב) צבעי הרקע קשיחים לערכת המודאל הכהה (`background:#0d1014`, `fill="#7a9ab8"`, index.html:15809-15812) ולא יהיו קריאים על דף לבן או בהדפסה.

**הערה**: לפני שמזיזים משהו לדשבורד — יש החלטה קודמת נגד. DECISIONS.md:1274-1278, «2026-04-19 — Revert: הסרת Dashboard 2.0 Phase A»: «הסרת 4 הגרפים (Chart.js) שנוספו ב-PR #16... סיבה: החלטה של המשתמש — הגרפים לא סיפקו ערך מספיק או לא התאימו לזרימת העבודה היומיומית». אחד מארבעת הגרפים שהוסרו היה בדיוק זה: «NCR trend (line, 12 חודשים, Safety vs Env)» (DECISIONS.md:1332). כלומר להחזיר גרף 12 חודשים לדשבורד זה להפוך החלטה מפורשת של מיכאל — צריך לשאול אותו, לא להניח. היעד ההגיוני שלא מתנגש עם ההחלטה הזו הוא דף סקירת ההנהלה ותיק המבדק, שם הגרף משרת מבקר ולא את הזרימה היומיומית. חומרה 🟡 נראית נכונה: זו נגישות, לא נתון שגוי.


### 6.6 — ✅ בוצע ב-**#660** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: שני חלקים מתוך שלושה כבר קיימים.
(א) גרף «נפתח מול נסגר» קיים בבית — HTML שורה 898: `<div class="card" id="dash-ovc-card" ...>📊 NCR נפתח מול נסגר — 4 שבועות`, מרונדר ב-`rDash()` שורות 5860-5893. השורה המכריעה, 5873-5876: `(DB.ncr||[]).forEach(function(n){var b=_wkBucket(n.sd||n.src_date||n.d); if(b>=0)weeks[b].opened++; if(n.s==='סגור'&&n.cd){var bc=_wkBucket(n.cd);if(bc>=0)weeks[bc].closed++;}});` — כלומר נפתח+נסגר כבר מחושבים, אבל ב-4 דליים שבועיים, ובעמודות div של CSS ולא SVG.
(ב) גרף 12 חודשים ב-SVG ידני קיים — `_ncrTrendSvg()` שורות 15786-15825, `for(var i=11;i>=0;i--){var m=new Date(d.getFullYear(),d.getMonth()-i,1);months.push({...})}` — אבל הוא מצייר רק נפתחים, מפוצלים בטיחות/סביבה: שורה 15798 `if(isEnv)months[mi].env++;else months[mi].saf++;`
(ג) «צבר» (מצטבר) — לא קיים בשום מקום. בכל הקובץ יש בדיוק 2 תגי `<svg>`: מחולל ה-QR (6636) ו-`_ncrTrendSvg` (15808). אפס `polyline`.

**השפעה**: מיכאל כבר רואה היום בבית נפתח-מול-נסגר, אבל רק ל-4 שבועות. מה שהוא לא יכול לראות זה אם הערימה גדלה או קטנה לאורך שנה — בדיוק השאלה שמבקר ISO שואל. הפער אמיתי אבל צר בהרבה ממה שהסעיף מתאר.

**מיקום התיקון**: index.html:5860-5893 (הבלוק של `dash-ovc` בתוך `rDash`) — להרחיב מ-4 שבועות ל-12 חודשים ולהוסיף סדרת צבר מצטבר. לחלופין למזג עם `_ncrTrendSvg()` ב-15786-15825.

**הערה**: שתי מלכודות נתונים לפני שנוגעים בזה. (1) `n.d` ב-`_wkBucket(n.sd||n.src_date||n.d)` הוא **התיאור** של ה-NCR, לא תאריך — `ncr-d` הוא `<textarea>` בתווית «תיאור» (שורה 2006) ו-`svNcr` שורה 4319 שומר `d:d`. לכן NCR בלי `sd` פשוט נעלם מספירת «נפתח» בשקט. (2) אותו בלבול כבר יוצר באג פעיל בשורה 5309: `var ncrM=(DB.ncr||[]).filter(function(r){return inMonth(r.d||r.ts);}).length;` — `new Date(<תיאור בעברית>)` הוא NaN, ולכן הצ'יפ «NCR» בכרטיס «סיכום חודש זה» תמיד 0 לכל NCR שנשמר דרך הטופס. לתקן את זה קודם, אחרת הגרף החדש יורש את אותה טעות. חופף לסעיף 6.5 (אותו גרף «קבור» במודאל סוכן ה-AI, נגיש רק דרך דף NCR → «🤖 סוכן AI» → «📈 מגמות», שורה 1470) — עדיף לטפל ב-6.5 ו-6.6 כמשימה אחת.


### 6.7 — ✅ בוצע ב-**#668** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: אין שום sparkline בקובץ: בכל 18,869 השורות יש 2 תגי `<svg>` בלבד — QR (6636) ו-`_ncrTrendSvg` (15808) — ו-`grep -c polyline` מחזיר 0.
המכולה כבר קיימת וריקה מתוכן מגמתי: HTML שורה 871 `<details class="more-accordion" id="dash-kpi-acc" style="display:none"><summary>📊 אינדיקטורים מובילים (Leading Indicators)</summary><div id="dash-kpi"></div></details>`, ממולאת ב-`_renderKpiCard()` שורות 5013-5072. ארבעת האריחים שם הם מספרים סטטיים, ואף אחד מהם אינו אחד מהארבעה שבסעיף: `tiles.push(tile(pctValid+'%','ציוד בתוקף',...))`, יחס Near-Miss:Incident ל-30 יום, `'NCR נסגרו עד 30 יום'`, ו-`'ממוצע ימי סגירת NCR'`. כמעט-ונפגע כמגמה, דיווחי נאמנים, סבבים ושיחות בטיחות — אף אחד מהם אינו מדוד כאן.

**השפעה**: אמיתי. מיכאל רואה מספר של היום בלי כיוון. «12 דיווחי נאמנים החודש» לא אומר כלום בלי לדעת שלפני חודשיים היו 30. זו בדיוק ההבחנה בין מדד מוביל למדד מפגר.

**מיקום התיקון**: index.html:5013-5072 (`_renderKpiCard`) — להוסיף sparkline לכל אריח; והאקורדיון ב-index.html:871.

**הערה**: הנתונים מוכנים, אין צורך במיגרציה: `near_miss.d`, `rounds.d` ו-`toolbox.d` כולם `<input type="date">` אמיתיים (שורות 2369, 2491), ול-`trustee_reports` יש כבר עזר חודשי — `_truRowMonth(r)` בשורה 14171: `return r.m||_truMonthKey(r.d);`. להשתמש בו ולא לקרוא `r.m` ידנית — ההערה בשורות 6211-6213 בדוח ה-ISO מזהירה מפורשות מהטעות הזו. שתי מלכודות תצוגה: `_renderKpiCard` מסתיר את כל האקורדיון כשאין אריח עם נתונים (`if(!tiles.length){if(acc)acc.style.display='none';box.innerHTML='';return;}` שורה 5066), ו-`_dashHideEmptySection()` (5124) מסתיר את כותרת «סטטיסטיקות ומגמות» — אריח שהוא sparkline בלבד חייב להשתלב בשני המנגנונים. יש כבר תקדים ויזואלי למיני-גרף ב-CSS: פס 30 הימים של הסבבים, שורות 5793-5817 — כדאי לשמור על אותה שפה ולא להוסיף שפה גרפית שנייה בבית.


### 6.8 — ✅ בוצע ב-**#668** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: היחיד שמפלח לפי אזור הוא `dash-by-loc` (שורות 5460-5494), והוא **תצלום רגע** בלי שום חלון זמן. השורה המכריעה, 5467-5472: `(DB.ncr||[]).forEach(function(n){if(n.s==='סגור')return; if(!n.location_id)return; if(!byLoc[n.location_id])byLoc[n.location_id]={ncr:0,nm:0}; byLoc[n.location_id].ncr++;});` — אין תנאי תאריך בכלל.
`_truCoverage(m)` (14647-14657) אכן לפי אזור, אבל לחודש בודד ולדיווחי נאמנים בלבד.
שתי מפות החום בקובץ — `_easpMatrix()` (10012) ו-`_rskMatrix()` — הן מטריצות 4×4 של הסתברות×חומרה, לא ציר זמן.

**השפעה**: בינוני. היום מיכאל יודע איזה אזור גרוע עכשיו; הוא לא יכול לראות איזה אזור הולך ומחמיר. לוועדת הבטיחות זה בדיוק ההבדל בין «יש 5 ליקויים באולם הטיגון» לבין «אולם הטיגון עולה חודש שלישי ברציפות».

**מיקום התיקון**: בלוק חדש ב-`rDash()` ליד `dash-by-loc` (index.html:5460-5494) + כרטיס בקטע הסטטיסטיקות סביב index.html:882. לשימוש חוזר: `_locName(id)`.

**הערה**: החסם כאן הוא נתונים ולא קוד. NCR נושא **שני** שדות מיקום — `loc` טקסט חופשי ישן ו-`location_id` FK — שניהם נשמרים ב-`svNcr` שורות 4322-4323, והטופס עדיין מציג רמז «מיקום ישן» כשיש אחד בלי השני: שורה 4287 `var lLeg=g('ncr-loc-legacy');if(lLeg)lLeg.style.display=(rec.loc&&!rec.location_id)?'':'none';`. עם 375 NCR בפרודקשן שרובם יובאו מאקסל, פס 6 חודשים לפי אזור יראה חודשים ריקים בכל מקום שבו יש רק `loc`. החלטה שצריך לקבל לפני הקוד: «אזור» = טבלת `locations` (ה-FK שיש ב-ncr/near_miss/inc/ptw/equip_inspections/hzm/hearing_tests) או `dep` של הנאמנים (`_truAreas()` שורה 14643)? אלה שתי אוצרות מילים שונות, וכרטיס כיסוי האזורים מ-PR #592 כבר משתמש ב-`dep`. לערבב ביניהן יפיק דוח שאי אפשר להסביר למבקר.


### 6.9 — ✅ בוצע ב-**#668** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: אין שום פילוח לפי מחלקה. `rTr()` שורות 9939-9943 היא טבלה שטוחה של כל ההדרכות: `tb.innerHTML=DB.tr.map(function(t){return '<tr><td><strong>'+esc(t.w)+'</strong></td><td>'+esc(t.n)+'</td><td>'+fd(t.d)+'</td><td>'+eb(t.e)+'</td>...` — אפס קיבוץ.
דוח מוכנות ה-ISO נותן מספר אחד לכל המפעל, `_isoClauses()` שורות 6246-6249: `var tr=_isoExpiry('tr'); add(both,'7.2','כשירות והדרכה',tr.total+' הדרכות · '+tr.ok+' בתוקף · '+tr.gone+' פגו',...)`.
מפתח הקישור שבסעיף נכון: `svTr()` שורה 4461 `var r={id:...,w:w,n:n,...}` כאשר `w=gv('t-w')` (תווית «שם עובד»), ו-`svEmp()` שורה 4491 `var r={id:...,n:n,r:gv('e-r'),dep:gv('e-d'),...}`.

**השפעה**: אמיתי. «איזו מחלקה בפיגור בהדרכות» היא השאלה הראשונה של מבקר ISO 45001 תחת סעיף 7.2, והיום אין דרך לענות עליה מהמערכת — רק מספר כולל.

**מיקום התיקון**: index.html:9939 (`rTr` — להוסיף סיכום מקובץ מעל הטבלה), או כרטיס חדש ליד סעיף 7.2 ב-`_isoClauses()` index.html:6246.

**הערה**: הסעיף מדייק בחצי: `tr.w` ↔ `emp.n` הוא אכן מפתח הקישור, אבל **המחלקה עצמה יושבת ב-`emp.dep`** ולא בשדה ששמו מחלקה — מאושר ב-`_EDIT_MODS` שורה 4024: `"emp":{..."f":{"e-r":"r","e-d":"dep",...}}`. הסיכון האמיתי: הקישור הוא טקסט חופשי משני הצדדים. `t-w` הוא `<input id="t-w">` חשוף (שורה 2072) — בלי `list=`, בלי datalist — בניגוד ל-`tsk-assignee` (שורה 2514) שכן מחובר לרשימה. כל שגיאת כתיב, שם הפוך או כינוי הופכים בשקט ל«לא משויך», והאחוזים ייראו שגויים. לפני בניית הדוח: לחבר את `t-w` ל-datalist של שמות `DB.emp` (התשתית כבר קיימת — שורה 4141 ממלאת datalists של אחראים מ-app_users + emp), ולהציג שורה מפורשת «N הדרכות ללא עובד מזוהה». בלי זה מיכאל יפסיק לסמוך על הדף אחרי הפעם הראשונה שהמספר ייראה לו לא הגיוני. לתוקף להשתמש ב-`du(t.e)` ו-`eb(t.e)` הקיימים ולא לחשב מחדש.


### 6.10 — ✅ בוצע ב-**#666** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: אין שום מדד «נסגר בזמן» — אף שורה לא משווה `closed_date` ל-`due`. אבל התלות שבסעיף מדויקת יותר ממה שנכתב: `closed_date` **כן קיים** באפליקציה ונכתב בשני מסלולי שינוי הסטטוס — `_tskBulkAction` שורה 8685 `if(status===_TSK_DONE&&!DB.tasks[idx].closed_date)DB.tasks[idx].closed_date=today;` ו-`_tskQuickStatus` שורות 8697-8698 — ונקרא בחמישה מקומות (5273, 5274, 5317, 11893, 11977).
השורה המכריעה היא ב-`svTsk()` שורות 8923-8926, שבונה את השורה מחדש בלי השדה ואז דורס: `var r={id:id,title:t,assignee:gv('tsk-assignee')||null,due:gv('tsk-due')||null,status:gv('tsk-status')||_TSK_OPEN,priority:...,source_table:...,source_id:...,parent_id:pid,project_id:...,notes:...,ts:new Date().toISOString()};` ואז `if(existingIdx>=0){DB.tasks[existingIdx]=r;sbUpd('tasks',r);...}`.

**השפעה**: היום — קל אבל אמיתי: משימה שנסגרת דרך המודאל לא מקבלת חותמת סגירה בכלל, ולכן היא נעדרת מ«משימות הושלמו» בכרטיס השבועי (5273) ומהספירה השנתית (11977). לדוח המבוקש — קטלני: סדרת האחוזים החודשית תחושב על תת-קבוצה מוטה של המשימות, וזה גרוע מלא להציג אותה.

**מיקום התיקון**: index.html:8917-8930 (`svTsk` — להעביר `closed_date` קיים קדימה ולחתום אותו כשהסטטוס הופך ל-`_TSK_DONE`, בדיוק כמו 8697-8698) — זה S בפני עצמו. הדוח החודשי עצמו: ליד `_renderKpiCard()` index.html:5013.

**הערה**: שני דברים שמשנים את התמונה. (1) `svTsk` מפיל גם את `ext_id` באותה דרך, ו-`_sbMergePull` מעדיף את העותק המקומי כל עוד הפעולה בתור — שורה 3272: `if((o==='ins'||o==='upd')&&local[r.id]){merged.push(local[r.id]);return;}` — כך שהאיבוד מורגש עד המשיכה הבאה. (2) **האלמוני החשוב**: `closed_date` לא מופיע באף מיגרציה. `migrations/2026-04-21_tasks.sql` יוצר את `tasks` בלעדיו, וה-ALTER-ים המאוחרים מוסיפים רק `ext_id`, `parent_id`, `project_id`. STATUS.md שורה 203 מצהיר «ללא migration — משתמש בעמודות קיימות», אבל תיקיית המיגרציות לא מגבה את זה. אם העמודה לא נוספה ידנית, כל PATCH שנושא `closed_date` נכשל ב-PGRST204 ונתקע בתור בניסיונות חוזרים — התרחיש הזה כבר קרה פעם אחת כאן והשאיר מנקה קשיח: `_obSanitize()` שורה 3425 `var BAD={equip_inspections:['last_inspection_date','status']};`. לא יכולתי לאמת מול Supabase — הרשת של סביבת הענן חוסמת את `*.supabase.co` (מתועד ב-CLAUDE.md). לפני כל עבודה על 6.10, שאילתה אחת ב-SQL Editor: `select column_name from information_schema.columns where table_name='tasks';`. אם `closed_date` חסר — מיגרציה של שורה אחת קודמת, ולפי כלל 7 היא לא מסומנת `[x]` עד שמיכאל מאשר שהיא רצה.


### 6.11 — ✅ confirmed · מאמץ M

**ראיה**: `_mrAINarrative` (index.html:12041) builds the full ISO 45001/14001 §9.3 four-part narrative and, on success, does exactly one thing with it — index.html:12119: `window._mrAIRaw=content;`. That is a page-lifetime JS variable. The only two buttons rendered beside the text (12114-12117) are `⧮ צור מחדש` (regenerate) and `📋 העתק` → `_mrAICopy` (12123), which only writes to the clipboard. There is no `fetch(SBU+'/rest/v1/...')` anywhere in the function. `_annualISOReport` (11960) has the identical shape — index.html:12033: `window._annualISORaw=content;`. `rMr()` (8547-8625) writes only into DOM ids (`g('mr-ncr-open').textContent=...`), nothing is stored. There is no review table anywhere: `_BACKUP_TABLES` (index.html:16780) — the app's own full table list — has no management-review entry, and none of the 78 files in migrations/ creates one (grep for mgmt/review/protocol returns only the two AI prompt strings at 12004 and 12084). Separately, STATUS.md:291 claims the page can be printed "דרך כפתור 🖨 הקיים בtopbar" — that button no longer exists; the topbar (index.html:758-778) holds ☰ · 🏠 · sync-pill · 🔔 · 📝 · ⋯ · 📱 and no printer.

**השפעה**: Michael runs the quarterly review, gets a narrative, and has to copy it into Word before he leaves the page — if the tab reloads on the iPhone first, the text is gone and regenerating costs another AI call and produces different wording. More seriously: nothing in the system records that a management review ever happened. §9.3 requires documented information as evidence of the results of management reviews. When an auditor says "show me the last three reviews — dates, inputs, decisions, who was there", the app has nothing; he is back to his own folder of Word files, which is the situation the page was built to end. And because no snapshot is kept, next quarter cannot be compared to this one.

**מיקום התיקון**: New migration `migrations/2026-09-XX_mgmt_reviews.sql`; save call inside `_mrAINarrative` right after index.html:12119 (and the same for `_annualISOReport` after 12033); register the table in `_sbTsCol` (index.html:3009) and `_DEL_PROTECTED` (index.html:3599); add it to `_BACKUP_TABLES` (index.html:16780); a list/view on `pg-mr` (index.html:1436-1445) plus a `VIEW_CONFIG` entry (index.html:11191) so `showView` opens a past review.

**הערה**: The precedent to copy is exact: `migrations/2026-04-18_ncr_ai.sql` persists versioned AI output per parent record with `version int`, `ts timestamptz`, `created_by` — same shape works for a review keyed by quarter. Two things that are easy to miss: (1) if the table is not added to `_BACKUP_TABLES` (16780) the nightly backup silently skips it, and (2) it must go into `_DEL_PROTECTED` (3599) like `ncr_ai` and `trustee_reports`, or `askDel` will happily delete minutes an auditor may ask for. Save the KPI snapshot as columns, not just the narrative text — that snapshot is what makes backlog item 6.4 (previous quarter + Δ) possible, since the prompt at 12084 already instructs the model to compare to the previous quarter while supplying no previous-quarter data. The table is new, so nothing destructive is involved; CLAUDE.md rule 7 still applies — do not tick it in STATUS.md until Michael confirms the migration ran.


### 6.12 — ✅ בוצע ב-**#660** (2026-09-20) · הראיה נשמרת למטה

**ראיה**: The tile is index.html:5109: `{v:c.lostDays,  cls:'kpi-r',label:'ימי אבדן',sub:'LTIF',go:"goPage('inc')"}` — label "ימי אבדן", sub-caption literally `'LTIF'`. The value comes from index.html:5226: `var kl=inc.reduce(function(s,i){return s+(i.dy||0);},0);` where `inc=DB.inc` (index.html:5212), so it is the plain sum of the `dy` column over the entire history — not a rate, and not even scoped to a period. LTIF is lost-time injuries × 1,000,000 ÷ hours worked; the app stores no man-hours anywhere (no settings/config table in `_BACKUP_TABLES` at 16780, and `dy` is written as a plain integer at index.html:4436 `dy:gi('i-dy')`). So the tile is a raw cumulative day count wearing the name of a frequency rate.

**השפעה**: Small on the home page, and it is worth saying why: the tile is candidate #8 in `cands` (index.html:5100-5112) and index.html:5113 does `cands.filter(v>0).slice(0,6)`. NCR פתוחים, משימות בפיגור, פג תוקף, סיכונים קריטיים, תקריות השנה, כמעט-ונפגע and PTW פעילות all sit ahead of it, so on this production database — 375 NCRs — at least two of those seven must be zero before the LTIF tile renders at all. Michael has probably never seen it. Where the mislabel actually reaches a reader is index.html:12007: the annual ISO surveillance report prompt asks section 2 for "LTIF, ימי אבדן, אחוז סגירת NCRs בזמן, מגמות", while the `data` object it is given (11983-12002) contains `incidents:{count, lost_days}` and no hours at all. The model is asked for a rate it cannot compute under a hard rule that says "אל תמציא נתונים" — so the external auditor's document either carries an invented LTIF or an awkward "לא נמצא בנתונים" in its headline KPI section.

**מיקום התיקון**: index.html:5109 (drop `sub:'LTIF'`, or replace it with what the number really is, e.g. the period); index.html:5226 (scope the sum to the current year so it matches the `incYear` tile beside it, using the same `new Date(i.dt).getFullYear()` test as index.html:5221); index.html:12007 (remove LTIF from the annual-report prompt, or add man-hours as an input first).

**הערה**: Do not "fix" this by computing a fake LTIF — there is no hours-worked figure in the system, and inventing a denominator in a document that goes to an ISO auditor is worse than the wrong caption. Either relabel (S, today) or, if Michael wants the real rate, that is a separate item: one annual man-hours field plus a count of incidents with `dy>0`, which is more than 60 lines once it needs a place to be stored and edited. Note that `mr-inc-sub` (index.html:8574) already presents the same quantity honestly — `lostDays+' ימי אבדן'` — so only the home tile is mislabelled; keep them consistent. Two things to sweep in the same pass while in this code: index.html:5226 uses the unguarded `i.dy||0` whereas index.html:8573 uses `parseInt(i.dy)||0`, and index.html:11888 in `_aiWeeklySummary` reads `parseInt(r.lost_days||0,10)` — but the column is `dy` (index.html:4436), so the weekly AI summary reports 0 lost days every single week regardless of the data.


### 6.13 — ✅ confirmed · מאמץ L

**ראיה**: Nothing in the file answers "are we getting safer". The only trend rendering that exists is `_ncrTrendSvg()` (index.html:15786) — a hand-rolled 12-month stacked bar of NCRs split saf/env — and `_ncrShowTrend` (index.html:15826) paints it with `P.innerHTML=_ncrTrendSvg()+legend+summary;` into `#ncr-agent-panel`, i.e. a 360px side panel inside the NCR Agent modal, reachable only from the 📈 מגמות button in that modal's header (index.html:15184). The management page renders period counts only — `rMr()` (8547) has no prior-period arm anywhere in 8547-8625. `_isoClauses()` (index.html:6204) answers "is the evidence there", never "is it better than last year": its recency test is `_isoRecent` (6190), a binary `String(v).substring(0,10)>=c`. The single comparison anywhere in the app is the dashboard insight at index.html:5190: `if(incThisWeek>incLastWeek&&incThisWeek>=2)ins.push('📈 השבוע נפתחו ...')` — one metric, week over week, and only fired when it got worse.

**השפעה**: When the plant manager or the board asks "is safety actually improving?", Michael has no answer he can hand over. He can show open counts, an ISO evidence sheet, and a four-part AI narrative that was told to compare to last quarter while being given no last-quarter numbers (index.html:12084 prompt vs. the `kpis` object at 12046-12065). The direction of travel — incidents, near-miss reporting rate, NCR closure speed, training compliance, this year against last — exists in the database and is visible nowhere. The one real 12-month chart in the product is buried behind a modal he would have to already know about.

**מיקום התיקון**: New `window.safetyTrendReport()` beside `window.isoAuditReport` (index.html:6289), reusing its print-window pattern verbatim (the `@page size:A4` stylesheet at 6311-6335, the `<button class="no-print" onclick="window.print()">` at 6336, the signature `.foot` at 6347 and the `window.open('','_blank')` at 6349); a `_trendSeries(tbl,fld,months)` helper next to `_isoRecent` (index.html:6190); an entry point in the reports sheet beside the ISO item at index.html:8766.

**הערה**: Check DECISIONS.md before shaping this. The entry "2026-04-19 — Revert: הסרת Dashboard 2.0 Phase A" (DECISIONS.md:1274-1279) records that four Chart.js graphs were put on the dashboard in PR #16 and Michael asked for them removed — "הגרפים לא סיפקו ערך מספיק או לא התאימו לזרימת העבודה היומיומית" — and the Chart.js CDN went with them. So this must not return as charts on the home page or as a bundled library (CLAUDE.md rule 6 forbids the build step anyway). The shape the claim proposes is exactly the one that survived: a separate, printable, on-demand sheet built from hand-written SVG, which is also what backlog 6.6 assumes ("SVG ידני, בלי ספרייה") and what `_ncrTrendSvg` already proves works here. Sequencing matters more than the L: 6.6 (12-month opened/closed/backlog) and 6.7 (sparklines for near-miss, trustee reports, rounds, toolbox) are the components of this sheet — build those two first and 6.13 becomes assembly rather than L, which is why the backlog itself files 6.6/6.7 under "החודש" and 6.13 under "פרויקט". One trap for any YoY helper: the date column differs per table — `dt` on `inc`, `d` on `near_miss`/`rounds`/`toolbox`, `sd`/`cd` on `ncr` — so take the field name as an argument the way `_isoRecent(tbl,fld,months)` already does; a caller that guessed wrong is live right now at index.html:11888 (`r.lost_days` instead of `dy`), and it reports zero forever without ever erroring.
