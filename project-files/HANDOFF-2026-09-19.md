# HANDOFF — 2026-09-19 · העברת הקשר לחשבון Claude השני

**למי:** הסשן הבא של Claude (בחשבון השני של מיכאל). **ממי:** הסשן שבנה את פלטפורמת נאמני הבטיחות (2026-09-18/19, PRs #535–#577).
**איך להתחיל בסשן החדש:** לקרוא `CLAUDE.md` → `STATUS.md` → `DECISIONS.md` → את הקובץ הזה → `project-files/SPEC-trustees-2026-09-18.md`. הכל ב-`main`. אין עבודה פתוחה על branches.

---

## 1. מי המשתמש ואיך עובדים איתו

- **מיכאל פרייליך** (mishaf1988@gmail.com), מנהל בטיחות בתעשיות תפוגן. **לא מפתח.** עובד בעיקר מהאייפון, שולח צילומי מסך, כותב קצר ("1", "בוצע", "מתקן?").
- מעדיף: תשובות קצרות בעברית, צעד-אחר-צעד עם קישורים כשזה תהליך ידני, בלי ז'רגון. כשמשהו לא עובד — לבקש צילום מסך.
- **כללי הריפו** (CLAUDE.md): קובץ יחיד `index.html`, עברית ב-JS רק כ-`\uXXXX`, תאריכים ריקים = `null`, לא לגעת ב-RLS קיים / `wa-send` / bnav, **כל PR ממוזג ל-`main` באותה ריצה** (Cloudflare Pages מפרסם רק `main` ל-https://tapugan-safety.pages.dev).
- דפוס העבודה שהוכיח את עצמו: branch → עריכה ב-Python by-anchor (Edit tool מפענח `\u` לעברית גולמית — אחרי כל עריכה להריץ re-escape על השורות שנוספו ב-`<script>`) → syntax check (3 בלוקי script, 0 כשלים) → הארנס Chromium headless על `index.html` מ-`file://` ב-375px עם רשת חסומה → commit עם footer → push → PR → merge (squash) → `git reset --hard origin/main` → שורת `[x]` ב-STATUS. ההארנסים **נמצאים עכשיו ב-repo**: `tests/harness/` (`bash tests/harness/run.sh`).

## 2. מה נבנה (הכל באוויר)

### פלטפורמת נאמני בטיחות (מחליפה את קבוצת הוואטסאפ)
מסמך המקור: "נאמן בטיחות — המשימות החודשיות" (8 משימות, 10 נק׳ למשימה עד 80, 2 לכל מפגע שנסגר עד 20, זכאות ≥5, שובר שוויון = מפגעים שנסגרו). גיליון הנאמנים: 8 שמות (לב, גלינה, ויטלי ויינר, טטינה, מוסא, רנט, עאיד, יוני).

| PR | מה |
|---|---|
| #559 T1 | טבלה `trustee_reports` + RLS (policy לכל פעולה: INSERT/SELECT לכל authenticated, UPDATE/DELETE admin/manager) + טריגרים (גזירת `m`, נרמול `s`, **סגירה דרך דיווח `t=8`+`ref`** — הנאמן סוגר בלי UPDATE). `TRUSTEE_TASKS`, `_truScore`, `_truBoard`. |
| #560 T2 | מסך הנאמן במצב עובד (`#emp-trustee`, `tru-only`), טופס `m-tru`, «צלם אחרי», `_truPull`. `_sbMergePull` הוצא מ-`sbSync`. |
| #561 T3 | מסך המנהל `pg-trustees`: חודש, פילים, לוח ניקוד, ממצאים, ⋯ (נסגר/פתח/משימה/הערת ניתוב/מחק), CSV לוועדה, שורת «היום» «N ממצאים ממתינים לניתוב». |
| #562 | בית העובד = מסך הנאמן בלבד (`EMP_HOME_TRUSTEES_ONLY`, 4 האריחים מוסתרים). |
| #563 | כניסה ללא סיסמה נקראת «🦺 דיווח נאמני בטיחות». |
| #564 | טבלה `trustees` (רשימה, seed 8 שמות) + `trustee_reports.tour`; בוררי שם = select + «אחר»; מנהל: ⋯ → «רשימת הנאמנים». |
| #565 | «איך זה עובד?» בכניסה; **סיור = כמה משימות**; «🏆 מי מוביל» + דירוג; «התחרות חודשית — מתאפס ב-1». **תיקון מרוץ ב-`_obDrain`** (פעולות שנכנסו בזמן העלאה נמחקו — באג ותיק בכל האפליקציה). |
| #566 | **כל משימה = רשימת פריטים** (עמדה/נקודה), לכל פריט תקין/ליקוי + תמונה; המיקום נשמר «מחסן · מטף מזרחי». |
| #567 | לוגו תפוגן: `logo.jpg` נחתך משוליים לבנים, כניסה 120px, טופ-בר 48/52px, בטלפון הכותרת ליד הלוגו מוסתרת. |
| #568 | טופ-בר נקי במצב נאמן (☰ 🔍 🔔 📝 ⋯ מוסתרים, לוגו 54px ממורכז). |
| #569–#571 | **ליקוי נאמן → WhatsApp/מייל מהשרת**: טריגר Supabase (pg_net) → `functions/api/trustee-notify.js` → claim על `notified_at` → Meta (תבנית `tfugen_trustee_hazard` אם קיימת, אחרת `tfugen_incident_alert`) / Resend. הגדרות: פעמון 🔔 → שורה «ליקוי מנאמן בטיחות» + יעדים + «שלח בדיקה». **נבדק מקצה לקצה: WhatsApp הגיע למיכאל.** `RESEND_KEY` נוסף ב-Cloudflare. |
| #572–#577 | `tools/vm-notifier/` — סקריפט Python למחשב דלוק ששולח מייל מתיבת מיכאל (Outlook / SMTP / Brevo-HTTPS), installer מודרך ללינוקס, README בעברית. **מיכאל ניסה DigitalOcean, SMTP חסום שם, מחק את השרת.** |

### תשתית שנוגעת לזה
- **Supabase** `znhjtpcltrxxyfjczgvw`: טבלאות חדשות `trustee_reports` (עמודות `id,u,m,t,d,location_id,loc,ok,f,photo_url,s,ref,mgr_note,tour,notified_at,ts`), `trustees` (`id,n,dep,ph,active,notes,ts`). טריגרים ב-`private.*`. `pg_net` מופעל (webhook). מיגרציות: `migrations/2026-09-18_trustee_reports.sql`, `2026-09-18_trustees_roster.sql`, `2026-09-19_trustee_notify_webhook.sql` — כולן הוחלו חי עם בדיקות persona.
- **Cloudflare Pages** `tapugan-safety`: env כולל `SUPABASE_SERVICE_ROLE_KEY`, `META_*`, ועכשיו `RESEND_KEY`.
- **Meta WhatsApp:** מספר בדיקה (+1 555…) — שולח רק ל-5 נמענים מאושרים; למיכאל מספיק. מספר עסקי אמיתי = שלב עתידי.
- **Resend:** החשבון על mishaf1988@gmail.com; במצב ניסיון שולח **רק לכתובת הזו**. ל-sviva@tapugan.co.il צריך אימות דומיין (3 רשומות DNS אצל IT).

## 3. החלטות שמיכאל קיבל (לא לפתוח מחדש בלי סיבה)
1. כניסת נאמן = דיווח ללא סיסמה (session אנונימי); הנאמן בוחר את שמו מהרשימה.
2. ליקוי **לא** הופך למשימה אוטומטית — מיכאל מנתב ידנית (משימה / הערת ניתוב).
3. אזורים = `locations` הקיים, אבל בטופס המיקום הוא טקסט חופשי חובה (הקטלוג לא קריא ל-session אנונימי ו-RLS לא שונה).
4. בית העובד = רק נאמנים; הכפתורים הגנריים מוסתרים (דגל `EMP_HOME_TRUSTEES_ONLY`).
5. **לא** לבנות אוטומציה של WhatsApp מהמספר האישי (סיכון חסימה) — הוסבר פעמיים, מיכאל קיבל.
6. השליחה מהשרת (Cloudflare) היא הפתרון הקבוע; המחשב/שרת הוא רק לערוץ מייל מתיבה שהשירות לא מורשה לשלוח אליה.
7. accepted risk: advisor `multiple_permissive_policies` (10) — לא לגעת (DECISIONS.md).

## 4. מה פתוח / מה מיכאל אמר שיעשה
- **מחשב קבוע כשרת (מייל מ-sviva@):** מיכאל אמר "אקנה מחשב שיעבוד קבוע" ואז "זה אחרי כבר" — **דחוי**. כשיחזור: `tools/vm-notifier/README.md`, מצב Outlook, `start.bat` + `shell:startup`, ובאפליקציה Email לא מסומן. הוא גם הזכיר "שירות של גרוק" — לא ברור מה זה, לשאול.
- **מייל מהשרת ל-sviva@:** אימות דומיין tapugan.co.il ב-Resend (Domains → Add domain → 3 רשומות DNS ל-IT) — או לשלוח ל-Gmail שלו (עובד עכשיו).
- **תבנית Meta ייעודית** `tfugen_trustee_hazard` (he, 3 פרמטרים: נאמן / משימה — מיקום / ממצא) — מיכאל יוצר במטא; השרת עובר אליה אוטומטית.
- **שלב 2 של הנאמנים (לא בהיקף):** הכרזת זוכה + היסטוריה ("מי שטרם זכה"), תזכורות אישיות, עריכת קטלוג המשימות בלי קוד.
- **ישן, עדיין פתוח (מסבבים קודמים):** Leaked-Password Protection ב-Supabase Auth; מחיקת ה-Edge Function `empty-ops-buckets` (מנוטרלת); סיבוב מפתח Resend (הושלם בפועל — מפתח חדש ב-Cloudflare 2026-09-19).
- **מיקום `EMP_HOME_TRUSTEES_ONLY` / `TRUSTEE_PHOTO_REQUIRED_OK`:** דגלים ב-`index.html` ליד הקוד של הנאמנים, למקרה שיבקש להחזיר את האריחים או לחייב תמונה גם לתקין.

## 5. מפת קוד מהירה (index.html)
- CSS נאמנים: אחרי `body.emp-mode #emp-mode-badge` (`.tru-*`, `tru-only`, `tru-open`).
- מסך העובד: `#pg-emp-home` → `#emp-trustee` (intro `#tru-intro`, `#tru-me` select, `#tru-body`).
- מודאלים: `#m-tru` (טופס סיור), `#m-tru-roster` (רשימה), `#m-notif` (תיבת יעדים `#notif-trustee-box`).
- JS: בלוק `// ---- SAFETY TRUSTEES` לפני `// ---- EMPLOYEE MODE (Stage A) ----`: `TRUSTEE_TASKS`, `_truScore/_truBoard`, `_truRender`, `_truForm*` (טופס), `svTru`, `_truPull`, `_truEmpHome`, `_truRoster*`, `rTrustees` + `_truMgr*`, `_truExportCsv`, `_truNotifyTest`.
- Sync: `_sbMergePull` (לפני `sbSync`), `_obDrain` (מיזוג בסיום).
- שרת: `functions/api/trustee-notify.js`; כלי: `tools/vm-notifier/`.
- הארנסים: `tests/harness/*.js|.mjs|.py`, `run.sh`.

## 6. פקודות שימושיות לסשן הבא
```
git log --oneline -20                          # מה היה
bash tests/harness/run.sh trustee              # להוכיח שהנאמנים עובדים (צריך playwright)
grep -n "SAFETY TRUSTEES" index.html           # להגיע לקוד
```
ב-Supabase (MCP): `select count(*) from trustee_reports` / `from trustees`; `select * from net._http_response order by id desc limit 3` (תוצאות ה-webhook).

---
*נכתב ע"י Claude בסיום הסשן, 2026-09-19. אם משהו כאן סותר את הקוד — הקוד צודק, ולעדכן את הקובץ.*
