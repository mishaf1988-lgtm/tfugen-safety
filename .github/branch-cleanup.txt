report
# First line is the mode for the push-triggered run of cleanup-merged-branches.yml: delete or report.
# "also:" lines (push mode only) name branches to delete regardless of PR state, never main.
